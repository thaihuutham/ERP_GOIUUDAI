# Session Log — 2026-04-09 11:35 +07

## User request
- Commit toàn bộ dự án, push lên GitHub, sau đó bắt đầu thực hiện nhiệm vụ theo kế hoạch.

## Work completed
- Đã commit + push toàn bộ trạng thái đang dở của migration role/policy-as-data:
  - Commit: `c1cc342`
  - Branch: `main`
  - Remote: `origin/main`
- Bắt đầu execution theo kế hoạch bằng việc ổn định lại gate sau checkpoint:
  - Sửa web role literals còn gây fail compile (`MANAGER/STAFF` -> `ADMIN/USER`) ở các điểm typed mạnh.
  - Chuẩn hóa permission UI cho thao tác nhạy cảm:
    - CRM manual mark-paid: chỉ `ADMIN`.
    - Sales checkout payment override: chỉ `ADMIN`.
  - Cập nhật `access-policy` unit test theo role model mới `ADMIN/USER`.
  - Sửa migration SQL `20260409120000...` để tránh lỗi enum cast (`USER` trên enum cũ).
  - Recover migration failed state bằng `prisma migrate resolve --rolled-back ...` rồi deploy lại thành công.

## Files changed (session này)
- `apps/api/prisma/migrations/20260409120000_admin_user_role_and_user_position_assignment/migration.sql`
- `apps/web/components/crm-operations-board.tsx`
- `apps/web/components/sales-checkout-board.tsx`
- `apps/web/lib/__tests__/access-policy.test.ts`
- `apps/web/lib/assistant-routes.ts`
- `apps/web/lib/module-definitions.ts`
- `apps/web/lib/modules.ts`
- `apps/web/next-env.d.ts`

## Verification
- Git checkpoint:
  - `git add -A && git commit -m "chore: checkpoint admin-user role migration and policy-as-data draft"` ✅
  - `git push origin main` ✅
- Infra/DB gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- Backend gate:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
- Frontend gate:
  - `npm run test:unit --workspace @erp/web -- lib/__tests__/access-policy.test.ts` ✅
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅

## Notes
- Đã hoàn tất đúng yêu cầu “commit + push trước, rồi bắt đầu task theo kế hoạch”.
- Migration `20260409120000...` đã được apply thành công trên local DB sau khi fix SQL.
- Vẫn còn nhiều literal `MANAGER/STAFF` rải rác trong codebase chưa được cutover hết; cần xử lý tiếp ở các bước sau của plan big-bang `ADMIN/USER`.
