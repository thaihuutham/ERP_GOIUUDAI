# Session Log - 2026-03-31 09:22 (Codex)

## Scope
Điều tra lỗi runtime trên web: `Application error: a client-side exception has occurred while loading localhost`.

## Changes Completed
- Reproduce lỗi bằng Playwright headless và thu được lỗi browser:
  - `Loading chunk ... failed` (chunk JS 404) ở frontend.
- Xác định root cause chính:
  - Web server cổng `3000` đang phục vụ bundle stale/không đồng bộ => client crash khi tải chunk.
- Xử lý runtime:
  - restart web server bằng `npm run dev --workspace @erp/web`.
  - verify lại route `/`, `/modules/hr`, `/modules/hr/personal-income-tax` đều `200`, không còn `pageerror`.
- Phát hiện thêm sau khi sửa web:
  - API cổng `3001` trả `404/500` cho endpoint PIT do schema DB local chưa sync với model mới.
- Xử lý API/runtime DB:
  - restart API server `npm run dev --workspace @erp/api`.
  - đồng bộ schema dev bằng `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:push --workspace @erp/api`.
  - verify `GET /api/v1/hr/personal-income-tax/records?limit=20` trả `200`.

## Verification
- Playwright check routes:
  - `/` => `200`, không pageerror
  - `/modules/hr` => `200`, không pageerror
  - `/modules/hr/personal-income-tax` => `200`, không pageerror
- API check:
  - `GET /api/v1/hr/personal-income-tax/records?limit=20` => `200`

## Notes
- Không thay đổi source code trong session này; chỉ xử lý runtime + sync schema local dev.
- `prisma migrate dev` không dùng được trực tiếp vì migration cũ đã bị chỉnh sau khi apply; dùng `prisma db push` để tránh reset dữ liệu local.
