# Session Log: 2026-03-30_1432

## THÔNG TIN SESSION

```
Ngày/giờ bắt đầu : 2026-03-30 14:24
Ngày/giờ kết thúc: 2026-03-30 14:32
Agent            : Codex
Account/Instance : Local desktop session
Task đang làm    : Điều tra và sửa lỗi không hiển thị dữ liệu demo trên dashboard
```

## TÓM TẮT SESSION
User báo “không thấy dữ liệu demo”. Điều tra cho thấy API đang resolve tenant thành `tenant_demo_company` nên các endpoint trả dữ liệu rỗng/0. Đã sửa web client để luôn gửi `x-tenant-id` (và gửi thêm custom tenant header nếu có), giúp dashboard/module đọc đúng tenant `GOIUUDAI` đã seed.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Kiểm tra runtime local:
  - Web đang chạy `localhost:3000`
  - API đang chạy `localhost:3001`
- Xác minh root cause bằng API call:
  - Không gửi header tenant -> `/reports/overview` trả toàn 0.
  - Gửi `x-tenant-id: GOIUUDAI` -> số liệu trả đúng (>0).
- Kiểm tra log request API:
  - request từ web trước fix có `tenantId: tenant_demo_company`.
- Sửa `apps/web/lib/api-client.ts`:
  - default tenant header key về `x-tenant-id`.
  - luôn gửi `x-tenant-id`.
  - nếu có `NEXT_PUBLIC_TENANT_HEADER_KEY` custom thì gửi thêm song song.
- Verify:
  - `npm run lint --workspace @erp/web` ✅
  - `curl -H 'x-tenant-id: GOIUUDAI' http://localhost:3001/api/v1/reports/overview` ✅

## THAY ĐỔI FILE

```
ADDED:
  .agent/sessions/2026-03-30_1432_codex.md

MODIFIED:
  apps/web/lib/api-client.ts
  planning/CURRENT_TASK.md
  .agent/memory/CONTEXT_SNAPSHOT.md
```

## HANDOFF CHO AGENT TIẾP THEO
1. Nếu user còn thấy 0 ở UI, ưu tiên yêu cầu hard refresh trình duyệt để nạp lại bundle mới.
2. Kiểm tra thêm env strategy của API (`ConfigModule envFilePath`) để đảm bảo local luôn load đúng `.env` khi chạy từ root/workspace.
3. Tiếp tục giữ server `localhost:3000` running sau mỗi thay đổi để user UAT.
