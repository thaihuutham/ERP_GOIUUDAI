# Session Log - 2026-04-08 09:30 +07

## Scope
- User yêu cầu thực thi ngay Phase 1.5 sau Phase 1 pagination/sorting server-driven cho `StandardDataTable`.
- Mục tiêu session: stabilization sau rollout bằng cách sửa regression E2E theo UX hiện tại (bulk modal + route guard) và chốt System Stability Gate.

## Changes
- `apps/web/e2e/tests/assistant-module.spec.ts`
  - cập nhật route-guard assertion theo behavior redirect `Tổng quan` + banner quyền.
  - chuyển bulk flows (runs/knowledge/channels) sang thao tác qua `Bulk Actions` modal.
  - fix selector `Bulk Actions` để tránh strict-mode conflict khi nhiều bảng.

- `apps/web/e2e/tests/workflows-module.spec.ts`
  - chuyển inbox bulk approve sang flow modal thực tế.
  - đóng bulk modal trước khi thao tác panel `Xử lý hàng loạt ...` để tránh pointer interception.

- `apps/web/e2e/tests/audit-module.spec.ts`
  - cập nhật heading assertion `Nhật ký hệ thống`.
  - read-only bulk utilities assert trong modal (`Copy IDs`, `Export CSV`, `Clear selection`).

- `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`
  - thêm helper `waitForInvoiceRow(...)` để giảm flaky timing khi finance invoices table load chậm/tạm rỗng.

## Tests & Verification
- Docker/DB gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅

- Build/lint gate:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅

- E2E gate:
  - Focused regressions (`assistant bulk`, `workflows bulk`) ✅
  - Full targeted suite:
    - `assistant-module.spec.ts`
    - `audit-module.spec.ts`
    - `workflows-module.spec.ts`
    - `crm-sales-finance-core-flow.spec.ts`
    - run with `--workers=2`
    - result: ✅ `19 passed`

## Notes
- Không thay đổi business logic runtime/API contract trong session này; chỉ cập nhật regression tests.
- Không thêm migration/schema.
- Không tạo ADR mới.
