# Session Log - 2026-04-02 18:30 +07

## Muc tieu
Sua loi runtime `Cannot GET /api/v1/hr/attendance/monthly`, toi uu bo loc thang/nam tren UI attendance, va dieu chinh bang cham cong gon hon theo rule hien thi moi.

## Thay doi chinh

### 1) Runtime/API
- Xac nhan source da co route:
  - `GET /api/v1/hr/attendance/monthly` trong `apps/api/src/modules/hr/hr.controller.ts`.
- Xac nhan runtime loi truoc khi restart:
  - `GET /api/v1/hr/attendance/monthly?year=2026&month=4` => `404`.
- Restart API process tren cong `3001` bang `npm run dev` tai `apps/api`.
- Verify sau restart:
  - `GET /api/v1/hr/attendance/monthly?year=2026&month=4` => `200`.

### 2) Regression smoke test API
- Cap nhat `apps/api/test/hr.api-flow.test.ts`:
  - them test `serves GET /api/v1/hr/attendance/monthly and maps year/month query correctly`.
  - assert status `200`, response payload, va mapping query `year/month` vao `hrService.getAttendanceMonthly('2026', '4')`.

### 3) Frontend attendance board
- Cap nhat `apps/web/components/hr-attendance-board.tsx`:
  - thay filter `YYYY-MM` bang 2 state rieng:
    - `selectedMonth: number`
    - `selectedYear: number`
  - bo sung 2 dropdown:
    - `Thang` (1..12)
    - `Nam` (current year +/- 5)
  - query monthly gui truc tiep `year/month` tu 2 state.
  - toi uu bang:
    - `table-layout: fixed`
    - giam width/padding/font cot ngay
    - giu horizontal scroll nhe khi can
    - them border ro rang cho ca `th/td`.
  - doi rule render o ngay:
    - `WORKED` => `HH:mm`
    - `NO_DATA` => rong (`''`)
    - `EXEMPT` => rong (`''`)
  - giu `Mien cham cong` o metadata phuong phap + cot tong thang.

### 4) E2E
- Cap nhat `apps/web/e2e/tests/hr-attendance-board.spec.ts`:
  - assert co 2 dropdown rieng `Thang` va `Nam`.
  - doi filter thang/nam gay reload monthly data.
  - assert o ngay `NO_DATA/EXEMPT` de trong.
  - assert khong con `--` trong o ngay.
  - assert van hien thi `Mien cham cong` o metadata/tong thang.

## Verify
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/hr.api-flow.test.ts test/hr-attendance.service.test.ts` ✅ (`8 passed`)
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `PLAYWRIGHT_PORT=4180 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test --config=apps/web/e2e/playwright.config.ts apps/web/e2e/tests/hr-attendance-board.spec.ts --reporter=line` ✅ (`2 passed`)

## Ghi chu
- Khong thay doi contract API monthly.
- Khong tao ADR moi (thay doi runtime + UI + test, khong phat sinh quyet dinh kien truc moi).
