# Session Log - 2026-04-08 06:27 +07

## Scope
- User yêu cầu:
  - `Commit + push GitHub`.
  - nâng cấp CRM customer filter thành **server-side filtering toàn bộ dataset** (không chỉ tập đã load trên web).

## Changes
- Backend: server-side custom filter query
  - `apps/api/src/modules/crm/crm.controller.ts`
    - `GET /crm/customers` forward thêm `customFilter` từ query request xuống service.
  - `apps/api/src/modules/crm/crm.service.ts`
    - mở rộng signature `listCustomers(...)` nhận `customFilter`.
    - parse/validate `customFilter` JSON theo đúng contract conditions đang dùng cho saved filter.
    - map condition -> Prisma `where` cho cả field cơ bản + relation.
    - combine filter vào DB query trước pagination (`take/cursor`) để đảm bảo đúng trên full dataset.
    - khi có `customFilter`, skip nhánh hybrid keyword search để tránh lệch kết quả relation filters.
- Frontend: gửi applied filter lên API
  - `apps/web/components/crm-customers-board.tsx`
    - thêm `toCustomerFilterQueryPayload(...)` để serialize filter draft.
    - `loadCustomers()` gửi `customFilter` trong query `/crm/customers`.
    - trigger reload khi trạng thái filter áp dụng thay đổi.
- Tests
  - `apps/api/test/crm.service.test.ts`
    - thêm unit test cho customFilter DB query + xác nhận không gọi `searchCustomerIds` hybrid branch.
  - `apps/api/test/crm.api-flow.test.ts`
    - thêm integration test xác nhận controller forward `customFilter` xuống service.

## Tests & Verification
- System gate
  - `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ⚠️ không có `erp-postgres` (chỉ có `seo-postgres`, `seo-redis`, `codex-lb`).
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ❌.
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ❌ (`localhost:55432` unreachable).
- Quality gate
  - `npm run lint --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run build --workspace @erp/web` ✅
- Test gate
  - `npm run test --workspace @erp/api -- --run test/crm.service.test.ts` ✅
  - `npm run test --workspace @erp/api -- --run test/crm.api-flow.test.ts` ❌ (permission guard đọc setting DB, fail do DB unreachable).
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅

## Notes
- Không thêm migration/schema mới.
- Blocker chính của gate còn lại là local DB runtime không up tại `localhost:55432`.
