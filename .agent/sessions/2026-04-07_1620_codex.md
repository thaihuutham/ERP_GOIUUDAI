# Session Log - 2026-04-07 16:20 +07

## Scope
- Implement plan Customer 360 trong chat cho phase 1 Zalo Personal:
  - strict ID-first matching;
  - panel Customer 360 trong màn chat;
  - quick-create customer từ thread (transaction + idempotent);
  - gán thủ công thread -> customer;
  - dọn route trùng `/modules/crm/conversations` thành alias redirect.

## Changes
- Data + migration:
  - `apps/api/prisma/schema.prisma`
    - thêm `Customer.needsSummary`.
  - `apps/api/prisma/migrations/20260407173000_add_customer_needs_summary/migration.sql`
    - thêm cột `needsSummary` cho bảng `Customer`.
- CRM backend:
  - `apps/api/src/modules/crm/crm.service.ts`
    - create/update customer hỗ trợ `needsSummary`.
  - `apps/api/src/modules/crm/crm-contracts.service.ts`
    - giữ `GET /crm/customers/:id/customer-360` làm payload tổng hợp;
    - `resolveCustomerIdForExternalIdentity` chuyển sang strict ID-only (không fallback phone).
  - `apps/api/src/modules/crm/crm.controller.ts`
    - expose `GET /crm/customers/:id/customer-360`.
- Conversations backend:
  - `apps/api/src/modules/conversations/conversations.controller.ts`
    - thêm:
      - `POST /conversations/threads/:id/link-customer`
      - `POST /conversations/threads/:id/quick-create-customer`
  - `apps/api/src/modules/conversations/conversations.service.ts`
    - `listThreads` trả thêm `matchStatus: matched|unmatched|suggested`;
    - bổ sung `suggestedCustomer` (gợi ý phone-only, không auto-link);
    - bổ sung `identityHint` cho UI prefill;
    - implement `linkThreadCustomer(...)`;
    - implement `quickCreateCustomerFromThread(...)` transaction:
      - nếu thread đã linked hoặc social identity đã có -> idempotent link;
      - nếu chưa có -> create customer tối thiểu + create social identity + link thread.
- Web UI:
  - `apps/web/components/zalo-automation-messages-workbench.tsx`
    - mở rộng thread list hiển thị trạng thái nhận diện;
    - thêm panel Customer 360 bên phải (core profile, summary, needsSummary, quick interaction log, history);
    - thêm thao tác unmatched/suggested:
      - gán thủ công theo customerId;
      - gán nhanh từ suggested customer;
      - popup `Tạo khách nhanh` prefill social ID + thông tin biết trước.
  - `apps/web/app/styles/modules/crm.css`
    - mở rộng layout 3 cột + style Customer 360 panel.
  - `apps/web/app/modules/crm/conversations/page.tsx`
    - đổi thành redirect alias sang `/modules/zalo-automation/messages`.
  - `apps/web/components/app-shell.tsx`
    - title `/modules/crm/conversations` đồng bộ về `Zalo Automation • Tin nhắn`.
  - `apps/web/components/crm-customers-board.tsx`
    - hỗ trợ deep-link `?customerId=` để mở nhanh hồ sơ đầy đủ từ panel chat.
- ADR mới:
  - `docs/decisions/ADR-052-CUSTOMER360-CHAT-ZALO-STRICT-ID-FIRST.md`.

## Tests & Verification
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- Backend quality:
  - `npm run prisma:generate --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- --run test/conversations.realtime.test.ts test/crm-contracts.service.test.ts` ✅
- Frontend quality:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
- E2E targeted:
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Notes
- `test/conversations.api-flow.test.ts` vẫn có lỗi bootstrap nền đã biết (scheduler/pool init), không phải regression từ patch này.
- `apps/web/tsconfig.tsbuildinfo` tiếp tục thay đổi do incremental typecheck/build.
