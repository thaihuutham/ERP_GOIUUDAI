# Session Log - 2026-04-06 17:52 +07

## Scope
- Kiểm tra chênh lệch số liệu giữa `Tiến độ` ở danh sách campaign và bảng account trong phần `Chi tiết Campaign`.
- Làm rõ nguyên nhân và vá UI để tránh hiểu nhầm vận hành.

## Findings
- `Tiến độ` (`S/P/K/F`) lấy từ tổng trạng thái recipients toàn campaign (groupBy theo `ZaloCampaignRecipient.status`).
- Bảng account detail (`Sent/Skipped/Failed`) lấy từ counters cấp account (`zalo_campaign_accounts.sentCount/skippedCount/failedCount`) chỉ phản ánh bản ghi đã được claim/gắn account trong runtime.
- Các recipient `SKIPPED` với lý do `NO_TARGET_THREAD` trong snapshot có `targetAccountId = null` nên:
  - xuất hiện ở `Tiến độ` và `Recipients Snapshot`;
  - không xuất hiện ở dòng account cụ thể.

## Changes
- `apps/web/components/zalo-automation-campaigns-workbench.tsx`
  - thêm tính `unassignedRecipientStats` từ `recipients` (những dòng chưa gán account);
  - thêm dòng `Chưa gán account` trong bảng account detail để hiển thị số liệu chưa map account;
  - thêm chú thích dưới bảng giải thích ý nghĩa chênh lệch và số `NO_TARGET_THREAD` chưa gán account.
- `apps/web/app/styles/modules/crm.css`
  - thêm style `zalo-campaign-unassigned-row` để tô nền phân biệt dòng chưa gán account.

## Verification
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)

## Notes
- Không thay đổi business logic gửi campaign; chỉ bổ sung hiển thị để reconcile số liệu rõ ràng theo đúng bản chất dữ liệu.
