# Session Log — 2026-04-08 17:14 +07

## User request
- Không thấy chỗ đặt webhook gửi dữ liệu sang n8n.
- Không thấy bảng để chia nick Zalo vào ngành.

## Work completed
- Thêm trang quản trị mới: `Zalo Automation > AI Routing`.
  - File component: `apps/web/components/zalo-automation-ai-routing-workbench.tsx`
  - Route page: `apps/web/app/modules/zalo-automation/ai-routing/page.tsx`
- Thêm menu sidebar cho trang mới:
  - `apps/web/lib/sidebar-config.ts`
- Thêm title mapping trong app shell:
  - `apps/web/components/app-shell.tsx`

## AI Routing page scope
- Runtime webhook n8n:
  - `mode` (`legacy|n8n|shadow`)
  - `chatEventsUrl`
  - `outboundHmacSecret`
  - `callbackHmacSecret`
  - `debounceSeconds`
  - `dispatchTimeoutMs`
  - `maxRetryAttempts`
  - `retryBackoffSeconds`
  - Lưu qua `PUT /api/v1/settings/domains/integrations` tại nhánh `integrations.aiRouting`.
- Bảng ngành AI:
  - List/search/create/update/toggle active qua `ai-industries`.
- Bảng map nick kênh -> ngành:
  - List/search/create/update/toggle active qua `ai-routing/channel-accounts`.
- Bảng map ngành -> workflow/agent:
  - List/search/create/update/toggle active qua `ai-routing/industry-bindings`.

## Verification
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Notes
- Không thay đổi backend business logic/schema trong batch này.
- Không phát sinh ADR mới (thay đổi UI expose cho API đã có).
