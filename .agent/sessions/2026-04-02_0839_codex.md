# Session Log — 2026-04-02 08:39 +07

## Mục tiêu
- Đổi kiến trúc integrations để cho phép nhập key/token trực tiếp từ UI.
- Đảm bảo key mới có hiệu lực runtime ngay sau khi lưu, không cần restart container/docker.

## Thay đổi đã thực hiện
- Backend settings/runtime:
  - `apps/api/src/modules/settings/settings-policy.types.ts`
    - thêm field key trực tiếp vào defaults:
      - `bhtot.apiKey`
      - `ai.apiKey`
      - `zalo.accessToken`
      - `zalo.webhookSecret`
  - `apps/api/src/modules/settings/settings-policy.service.ts`
    - thêm resolver `resolveSecretValue(secretValue, secretRef, envFallback)`.
    - cập nhật `testConnection` và `hasSecret/isConfigured` theo precedence:
      - `direct key` > `secretRef` > `ENV fallback`.
    - bỏ chặn validation với key thô ở domain `integrations`.
    - normalize/persist cả key trực tiếp và `*Ref`.
    - legacy bridge (`buildLegacySystemConfig`, `syncFromLegacySystemConfig`) hỗ trợ `apiKey` trực tiếp.
  - `apps/api/src/common/settings/runtime-settings.service.ts`
    - runtime integrations đọc key trực tiếp trước, rồi mới `secretRef`, rồi env fallback.
    - giữ cache invalidate sau update domain để runtime apply ngay.
  - `apps/api/src/modules/settings/settings.service.ts`
    - BHTOT sync lấy key theo precedence mới.
    - normalize legacy `apiKey`/`apiKeyRef` tương thích ngược.

- Frontend Settings Center:
  - `apps/web/components/settings-center.tsx`
    - thêm `FieldType = 'secret'` và render input password.
    - thêm field nhập key trực tiếp cho mọi connector key chính:
      - `bhtot.apiKey`
      - `ai.apiKey`
      - `zalo.accessToken`
      - `zalo.webhookSecret`
    - vẫn giữ field `SecretRef` làm fallback.

- Test/docs/ADR:
  - `apps/api/test/settings-policy.service.test.ts`: cập nhật expectation theo policy mới (key trực tiếp được persist và runtime dùng được).
  - tạo ADR mới:
    - `docs/decisions/ADR-026-UI-MANAGED-INTEGRATION-SECRETS-RUNTIME-HOT-RELOAD.md`
  - cập nhật docs deploy/runbook:
    - `docs/deployment/VM_AUTODEPLOY.md`
    - `docs/operations/RUNBOOK.md`
  - cập nhật superseded note:
    - `docs/decisions/ADR-013-SETTINGS-CENTER-ENTERPRISE-DOMAIN-POLICY.md`
    - `docs/decisions/ADR-017-RUNTIME-ENFORCEMENT-SETTINGS-CENTER.md`

## Verification
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅

- API:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/zalo.service.test.ts` ✅

- Web:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅

- E2E (Settings Center):
  - `PLAYWRIGHT_PORT=3110 CI=1 npx playwright test -c apps/web/e2e/playwright.config.ts apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/settings-center-audit-scope.spec.ts --reporter=line` ✅ (2 passed)

## Ghi chú
- Mô hình `secretRef-only` cho integrations đã được thay bằng mô hình hybrid theo ADR-026.
- Các hệ thống đang vận hành với `secretRef/env` vẫn tương thích nhờ fallback.
