# Session Log - 2026-04-02 18:01 +07

## Mục tiêu
Implement trang chấm công đa phương thức theo kế hoạch đã chốt:
- Admin gán phương pháp chấm công theo từng nhân sự.
- Remote check-in/check-out theo phiên, tự checkout + logout khi idle 6 phút.
- Office import công qua file `.xlsx`.
- Exempt hiển thị `Miễn chấm công`.
- Bảng tháng dạng ma trận theo ngày có dropdown chọn tháng.

## Thay đổi chính

### 1) Backend (API + data contract)
- Mở rộng schema/migration:
  - `apps/api/prisma/schema.prisma`
  - `apps/api/prisma/migrations/20260402170500_add_attendance_method_and_monthly_minutes/migration.sql`
  - thêm enum `AttendanceMethod` (`REMOTE_TRACKED`, `OFFICE_EXCEL`, `EXEMPT`)
  - thêm `Employee.attendanceMethod`
  - thêm `Attendance.workedMinutes`, `Attendance.attendanceMethod`
- Mở rộng controller:
  - `apps/api/src/modules/hr/hr.controller.ts`
  - thêm endpoint:
    - `GET /api/v1/hr/attendance/monthly`
    - `POST /api/v1/hr/attendance/office-import`
- Hoàn thiện service attendance:
  - `apps/api/src/modules/hr/hr.service.ts`
  - thêm validate `attendanceMethod` khi create/update employee.
  - check-in/check-out chỉ cho `REMOTE_TRACKED` với lỗi rõ ràng cho `OFFICE_EXCEL`/`EXEMPT`.
  - hỗ trợ nhiều phiên remote trong ngày (check-in lại sau khi đã check-out).
  - check-out cộng dồn phút vào `workedMinutes`.
  - thêm monthly matrix API theo contract `{ year, month, daysInMonth, rows[] }`.
  - thêm office import API theo contract `{ totalRows, importedCount, skippedCount, errors[] }`.

### 2) Frontend (route chuyên biệt attendance)
- Tạo board attendance riêng:
  - `apps/web/components/hr-attendance-board.tsx`
- Tích hợp route section HR:
  - `apps/web/components/hr-section-screen.tsx`
  - nhánh `attendance` render `HrAttendanceBoard` (không dùng generic `ModuleWorkbench`).
- Cập nhật module HR employees để có field chọn `attendanceMethod`:
  - `apps/web/lib/module-definitions.ts`
- Cài dependency parse excel:
  - `apps/web/package.json`
  - `package-lock.json`
  - thêm `xlsx` cho workspace web.
- Hành vi UI đã có:
  - Dropdown chọn tháng (YYYY-MM).
  - Bảng ma trận theo ngày, cột tổng tháng.
  - Hiển thị `HH:mm`, `Miễn chấm công`, `--`.
  - Admin inline đổi `attendanceMethod` từng nhân sự.
  - Upload `.xlsx` (frontend parse -> gửi JSON rows API import).
  - Remote self check-in/check-out.
  - Hook idle 6 phút không click: auto check-out (best effort) + `logout()`.
  - Hỗ trợ `AUTH_ENABLED=false`: chọn nhân sự remote để test/vận hành nội bộ.

### 3) Tests
- Backend tests mới:
  - `apps/api/test/hr-attendance.service.test.ts`
  - cover:
    - validate `attendanceMethod` create/update employee
    - chặn check-in/out cho OFFICE/EXEMPT
    - cộng dồn phút nhiều phiên check-out
    - monthly API (WORKED/NO_DATA/EXEMPT)
    - office import + row errors
- Frontend e2e mới:
  - `apps/web/e2e/tests/hr-attendance-board.spec.ts`
  - cover:
    - render ma trận tháng + đổi tháng
    - admin đổi `attendanceMethod`
    - import `.xlsx` + summary
    - auto checkout khi idle (timeout override test env)

### 4) ADR
- Tạo mới:
  - `docs/decisions/ADR-029-HR-ATTENDANCE-MULTI-METHOD-REMOTE-OFFICE-EXEMPT.md`

## Verify
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:deploy --workspace @erp/api` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run prisma:generate --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/hr-attendance.service.test.ts test/hr.service.test.ts` ✅ (`9 passed`)
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `PLAYWRIGHT_PORT=4180 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test --config=apps/web/e2e/playwright.config.ts apps/web/e2e/tests/hr-attendance-board.spec.ts --reporter=line` ✅ (`2 passed`)

## Ghi chú
- Trong quá trình verify web lint có 1 lần fail tạm thời do `.next/types` chưa regenerate (chạy song song lint/build), đã rerun và pass sau khi build hoàn tất.
- Không thay đổi luồng deploy production thủ công trên VM.
