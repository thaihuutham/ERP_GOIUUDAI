# Session Log - 2026-04-06 08:57 +07

## Scope
Follow-up theo phản hồi user: tên người gửi phải là **user ERP cụ thể vừa gửi**, không phải nhãn role (`Admin/Manager/Staff`).

## Changes
- `apps/web/components/zalo-automation-messages-workbench.tsx`
  - bỏ hoàn toàn mapping senderName theo role ở frontend.
  - request gửi personal/OA/fallback không còn truyền `senderName` từ client.
  - giữ layout bubble trái/phải theo `senderType`.
- `apps/api/src/modules/zalo/zalo.service.ts`
  - inject `ClsService` + đọc auth context (`AUTH_USER_CONTEXT_KEY`).
  - thêm `resolveCurrentSenderDisplayName()`:
    - ưu tiên `employee.fullName`,
    - fallback `email`,
    - fallback `userId`.
  - `sendPersonalMessage` và `sendOaMessage` giờ dùng sender từ auth context ERP thay vì nhận từ payload.
- `apps/api/src/modules/conversations/conversations.service.ts`
  - inject `ClsService`.
  - `appendMessage` tự resolve senderName từ auth context khi `senderType=AGENT` và payload không có senderName.
  - thêm helper resolve tương tự theo `employee.fullName -> email -> userId`.
- `apps/api/test/zalo.service.test.ts`
  - cập nhật constructor mock theo dependency mới.
  - thêm test xác nhận OA message dùng `employee.fullName` từ auth context làm senderName.
- `apps/api/test/conversations.realtime.test.ts`
  - cập nhật constructor mock theo dependency `ClsService`.
- `apps/web/e2e/tests/conversations-inbox.spec.ts`
  - cập nhật assertion: frontend không gửi `senderName` trong payload OA.

## Verification
- `npm run lint --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run build --workspace @erp/web` ✅
- `npm run test --workspace @erp/api -- test/zalo.service.test.ts test/conversations.realtime.test.ts` ✅ (`8 passed`)
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Notes
- Sender identity giờ được quyết định ở backend theo auth context user thực thi request, tránh spoof từ client.
