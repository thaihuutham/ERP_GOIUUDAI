# Session Log - 2026-03-30 16:28 +07

## Scope
- Triển khai Level 2 Hybrid Search bằng Meilisearch cho 3 module lõi: CRM customers, Sales orders, Catalog products.
- Giữ nguyên API contract hiện có, thêm fallback SQL + cơ chế reindex/status vận hành.

## Implemented
1. **ADR + thiết kế**
   - Thêm `docs/decisions/ADR-012-LEVEL2-MEILI-HYBRID-SEARCH.md`.

2. **Backend SearchModule**
   - Thêm module mới: `apps/api/src/modules/search/`
     - `search.module.ts`
     - `search.service.ts`
     - `search.types.ts`
   - Hỗ trợ:
     - Hybrid read search (Meili ranked ids + Postgres source of truth)
     - Best-effort write sync (không fail transaction chính)
     - Reindex theo entity (`customers|orders|products|all`)
     - Status runtime/index stats.

3. **Tích hợp vào module nghiệp vụ**
   - CRM:
     - Hybrid list cho `listCustomers`.
     - Sync index sau create/update/merge và các điểm cập nhật customer liên quan.
   - Sales:
     - Hybrid list cho `listOrders`.
     - Sync index sau create/update/approval.
   - Catalog:
     - Hybrid list cho `listProducts`.
     - Sync index sau create/update/archive/setPricePolicy.

4. **Settings API cho vận hành**
   - Bổ sung endpoint:
     - `GET /api/v1/settings/search/status`
     - `POST /api/v1/settings/search/reindex`
   - Lưu metadata reindex gần nhất vào `search_reindex_last_result`.

5. **CLI Reindex**
   - Thêm script: `apps/api/scripts/search-reindex.ts`
   - Thêm npm script:
     - `npm run search:reindex --workspace @erp/api -- --entity=all`

6. **Deploy / Env**
   - `docker-compose.yml`:
     - Thêm service `meilisearch` + volume `meili_data`.
     - Thêm biến `SEARCH_ENGINE`, `MEILI_*` vào service API.
   - `.github/workflows/deploy-vm.yml`:
     - Inject thêm `SEARCH_ENGINE`, `MEILI_*`.
   - `scripts/deploy/deploy-from-runner.sh`:
     - Ghi và validate thêm `SEARCH_ENGINE`, `MEILI_*` vào `.deploy.env`.
   - `config/.env.example`, `docs/deployment/VM_AUTODEPLOY.md`:
     - Cập nhật hướng dẫn cấu hình search.

7. **Tests**
   - Thêm unit/integration tests mới:
     - `apps/api/test/search.service.test.ts`
     - `apps/api/test/search-hybrid.api-flow.test.ts`
   - Cập nhật test hiện có để tương thích constructor mới:
     - `apps/api/test/catalog.service.test.ts`

## Verification
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/search.service.test.ts test/search-hybrid.api-flow.test.ts test/catalog.service.test.ts` ✅

## Notes
- Full test suite `npm run test --workspace @erp/api` hiện còn fail ở nhiều API-flow cũ do bối cảnh auth/tenant hiện tại (`401`), không phát sinh từ thay đổi search module lần này.
