# Session Log - 2026-03-30 14:47 +07

## Scope
- Audit lại các lỗi cấu hình/bảo mật user nêu (AUTH, tenant spoofing, webhook, DB credential, tenant override).
- Xác minh bằng kiểm thử runtime thực tế.
- Vá ngay các điểm còn rủi ro.

## What was verified
1. `JwtAuthGuard` default `AUTH_ENABLED=true`, không còn mặc định public.
2. `tenant-context.util` ưu tiên tenant từ JWT thay vì header.
3. `zalo.service` webhook fail-closed khi thiếu `ZALO_OA_WEBHOOK_SECRET`.
4. `tenant-prisma.extension` đã khóa `tenant_Id` cho where unique/update/delete/upsert.
5. `docker-compose.yml` không còn default DB creds `erp:erp`.

## New hardening applied in this session
1. `apps/api/src/common/tenant/tenant-context.util.ts`
   - Chỉ chấp nhận tenant từ JWT khi JWT verify chữ ký thành công (HS256 + `JWT_SECRET`).
   - Giữ logic cảnh báo spoof khi header tenant khác JWT tenant.
2. `docker-compose.yml`
   - `JWT_SECRET` chuyển sang dạng required: `${JWT_SECRET:?JWT_SECRET is required}`.
3. `.env`
   - Bổ sung rõ `AUTH_ENABLED=true` để tránh mơ hồ cấu hình.
4. `docs/specs/PROJECT_OVERVIEW.md`
   - Cập nhật thứ tự tenant resolution đúng với code: JWT verified -> header (public) -> fallback.

## Runtime verification evidence
- `GET /api/v1/reports/overview` không token => `401`.
- JWT tenant `GOIUUDAI` + header spoof `tenant_demo_company` => trả dữ liệu `GOIUUDAI`.
- JWT tenant `tenant_demo_company` + header spoof `GOIUUDAI` => trả dữ liệu tenant demo (0 theo seed).
- `POST /api/v1/zalo/oa/webhook/messages` thiếu secret/signature => `401`.
- Webhook public + fake Bearer JWT (secret sai) không làm đổi tenant context (tenant meta vẫn fallback `GOIUUDAI`).
- `npm run lint --workspace @erp/api` pass.
- `docker compose config` pass.

## Notes
- API local cần DB credential khớp volume hiện tại. Trong session này API được chạy bằng override tạm:
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail`
- Web dev server vẫn chạy tại `http://localhost:3000` để user UAT.
