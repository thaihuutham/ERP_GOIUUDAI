# Session Log - 2026-04-07 10:46 +07

## Scope
- Triển khai kế hoạch CRM module mới theo hướng contract-centric cho Customer360:
  - lưu lịch sử mua hàng đa sản phẩm theo kỳ hợp đồng;
  - quản lý renewal/reminder cho viễn thông + bảo hiểm ô tô + bảo hiểm xe máy + dịch vụ số;
  - đồng bộ external orders + OCR document review/approve;
  - hợp nhất định danh khách hàng đa kênh (phone-first + social identities).

## Changes
- Schema + migration:
  - cập nhật `apps/api/prisma/schema.prisma` với enums/models CRM mới.
  - tạo migration:
    - `apps/api/prisma/migrations/20260407102631_add_crm_contracts_and_renewal_v1/migration.sql`.
- CRM backend:
  - thêm service:
    - `apps/api/src/modules/crm/crm-contracts.service.ts`
    - `apps/api/src/modules/crm/crm-renewal.util.ts`
    - `apps/api/src/modules/crm/crm-renewal-reminder-scheduler.service.ts`
  - mở rộng controller:
    - `apps/api/src/modules/crm/crm.controller.ts` (contracts, vehicles, social identities, OCR/sync APIs, renewal worklist).
  - wiring module:
    - `apps/api/src/modules/crm/crm.module.ts` (providers/exports/imports + scheduler).
- Settings runtime/policy:
  - `apps/api/src/modules/settings/settings-policy.types.ts`
  - `apps/api/src/modules/settings/settings-policy.service.ts`
  - `apps/api/src/common/settings/runtime-settings.service.ts`
  - `apps/api/src/modules/settings/settings-layout.metadata.ts`
  - bổ sung `sales_crm_policies.renewalReminder` (global + per-product lead days).
- Custom fields:
  - `apps/api/src/modules/custom-fields/custom-fields.service.ts`
  - thêm alias + delegate cho `SERVICE_CONTRACT`, `VEHICLE`, `INSURANCE_POLICY`.
- Conversations identity resolve:
  - `apps/api/src/modules/conversations/conversations.service.ts`
  - `apps/api/src/modules/conversations/conversations.module.ts`
  - ingest thread tự resolve customer theo social identity, fallback phone.
- Tests:
  - mới:
    - `apps/api/test/crm-renewal.util.test.ts`
    - `apps/api/test/crm-contracts.service.test.ts`
  - cập nhật:
    - `apps/api/test/custom-fields.service.test.ts`
    - `apps/api/test/settings-policy.service.test.ts`
- ADR:
  - `docs/decisions/ADR-051-CRM-CUSTOMER360-CONTRACT-RENEWAL-CORE.md`

## Verification
- Backend quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
- Targeted tests:
  - `npm run test --workspace @erp/api -- --run test/crm.service.test.ts test/crm-renewal.util.test.ts test/crm-contracts.service.test.ts test/settings-policy.service.test.ts test/custom-fields.service.test.ts test/conversations.realtime.test.ts` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Notes
- `test/crm.api-flow.test.ts` khi chạy cùng AppModule vẫn có unhandled nền từ các scheduler/pool Zalo mock thiếu dependency runtime (`config/prisma undefined`) — issue test integration nền đã biết, không phát sinh từ patch CRM contracts.
