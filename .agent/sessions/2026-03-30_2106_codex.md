# Session Log - 2026-03-30 21:06 (Codex)

## Scope
Hotfix lỗi runtime `Request failed (404)` khi người dùng chỉnh Settings Center và cải thiện thông báo lỗi UI.

## Investigation
- Xác nhận từ runtime API:
  - `GET /api/v1/settings/config` trả `200`.
  - `GET /api/v1/settings/center` và `PUT /api/v1/settings/domains/org_profile` trả `404`.
- Kết luận: process API đang chạy là bản cũ chưa có endpoints Settings Center mới.

## Changes Completed
- Runtime recovery:
  - restart API local bằng source mới: `npm --prefix apps/api run dev`.
  - xác nhận route map có đủ:
    - `/api/v1/settings/center`
    - `/api/v1/settings/domains/:domain`
    - `/api/v1/settings/domains/:domain/validate`
    - `/api/v1/settings/domains/:domain/test-connection`
    - snapshot/audit endpoints.

- UX/error handling hardening (web):
  - `apps/web/lib/api-client.ts`:
    - parse nested error message từ payload (`error.message`, `error.details.message`) thay vì chỉ đọc top-level `message`.
  - `apps/web/components/settings-center.tsx`:
    - thêm mapping cho 404 Settings endpoints với thông báo người dùng rõ ràng:
      - API backend chưa cập nhật Settings Center, cần restart/cập nhật API.
    - áp dụng mapping cho các action load/validate/save/test/snapshot/restore.

## Verification
- `npm --prefix apps/web run lint` ✅
- `GET /api/v1/settings/center` (`x-tenant-id: GOIUUDAI`) ✅ `200`
- `PUT /api/v1/settings/domains/org_profile` ✅ `200`

## Runtime
- API dev server đang chạy tại `http://localhost:3001` (session PTY).
- Web server vẫn đang chạy tại `http://localhost:3000` để UAT.
