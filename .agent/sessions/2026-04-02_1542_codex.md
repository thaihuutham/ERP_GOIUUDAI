# Session Log - 2026-04-02 15:42 +07

## Mục tiêu
Sửa branding fallback còn sót `Digital Retail ERP Co.` khiến UI hiển thị chữ cái `D` và tên công ty cũ.

## Thay đổi chính
- Đã thay toàn bộ fallback `companyName` ở backend sang `GOIUUDAI`:
  - `apps/api/src/modules/settings/settings-policy.types.ts`
  - `apps/api/src/modules/settings/settings.service.ts`
  - `apps/api/src/modules/settings/settings-policy.service.ts`
  - `apps/api/src/common/settings/runtime-settings.service.ts`

## Verify
- `rg -n "Digital Retail ERP Co\.|Digital Retail ERP" apps/api apps/web` ✅ (không còn kết quả)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅

## Ghi chú
- Nếu tenant đã lưu `organization.companyName` cũ trong DB thì UI vẫn có thể hiện tên cũ cho đến khi cập nhật tại Settings (Hồ sơ tổ chức) hoặc reset cấu hình tương ứng.
