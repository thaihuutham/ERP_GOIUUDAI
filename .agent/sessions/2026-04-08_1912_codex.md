# Session Log — 2026-04-08 19:12 +07

## User request
- Chuẩn hóa CRM taxonomy runtime: dữ liệu `giai đoạn` phải lấy từ Settings Center Enterprise.
- Audit toàn project để tìm loại dữ liệu đã có cấu hình trong Settings Center nhưng còn bị hardcode, và sửa đồng bộ.
- Ràng buộc: không tự ý migrate dữ liệu nghiệp vụ cũ sang taxonomy mới khi taxonomy cũ không còn trong Settings.

## Work completed
- Backend CRM taxonomy/runtime
  - `apps/api/src/modules/crm/crm.service.ts`
    - bỏ gắn cứng tag `da_mua` khi `markPaymentRequestPaid`; chỉ gắn khi tag này thực sự tồn tại trong `salesPolicy.tagRegistry.customerTags`.
    - giữ behavior không tự migrate stage cũ của bản ghi hiện hữu; với bản ghi mới có trạng thái `DONG_Y_CHUYEN_THANH_KH` thì stage vẫn được suy ra từ taxonomy hiện hành (finalized stage) thay vì hardcode.
- Conversations typing fix
  - `apps/api/src/modules/conversations/conversations.service.ts`
    - sửa type helper `resolveCustomerSourceByChannel` nhận `string | null` để pass TypeScript lint.
- Frontend cleanup các hardcode taxonomy/tag
  - `apps/web/components/crm-customers-board.tsx`
    - bỏ placeholder hardcode source/tag (`ONLINE/REFERRAL`, `vip/da_mua`), thay bằng hướng dẫn theo Settings Center.
  - `apps/web/lib/crm-customer-import.ts`
    - template import Excel lấy sample `tags` từ `customerTags` runtime thay vì hardcode `vip/khach_moi/da_mua`.
  - `apps/web/components/crm-customers-import-board.tsx`
    - tải thêm `tagRegistry.customerTags` từ `/crm/taxonomy` và truyền vào builder template.
  - `apps/web/lib/action-presets.ts`
    - bỏ preset `resultTag` hardcode (`quan_tam`, `can_goi_lai`) trong action tạo interaction.
  - `apps/web/lib/module-definitions.ts`
    - bỏ placeholder tag hardcode và bỏ options `resultTag` hardcode, thay bằng nhập theo Settings Center.
  - `apps/web/components/zalo-automation-campaigns-workbench.tsx`
    - thay example/placeholder tag hardcode (`vip,lead-moi`) thành trung tính.
  - `apps/web/components/zalo-automation-messages-workbench.tsx`
    - thay placeholder tag hardcode thành trung tính.

## Verification
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run lint --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run test --workspace @erp/api -- test/crm.service.test.ts test/conversations.api-flow.test.ts` ✅ (`11 passed`)
- `npm run build --workspace @erp/api` ✅
- `npm run build --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`3 passed`)

## Notes
- Không tạo migration/schema mới trong batch này.
- Không phát sinh ADR mới (batch là hardcode-runtime consistency fix theo kiến trúc Settings Center hiện có).
