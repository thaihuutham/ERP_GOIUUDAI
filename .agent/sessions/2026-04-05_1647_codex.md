# Session Log - 2026-04-05 16:47 +07

## Scope
Tiếp tục IAM rollout theo kế hoạch và hoàn thành Task 8: thêm API quản trị `user scope override` và `title scope mapping` trong Settings Enterprise.

## Files Changed
- `apps/api/src/modules/settings/settings-enterprise.service.ts`
- `apps/api/src/modules/settings/settings.controller.ts`
- `apps/api/test/settings-enterprise.service.test.ts`
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `.agent/sessions/2026-04-05_1647_codex.md`

## Key Outcomes
1. Thêm service method `updateUserScopeOverride(userId, payload)`:
   - validate user + `scopeMode` + optional `rootOrgUnitId` + date range,
   - upsert dữ liệu vào `iam_user_scope_override`,
   - trả về `override` và `effectiveScope`.
2. Thêm service method `updateTitleScopeMapping(payload)`:
   - validate `titlePattern` + `scopeMode`,
   - upsert hoặc remove mapping title trong `settings.access_security.v1` (`iamTitleScopeMappings`),
   - normalize/dedupe/sort theo `priority`.
3. Bổ sung endpoint controller:
   - `PUT /settings/iam/users/:userId/scope-override`
   - `PUT /settings/iam/title-scope-mapping`
   - cả hai yêu cầu `ADMIN` và có `@AuditAction`.
4. Bổ sung test service:
   - update user scope override trả effective scope đúng,
   - update title scope mapping trả `scopeMode` đúng.

## Verification
- `npm run test --workspace @erp/api -- test/settings-enterprise.service.test.ts` ✅ (`6 passed`)
- `npm run test --workspace @erp/api -- test/settings-enterprise.service.test.ts test/permission.guard.test.ts test/iam-access.service.test.ts test/iam-scope.service.test.ts test/workflows.service.test.ts` ✅ (`23 passed`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Blockers / External
- Không có blocker kỹ thuật trong phạm vi Task 8.

## Notes
- Chưa thay đổi logic `IamScopeService.resolveModeFromTitle` (vẫn heuristic hiện tại); Task 8 tập trung thêm admin APIs và persistence cho override/mapping.
