# Session Log - 2026-04-06 16:55 +07

## Scope
- Fix UI campaign detail account table bị dồn cột.
- Giảm lỗi `NO_TARGET_THREAD` bằng cách bổ sung fallback tra phone -> UID trước khi đánh dấu recipient skip.

## Changes
- UI table layout:
  - `apps/web/components/zalo-automation-campaigns-workbench.tsx`
    - add class `data-table` cho các table campaign.
    - account table dùng `data-table zalo-campaign-account-table`.
  - `apps/web/app/styles/modules/crm.css`
    - thêm `zalo-campaign-account-table` styles:
      - `min-width` lớn hơn;
      - cột account/status/last-sent có width tối thiểu;
      - cột số giữ `nowrap` để không dồn chữ.

- Phone -> UID fallback cho snapshot:
  - `apps/api/src/modules/zalo/zalo.service.ts`
    - thêm method `resolvePersonalThreadsByPhones(accountIds, phones)`:
      - lấy contacts từ `getAllFriends()` trên account PERSONAL đã connect;
      - chuẩn hóa phone và map sang UID/thread id.
    - thêm helper `resolveContactUid(...)`.
  - `apps/api/src/modules/zalo/zalo-campaign.service.ts`
    - khi snapshot customer:
      - ưu tiên thread interaction cũ;
      - nếu không có thread cũ thì fallback tra phone -> UID;
      - nếu tra được UID, recipient giữ `PENDING` + có `externalThreadId`.
    - thêm metadata runtime:
      - `reachableAccountIds`
      - `resolvedFromPhoneLookup`
    - compatibility check:
      - recipient từ phone-lookup chỉ cho phép account nằm trong `reachableAccountIds` claim gửi.

## Verification
- Backend:
  - `npm run -w apps/api lint` ✅
  - `npm run -w apps/api build` ✅
  - `npm run -w apps/api test -- --run test/zalo-campaign.service.test.ts test/zalo.controller.test.ts test/zalo.service.test.ts` ✅ (`18 passed`)

- Frontend:
  - `npm run -w apps/web lint` ✅
  - `npm run -w apps/web build` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)

## Stability Gate
- `docker ps` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
