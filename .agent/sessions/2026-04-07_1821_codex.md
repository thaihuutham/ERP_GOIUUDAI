# Session Log - 2026-04-07 18:21 +07

## Scope
- User yêu cầu implement plan:
  - tách trạng thái CSKH khỏi `GenericStatus`,
  - thêm `zaloNickType`,
  - đổi rule lọc/sending campaign Zalo,
  - đổi delete customer thành soft-skip,
  - thêm import Excel khách hàng (upsert),
  - cập nhật test + docs + ADR.

## Changes
- Prisma + migration:
  - `apps/api/prisma/schema.prisma`
    - `Customer.status` dùng `CustomerCareStatus` (9 trạng thái).
    - thêm `Customer.zaloNickType` dùng `CustomerZaloNickType` (4 trạng thái, default `CHUA_KIEM_TRA`).
  - `apps/api/prisma/migrations/20260407202000_customer_care_status_and_zalo_nick_type/migration.sql`
    - migrate toàn bộ status cũ -> `MOI_CHUA_TU_VAN`;
    - thêm/backfill `zaloNickType`.
- CRM backend:
  - `apps/api/src/modules/crm/crm.controller.ts`
    - `DELETE /crm/customers/:id` chuyển semantics soft-skip.
    - thêm `POST /crm/customers/import`.
  - `apps/api/src/modules/crm/crm.service.ts`
    - parse/validate enum trạng thái CSKH + zaloNickType.
    - `softSkipCustomer` set `SAI_SO_KHONG_TON_TAI_BO_QUA_XOA` (không xóa cứng).
    - auto set `customerStage=DA_MUA` khi `status=DONG_Y_CHUYEN_THANH_KH`.
    - import customers (admin only), upsert theo phoneNormalized fallback emailNormalized.
- CRM frontend:
  - `apps/web/components/crm-customers-board.tsx`
    - thay dropdown/status list sang 9 trạng thái CSKH.
    - thêm cột + dropdown `Loại nick Zalo`.
    - bulk actions đổi theo trạng thái CSKH.
    - delete UI đổi thành `BỎ QUA/Xóa` (soft-skip).
    - thêm import Excel (`ExcelImportBlock`) + parse `.xlsx` + summary.
- Zalo campaign:
  - `apps/api/src/modules/zalo/zalo-campaign.service.ts`
    - bỏ filter cứng `customer.status=ACTIVE`.
    - hỗ trợ `recipientFilterJson.zaloNickTypes` + default filter (`CHUA_KIEM_TRA`, `GUI_DUOC_TIN_NHAN`).
    - rule dispatch theo `zaloNickType` (`CHAN_NGUOI_LA`, `CHUA_CO_NICK_ZALO`, ...).
    - auto update `customer.zaloNickType` theo kết quả gửi/lỗi.
  - `apps/web/components/zalo-automation-campaigns-workbench.tsx`
    - thêm multi-select `Loại nick Zalo (tuỳ chọn)` vào form create campaign.
- Compatibility fixes do enum change:
  - `apps/api/src/modules/crm/crm-contracts.service.ts`
  - `apps/api/src/modules/zalo/zalo.service.ts`
  - `apps/api/src/modules/settings/settings.service.ts`
  - fix các điểm còn gán `Customer.status = ACTIVE`.
- Tests:
  - `apps/api/test/zalo-campaign.service.test.ts`
    - thêm test default/explicit `zaloNickTypes` filter.
    - thêm test rule `CHAN_NGUOI_LA`.
    - thêm test auto update `zaloNickType` khi gửi thành công/lỗi.
  - cập nhật test CRM/Search:
    - `apps/api/test/crm.service.test.ts`
    - `apps/api/test/crm.api-flow.test.ts`
    - `apps/api/test/search.service.test.ts`
    - `apps/api/test/search-hybrid.api-flow.test.ts`
  - cập nhật e2e CRM bulk status:
    - `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`
- Documentation + decision:
  - cập nhật `docs/specs/CUSTOMER360_DATA_DICTIONARY.md`.
  - cập nhật `docs/specs/ZALO_CAMPAIGNS_V1.md`.
  - thêm `docs/decisions/ADR-053-CUSTOMER-CARE-STATUS-AND-ZALO-NICKTYPE-RULES.md`.

## Tests & Verification
- Backend quality:
  - `npm run prisma:generate --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
- Backend tests (targeted):
  - `npm run test --workspace @erp/api -- --run test/zalo-campaign.service.test.ts test/crm.service.test.ts test/search.service.test.ts test/crm-contracts.service.test.ts` ✅
  - Note: chạy chung với `crm.api-flow` và `search-hybrid.api-flow` phát sinh lỗi nền scheduler/pool unhandled (suite đã `skip`), không phải regression của patch.
- Frontend quality:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
- E2E:
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date`)

## Notes
- Migrate status ban đầu báo pending migration `20260407202000_customer_care_status_and_zalo_nick_type`; đã apply bằng `prisma migrate deploy` và xác nhận lại.
- Không thay đổi module khác vẫn dùng `GenericStatus` ngoài phạm vi Customer.
