# Session Log - 2026-04-02 18:56 +07

## Muc tieu
Implement V2 cham cong theo ngay cho 1 nhan su co nhieu phuong thuc trong cung thang (Remote + Office + Exempt), bo gate theo `Employee.attendanceMethod`, them API exempt-day, va cap nhat board + test theo acceptance moi.

## Thay doi chinh

### 1) Backend attendance V2 (theo ngay)
- Cap nhat `apps/api/src/modules/hr/hr.service.ts`:
  - `getAttendanceMonthly` tong hop theo `Attendance.attendanceMethod` tren tung record/ngay.
  - Rule ngay:
    - co record `EXEMPT` => `status=EXEMPT`, `workedMinutes=0`.
    - khong EXEMPT, co phut cong > 0 => `WORKED`.
    - con lai => `NO_DATA`.
  - `monthTotalMinutes` chi cong ngay `WORKED`.
- Bo gate cung theo `Employee.attendanceMethod` trong:
  - `checkIn/checkOut`.
  - `importOfficeAttendance`.
- Them conflict guard theo ngay:
  - Ngay da `EXEMPT` thi chan `check-in` va chan `office-import`.
  - Khi danh dau EXEMPT thi chan neu ngay da co worked data/open session.
- Them methods:
  - `markAttendanceExemptDay(payload)`
  - `unmarkAttendanceExemptDay(employeeId, workDate)`
  - helper `assertAttendanceDayNotExempt(...)`.

### 2) API moi exempt day
- Cap nhat `apps/api/src/modules/hr/hr.controller.ts`:
  - `POST /api/v1/hr/attendance/exempt-day`
  - `DELETE /api/v1/hr/attendance/exempt-day?employeeId=...&workDate=...`
- Contract `GET /hr/attendance/monthly` giu nguyen shape.

### 3) Frontend attendance board
- Cap nhat `apps/web/components/hr-attendance-board.tsx`:
  - Giu filter `Thang/Nam` va rule cell rong cho `NO_DATA` + `EXEMPT`.
  - Them thao tac admin exempt theo ngay ngay tren cell:
    - icon-only button (khong text) de giu cell trong.
    - `POST/DELETE /hr/attendance/exempt-day`.
  - Them metadata row:
    - `WORKED: x | EXEMPT: y`.
  - Bo rang buoc UI check-in theo employee method; backend xu ly conflict theo ngay.

### 4) Test cap nhat
- `apps/api/test/hr-attendance.service.test.ts`
  - update theo V2 (9 tests):
    - mixed monthly by day
    - import mixed methods
    - check-in/import blocked on exempt day
    - mark/unmark exempt day
    - block mark exempt when day already has worked/open data
- `apps/api/test/hr.api-flow.test.ts`
  - them integration test map route `POST/DELETE attendance/exempt-day`.
- `apps/web/e2e/tests/hr-attendance-board.spec.ts`
  - assert admin mark 2 ngay EXEMPT tren cung nhan su
  - assert metadata dem `WORKED/EXEMPT`
  - assert cell `NO_DATA/EXEMPT` van rong
  - assert import/check-in flow van hoat dong.

## Verify
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/hr-attendance.service.test.ts` ✅ (`9 passed`)
- `npm run test --workspace @erp/api -- test/hr.api-flow.test.ts` ✅ (`3 passed`)
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `PLAYWRIGHT_PORT=4180 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test --config=apps/web/e2e/playwright.config.ts apps/web/e2e/tests/hr-attendance-board.spec.ts --reporter=line` ✅ (`2 passed`)

## Ghi chu
- Trong qua trinh verify web lint gap stale `.next/types`; da clear `apps/web/.next/types` + `apps/web/tsconfig.tsbuildinfo`, build lai, sau do lint pass.
- Khong thay doi schema/contract monthly.
- Them ADR moi:
  - `docs/decisions/ADR-030-HR-ATTENDANCE-DAILY-METHOD-PRIORITY-EXEMPT-DAY.md`.
