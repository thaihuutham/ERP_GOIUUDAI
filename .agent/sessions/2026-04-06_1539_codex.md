# Session Log - 2026-04-06 15:39 +07

## Scope
- Thực hiện yêu cầu UI cho Zalo Campaigns:
  - chuyển phần `Operator campaign` sang dạng dropdown dễ quan sát;
  - bổ sung icon `i` cho các trường cài đặt để mở popup giải thích chi tiết.

## Changes
- `apps/web/components/zalo-automation-campaigns-workbench.tsx`
  - hoàn thiện create-form operator theo dropdown + nút `Thêm operator` + chip `Bỏ`.
  - thêm bộ nội dung `CAMPAIGN_FIELD_HELP` cho các trường cài đặt chính.
  - thêm `renderFieldLabel(...)` để gắn icon `i` cho các trường.
  - thêm `Modal` hiển thị mô tả chi tiết, bullet hướng dẫn, ví dụ (nếu có).
  - fix accessibility collision:
    - đổi help button `aria-label` thành text generic (`Mở giải thích trường dữ liệu`),
    - thêm `title="Giải thích: <field>"` để vẫn rõ field mục tiêu.

- `apps/web/app/styles/modules/crm.css`
  - thêm style cho:
    - `.zalo-campaign-label-row`
    - `.zalo-campaign-help-btn`
    - `.zalo-campaign-empty-operator`
    - `.zalo-campaign-help-modal`

- `apps/web/e2e/tests/zalo-campaigns-flow.spec.ts`
  - thêm kiểm tra mở popup help từ icon `i`.
  - thêm kiểm tra luồng add operator bằng dropdown trên form create.
  - cập nhật selector test để ổn định:
    - click help button qua `getByTitle('Giải thích: Tên campaign')`.
    - nhập form create qua placeholder để tránh phụ thuộc accessible-label của browser.

## Verification
- `npm run -w apps/web lint` ✅
- `npm run -w apps/web build` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)
- `npm run -w apps/api lint` ✅
- `npm run -w apps/api build` ✅

## Stability Gate
- `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres Up`)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
