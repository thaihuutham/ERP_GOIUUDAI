# Session Log: 2026-03-29_1408

## THÔNG TIN SESSION

```
Ngày/giờ bắt đầu : 2026-03-29 13:58
Ngày/giờ kết thúc: 2026-03-29 14:08
Agent            : Codex
Account/Instance : Local desktop session
Task đang làm    : RETAIL-CRM-002 — hoàn thiện Next Up #1 và #2
```

## TÓM TẮT SESSION
Đã hoàn thiện hai hạng mục pending:
1) chuẩn hóa `start` production API không phụ thuộc `ts-node/esm`,
2) rollout env/secrets mới cho luồng deploy VM qua GitHub Actions (AI + Zalo OA).

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Dọn cấu hình API package:
  - bỏ duplicate `tsc-alias` trong `apps/api/package.json`.
- Chuẩn hóa deploy env pipeline:
  - cập nhật `.github/workflows/deploy-vm.yml` truyền env từ GitHub Secrets/Variables.
  - cập nhật `scripts/deploy/deploy-from-runner.sh`:
    - tạo runtime env file `.deploy.env` (chmod 600),
    - chạy `docker compose --env-file` cho `build` + `up -d`,
    - cảnh báo nếu `JWT_SECRET` đang rỗng/placeholder.
  - cập nhật `docker-compose.yml` sang env-substitution với default an toàn.
- Mở rộng env docs/config cho OA outbound:
  - thêm `ZALO_OA_OUTBOUND_URL`, `ZALO_OA_ACCESS_TOKEN`, `ZALO_OA_API_BASE_URL`, `ZALO_OA_OUTBOUND_TIMEOUT_MS` vào `config/.env.example`.
- Cập nhật tài liệu:
  - `README.md` (thêm hướng dẫn `start:prod` + env OA outbound/AI timeout)
  - `docs/deployment/VM_AUTODEPLOY.md`
  - `docs/operations/RUNBOOK.md`
- Tạo ADR deploy:
  - `docs/decisions/ADR-007-DEPLOY-ENV-ROLLOUT-GITHUB-VM.md`.
- Bảo vệ secrets:
  - thêm `.deploy.env` vào `.gitignore`.

### 🔄 Đang dở
- Chưa chạy deploy thật trên GitHub Actions do session local không có quyền set Secrets/Variables của repo.

## VERIFICATION
- `bash -n scripts/deploy/deploy-from-runner.sh` ✅
- `docker compose config` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run start --workspace @erp/api` (smoke boot với env test) ✅

## THAY ĐỔI FILE

```
ADDED:
  docs/decisions/ADR-007-DEPLOY-ENV-ROLLOUT-GITHUB-VM.md
  .agent/sessions/2026-03-29_1408_codex.md

MODIFIED:
  .github/workflows/deploy-vm.yml
  scripts/deploy/deploy-from-runner.sh
  docker-compose.yml
  config/.env.example
  apps/api/package.json
  .gitignore
  README.md
  docs/deployment/VM_AUTODEPLOY.md
  docs/operations/RUNBOOK.md
  planning/CURRENT_TASK.md
  .agent/memory/CONTEXT_SNAPSHOT.md
```

## VẤN ĐỀ PHÁT SINH

| Vấn đề | Đã xử lý chưa | Cách xử lý |
|---|---|---|
| Tiến trình `node dist/main.js` cũ còn chạy nền sau lần verify trước | ✅ | Dừng process trước khi chạy verify mới |
| `apps/api/package.json` bị duplicate `tsc-alias` | ✅ | Xóa dòng duplicate, giữ 1 bản |

## HANDOFF CHO AGENT TIẾP THEO

1. Thiết lập GitHub Secrets/Variables theo checklist mới trong `docs/operations/RUNBOOK.md`.
2. Chạy `deploy-vm` bằng `workflow_dispatch` để xác nhận rollout env production.
3. Kiểm tra hậu deploy cho AI scoring + Zalo OA webhook/outbound bằng credentials thật.
