# Session Log - 2026-04-06 11:53 +07

## Scope
- Tinh chỉnh UI trang `Zalo Automation > Tin nhắn` theo feedback user:
  - bỏ khối header lớn để tăng diện tích;
  - mở rộng chiều cao khu vực danh sách hội thoại + luồng tin nhắn;
  - hiển thị unread rõ ràng bằng badge đỏ theo hội thoại và theo tài khoản;
  - bỏ thread không phải Zalo (`Kênh khác`).

## Findings
- Khối thông tin lớn phía trên (header cũ) chiếm diện tích dọc, làm vùng chat bị thu hẹp.
- List hội thoại có lẫn thread `OTHER` khiến user thấy `Smoke Customer / Kênh khác` dù không phải hội thoại Zalo.
- Unread cũ hiển thị dạng text nhỏ `Unread: x` ở bên phải, khó scan nhanh.

## Changes
- `apps/web/components/zalo-automation-messages-workbench.tsx`
  - bỏ block header lớn, giữ toolbar gọn ở đầu trang.
  - filter thread list chỉ còn `ZALO_PERSONAL` và `ZALO_OA`.
  - thêm unread badge đỏ trước tên hội thoại.
  - tổng hợp unread theo account và hiển thị chip đỏ ở phần tài khoản Zalo.
  - hiển thị unread count trong option `Tất cả tài khoản` và từng tài khoản khi có unread.
- `apps/web/app/styles/modules/crm.css`
  - tăng chiều cao khả dụng của `zalo-chat-layout` (`min-height: calc(100vh - 220px)`).
  - cho thread list/message list dùng flex-fill để kéo xuống hết khu vực panel.
  - thêm style badge đỏ unread cho conversation/account.
- `apps/web/e2e/tests/conversations-inbox.spec.ts`
  - cập nhật test để khớp UI mới (không còn heading header cũ, có unread badges, vẫn kiểm tra gửi tin và ai-runs).

## Verification
- `npm run -w apps/web build` ✅
- `npm run -w apps/web lint` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Stability Gate
- `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
