# Session Log - 2026-03-30 22:05 (Codex)

## Scope
Implement full plan IAM + Org Chart + Permission Engine trong Settings Center Enterprise, gồm backend, web auth flow, test và tài liệu.

## Changes Completed

### Backend
- Bổ sung module/auth endpoints:
  - `POST /api/v1/auth/login`
  - `POST /api/v1/auth/refresh`
  - `POST /api/v1/auth/logout`
  - `POST /api/v1/auth/change-password`
- Mở rộng schema và migration IAM:
  - `User`: `isActive`, `mustChangePassword`, `lastLoginAt`, `passwordChangedAt`, `passwordResetAt`
  - `Employee`: `orgUnitId`
  - `OrgUnit`, `PositionPermissionRule`, `UserPermissionOverride`
  - enums `OrgUnitType`, `PermissionAction`, `PermissionEffect`
- Bổ sung `PermissionGuard` global deny-first, fallback `@Roles`, break-glass theo policy.
- Wire global guard chain trong `AppModule`: `JwtAuthGuard` -> `PermissionGuard`.
- Hoàn thiện Settings Enterprise service/controller cho IAM/org/permission endpoints mới.
- Bổ sung password utility (`bcryptjs`) và password policy validation.
- Mở rộng settings domain `access_security.permissionPolicy`.

### Web
- Thêm auth session runtime:
  - local session storage cho token/user
  - gắn `Authorization` bearer vào `apiRequest`
  - login gate + force change-password gate khi auth mode bật
  - ẩn role switch localStorage trong auth mode
- Settings Center no-JSON bổ sung panel:
  - org tree (`create`, `move`, tree view)
  - IAM provisioning (`create user+employee`, `reset password`)
  - permission matrix theo position và user overrides (5 actions, ALLOW/DENY)

### Tests
- Thêm test mới:
  - `test/auth.service.test.ts`
  - `test/password.util.test.ts`
  - `test/permission.util.test.ts`
  - `test/permission.guard.test.ts`
  - `test/jwt-auth.guard.test.ts`
  - `test/settings-enterprise.service.test.ts`

### Docs / Decisions
- ADR mới:
  - `docs/decisions/ADR-015-SETTINGS-IAM-ORG-PERMISSION-ENGINE.md`
- Cập nhật deploy runbook:
  - thêm `NEXT_PUBLIC_AUTH_ENABLED`, `PERMISSION_ENGINE_ENABLED`
  - thêm staged rollout steps cho IAM/permission

## Verification
- `npm --prefix apps/api run lint` ✅
- `npm --prefix apps/api run build` ✅
- `npm --prefix apps/api run test` ✅ (`24/24` files, `61/61` tests)
- `npm --prefix apps/web run lint` ✅
- `npm --prefix apps/web run build` ✅
- `curl http://localhost:3000` ✅ (`200`)
- `curl http://localhost:3001/api/v1/health` ✅ (`200`)

## Notes
- Runtime local vẫn đang có web/app server sẵn (`3000`, `3001`).
- Mặc định repo vẫn safe rollout:
  - `AUTH_ENABLED=false`
  - `NEXT_PUBLIC_AUTH_ENABLED=false`
  - `PERMISSION_ENGINE_ENABLED=false`
