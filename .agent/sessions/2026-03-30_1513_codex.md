# Session Log - 2026-03-30 15:13 +07

## Scope
- Điều chỉnh sidebar theo yêu cầu UI: tách các trang liên quan Zalo thành nhóm riêng.

## Changes
1. Sidebar grouping trong `apps/web/components/app-shell.tsx`:
   - Chia navigation thành 3 nhóm: trước `Dự án` -> `ZALO Tự động` -> phần còn lại.
   - Thêm group title `ZALO Tự động` ngay dưới `Dự án`.
   - Thêm link `Inbox hội thoại` trỏ `'/modules/crm/conversations'` trong group mới.
   - Tránh active-state chồng chéo: route conversations chỉ active ở group Zalo, không active mục CRM chính.
   - Toolbar title cho route `/modules/crm/conversations` đổi thành `ZALO Tự động`.
2. Style update trong `apps/web/app/globals.css`:
   - Thêm class `side-nav-group` và `side-section-title-accent` để hiển thị nhóm menu rõ ràng.
3. Verification:
   - `npm run lint --workspace @erp/web` pass.
4. Runtime:
   - Restart web app, giữ chạy tại `http://localhost:3000`.

## Notes
- Không thay đổi business logic module.
- Không thay đổi API/DB schema.
