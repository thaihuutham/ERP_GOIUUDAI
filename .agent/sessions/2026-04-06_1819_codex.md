# Session Log - 2026-04-06 18:19 +07

## Scope
- Điều chỉnh UX module Zalo Campaigns theo yêu cầu:
  - đưa block gán operator lên chung vùng cấu hình nội dung campaign;
  - làm gọn card account (quota/ngày cùng dòng tên nick) và mở rộng vùng template nội dung;
  - tách `Chi tiết Campaign` + `Recipients Snapshot` sang trang riêng, mở khi click tên campaign ở danh sách.

## Changes
- `apps/web/components/zalo-automation-campaigns-workbench.tsx`
  - thêm prop `campaignId?: string` để component chạy theo 2 mode:
    - list/create mode (`/campaigns`)
    - detail mode (`/campaigns/[campaignId]`).
  - list page:
    - chỉ hiển thị form tạo + danh sách campaign;
    - tên campaign là link điều hướng sang detail route.
  - detail page:
    - hiển thị `Chi tiết Campaign`, `Recipients Snapshot`, `Lịch sử Attempts`;
    - thêm nút quay lại danh sách.
  - create form:
    - đưa block operator management lên trước phần chọn account/template;
    - layout card account mới:
      - quota/ngày đưa lên cùng dòng tên nick;
      - template textarea tăng diện tích (rows tăng, full width).

- `apps/web/app/modules/zalo-automation/campaigns/[campaignId]/page.tsx` (new)
  - route chi tiết campaign theo id, render workbench ở detail mode.

- `apps/web/app/styles/modules/crm.css`
  - thêm/điều chỉnh style cho:
    - link tên campaign;
    - card account top-row, inline quota field, template textarea lớn hơn;
    - block operator picker gọn hơn theo đúng vị trí mới.

- `apps/web/e2e/tests/zalo-campaigns-flow.spec.ts`
  - cập nhật mock API hỗ trợ `GET /api/v1/zalo/campaigns/:id` cho detail page.
  - cập nhật flow test:
    - sau khi tạo campaign, click tên để vào detail route rồi chạy Start/Pause/Resume/Cancel;
    - delete draft thực hiện từ detail route.

## Verification
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)

## Notes
- Không thay đổi business logic campaign backend.
- Có route detail mới: `/modules/zalo-automation/campaigns/[campaignId]`.
