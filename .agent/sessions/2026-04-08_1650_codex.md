# Session Log - 2026-04-08 16:50 +07

## User request
- Kiểm tra lỗi không load được hội thoại/tin nhắn Zalo (UI báo: `limit must not be greater than 100`).

## Root cause
- Backend dùng `PaginationQueryDto` với ràng buộc `limit <= 100`.
- Frontend có 2 chỗ gọi API messages vượt ngưỡng:
  - `apps/web/components/zalo-automation-messages-workbench.tsx`: `limit: 200`.
  - `apps/web/components/crm-conversations-workbench.tsx`: `limit: 120`.

## Work completed
1. Fix UI query limit
- Đổi `limit` về `100` cho call:
  - `GET /api/v1/conversations/threads/:id/messages` trong:
    - `zalo-automation-messages-workbench.tsx`
    - `crm-conversations-workbench.tsx`

2. Verification
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
  - ghi nhận lint lần đầu fail do `.next/types` stale; sau `next build` thì lint pass.
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)
- Infra gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅

## Notes
- Không thay đổi backend business logic, không có migration mới, không phát sinh ADR mới.

