# Session Log - 2026-03-31 19:04 +07

## Context
- Task: Implement CRM-Sales-Finance core flow plan:
  - Replace placeholder create buttons with real API flows.
  - Add Sales order transitions.
  - Add Finance invoice-from-order flow with ERP rules.
  - Harden deploy migration gate and verify migration status.

## Changes implemented
- Database / Prisma
  - Updated `apps/api/prisma/schema.prisma`:
    - `Order.invoices` relation.
    - `Invoice.orderId` nullable relation to `Order`.
    - `@@unique([tenant_Id, orderId])` + index for `1 order -> 1 invoice`.
  - Added migration:
    - `apps/api/prisma/migrations/20260331190000_add_invoice_order_link_core_flow/migration.sql`

- Deploy
  - Updated `scripts/deploy/deploy-from-runner.sh`:
    - run `prisma migrate deploy` before starting `api/web`.
    - fail fast when migration cannot be applied.
  - Updated `docs/deployment/VM_AUTODEPLOY.md` with migration gate + release checklist.

- API
  - CRM:
    - `GET /crm/taxonomy` (`crm.controller.ts`, `crm.service.ts`)
  - Sales:
    - New DTOs in `apps/api/src/modules/sales/dto/sales.dto.ts`
    - `POST /sales/orders/:id/approve`
    - `POST /sales/orders/:id/reject`
    - Normalize approval outcomes to `APPROVED/REJECTED`
  - Finance:
    - Extend invoice DTO/model usage with `orderId`
    - `POST /finance/invoices/from-order`
    - Enforce:
      - order must be `APPROVED`
      - block duplicate invoice for same order
    - Invoice responses include `orderId/orderNo`

- Web
  - `apps/web/components/crm-customers-board.tsx`
    - real create customer form (`POST /crm/customers`)
    - runtime taxonomy from `/crm/taxonomy`
    - stage/source fallback now validates against runtime lists
    - disabled non-implemented detail actions to avoid no-response UX
  - `apps/web/components/sales-operations-board.tsx`
    - real create order flow (multi line items)
    - approve/reject order actions
    - export invoice from approved order
  - `apps/web/components/finance-operations-board.tsx`
    - real manual create invoice
    - issue/approve invoice in detail panel
    - record payment allocation in detail panel

- Tests
  - API:
    - `apps/api/test/sales.service.test.ts`:
      - approval decision updates order lifecycle consistently
    - `apps/api/test/finance.service.test.ts`:
      - manual invoice creation
      - create invoice from approved order
      - block non-approved order
      - block second invoice for same order
    - `apps/api/test/crm.api-flow.test.ts`:
      - runtime taxonomy endpoint
  - Web E2E:
    - Added `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`
    - Added dedicated config `apps/web/e2e/playwright.crm-sales-finance.config.ts` (port 3110, `reuseExistingServer=false`) to avoid running against external app on 3100.

- ADR
  - Added `docs/decisions/ADR-020-CRM-SALES-FINANCE-CORE-ORDER-INVOICE-LIFECYCLE.md`

## Verification
- API tests:
  - `npm run test --workspace @erp/api -- test/sales.service.test.ts test/finance.service.test.ts test/crm.api-flow.test.ts` ✅
- API quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
- Web quality:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
- Web E2E:
  - `npx playwright test -c apps/web/e2e/playwright.crm-sales-finance.config.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts` ✅
- Migration status:
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅

## Notes
- Local migration apply initially failed due pre-existing relation (`PersonalIncomeTaxProfile`) from prior schema push.
- Resolved by marking migration applied locally:
  - `prisma migrate resolve --applied 20260330224000_add_hr_tax_goal_employee_info`
- Then `migrate deploy` successfully applied remaining pending migrations.
