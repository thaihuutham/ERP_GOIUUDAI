# Session Log — 2026-04-09 08:29 +07

## User request
- User yêu cầu tiếp tục Phase 2 rollout (`ok, tiếp tục phase 2 đi`).

## Work completed
- Hoàn tất wiring Phase 2 cho deploy/runtime:
  - `docker-compose.yml`
    - thêm `IAM_V2_ENABLED` vào env của service `api`.
  - `config/.env.example`
    - thêm `IAM_V2_ENABLED`.
    - thêm toggle deploy smoke:
      - `POST_DEPLOY_AUTH_RBAC_SMOKE_ENABLED`
      - `POST_DEPLOY_AUTH_RBAC_SMOKE_MODULES`
  - `.github/workflows/deploy-vm.yml`
    - truyền thêm env:
      - `IAM_V2_ENABLED`
      - `POST_DEPLOY_AUTH_RBAC_SMOKE_ENABLED`
      - `POST_DEPLOY_AUTH_RBAC_SMOKE_MODULES`
  - `scripts/deploy/deploy-from-runner.sh`
    - đọc/validate/ghi `.deploy.env` cho các biến mới trên.
    - khi `POST_DEPLOY_AUTH_RBAC_SMOKE_ENABLED=true`, tự chạy `scripts/deploy/smoke-auth-rbac-modules.sh` sau healthcheck.
- Cập nhật tài liệu vận hành/deploy:
  - `docs/deployment/VM_AUTODEPLOY.md`
    - cập nhật flow deploy có optional post-deploy smoke.
    - bổ sung env vars mới và ghi chú auto smoke.
  - `docs/operations/RUNBOOK.md`
    - bổ sung env vars mới.
    - bổ sung cách bật auto smoke trong workflow deploy.
  - `docs/plans/2026-04-09-erp-completion-safe-baseline-roadmap.md`
    - ghi nhận continuation cho wiring deploy/runtime Phase 2.

## Verification
- `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `bash -n scripts/deploy/deploy-from-runner.sh scripts/deploy/iam-v2-rollout.sh scripts/deploy/smoke-auth-rbac-modules.sh` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `SMOKE_AUTH_ENABLED=false scripts/deploy/smoke-auth-rbac-modules.sh` ✅ (`PASS auth/rbac smoke.`)

## Notes
- Không thay đổi business logic nghiệp vụ ERP.
- Không thay đổi schema/migration trong batch này.
- Mục tiêu batch: giảm lệch giữa docs và deploy runtime cho rollout Auth + RBAC ở UAT.
