# Session Log - 2026-04-06 09:58 +07

## Scope
Xác minh chính xác tình trạng nhận tin nhắn mới từ `zca-js` và xử lý lỗi listener mất sau restart API.

## Changes
- `apps/api/src/modules/zalo/zalo-personal.pool.service.ts`
  - Thêm listener diagnostics snapshot theo account:
    - `lastRawEventAt`, `lastRawEvent`, `lastIngestedAt`, `lastErrorAt`, `wsState`.
  - Thêm API nội bộ:
    - `getListenerDebug(accountId)`
    - `requestOldMessages(accountId)`
  - Gắn cập nhật snapshot vào event listener:
    - `connected`, `message`, `error`, `closed`.
  - Triển khai `OnModuleInit`:
    - tự động restore personal listener cho account `PERSONAL` đang `CONNECTED` có session hợp lệ ngay khi API boot.
- `apps/api/src/modules/zalo/zalo.service.ts`
  - Thêm methods:
    - `getPersonalListenerDebug(id)`
    - `requestPersonalOldMessages(id)`
- `apps/api/src/modules/zalo/zalo.controller.ts`
  - Thêm endpoint:
    - `GET /api/v1/zalo/accounts/:id/personal/listener-debug`
    - `POST /api/v1/zalo/accounts/:id/personal/request-old-messages`

## Runtime Validation
- Sau restart API, log xác nhận:
  - `Restored personal listener for account cmnmdk9gh0rcqt3dwvak88c0a.`
- Endpoint debug trả:
  - `poolStatus=CONNECTED`, `hasListener=true`, `wsStateLabel=OPEN`.
- Tại thời điểm kiểm tra `2026-04-06 09:57:58 +07`:
  - `lastRawEventAt=null`, `lastIngestedAt=null` (listener chưa nhận event inbound mới trong phiên hiện tại).

## Verification
- `npm run -w apps/api lint` ✅
- `npm run -w apps/api build` ✅

## Notes
- Phát hiện root issue vận hành: restart API làm mất listener in-memory nhưng DB vẫn có thể giữ `CONNECTED`; đã xử lý bằng auto-restore on startup.
