# Session Log - 2026-04-05 15:05 +07

## Scope
Triển khai phase P4 cho tích hợp ZaloCRM vào ERP: hardening vận hành bằng metrics endpoint (active account, reconnect fail, assignment mismatch), bổ sung runtime instrumentation reconnect fail, và cập nhật runbook support.

## Files Changed
- `apps/api/src/modules/zalo/zalo-personal.pool.service.ts`
- `apps/api/src/modules/zalo/zalo-account-assignment.service.ts`
- `apps/api/src/modules/zalo/zalo.service.ts`
- `apps/api/src/modules/zalo/zalo.controller.ts`
- `apps/api/test/zalo.service.test.ts`
- `apps/api/test/zalo-account-assignment.service.test.ts`
- `docs/operations/RUNBOOK.md`
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `.agent/sessions/2026-04-05_1505_codex.md`

## Key Outcomes
1. Added endpoint `GET /api/v1/zalo/operations/metrics` để phục vụ support/ops.
2. Endpoint trả snapshot gồm:
   - `accountMetrics`: tổng account, active account (`CONNECTED`), breakdown theo loại/trạng thái.
   - `reconnectMetrics`: tổng reconnect fail runtime + theo từng account.
   - `assignmentMetrics`: mismatch assignment active theo reason + sample bản ghi.
3. Runtime reconnect instrumentation:
   - ghi nhận fail trong `reconnectWithSavedSession` catch.
   - lưu `count`, `lastFailureAt`, `lastErrorMessage` theo account.
4. Assignment mismatch checker:
   - detect `USER_ID_EMPTY`, `USER_NOT_FOUND`, `USER_INACTIVE`, `DUPLICATE_ACTIVE_ASSIGNMENT`.
   - thêm guard metrics: chỉ `ADMIN`/`MANAGER` hoặc system context.
5. Runbook bổ sung section 13 cho cách gọi endpoint, diễn giải metric, và checklist xử lý sự cố.

## Verification
- `npm run test --workspace @erp/api -- test/zalo-account-assignment.service.test.ts test/zalo.service.test.ts` ✅ (`11 passed`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Blockers / External
- Không có blocker kỹ thuật trong phase P4.

## Notes
- Mismatch metric hiện là runtime/operational health check, không thay đổi business flow ACL hiện tại.
- Reconnect failure counter là runtime in-memory; reset khi API process restart (đã ghi rõ trong runbook qua cụm "runtime counter").
