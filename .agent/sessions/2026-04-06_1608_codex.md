# Session Log - 2026-04-06 16:08 +07

## Scope
- Chuẩn hóa lại semantics `Quota account` cho Zalo campaign:
  - quota theo ngày;
  - reset lúc 24:00 theo timezone campaign.
- Cập nhật wording UI help để người dùng vận hành dễ hiểu.
- Hoàn thiện phần service đang dang dở (helper daily quota, terminal status).

## Changes
- Backend campaign runtime:
  - `apps/api/src/modules/zalo/zalo-campaign.service.ts`
    - thêm helper:
      - `ensureDailyQuotaWindow(...)`
      - `getDateKeyInTimezone(...)`
      - `getNextDailyResetAt(...)`
    - mỗi lần gửi thành công:
      - tăng `dailySentCount`;
      - cập nhật `dailyQuotaDate`;
      - nếu chạm quota ngày thì dời `nextSendAt` sang mốc reset ngày kế.
    - bỏ logic fail campaign theo `sentCount >= quota` tích lũy.
    - cập nhật terminal check để chỉ fail khi thực sự không còn account runnable.
    - bỏ chặn snapshot theo tổng quota account (quota giờ là quota/ngày).

- Prisma:
  - `apps/api/prisma/schema.prisma`
    - `ZaloCampaignAccount.dailySentCount Int @default(0)`
    - `ZaloCampaignAccount.dailyQuotaDate String?`
  - migration mới:
    - `apps/api/prisma/migrations/20260406161000_add_daily_quota_fields_zalo_campaign_accounts/migration.sql`

- Frontend help text:
  - `apps/web/components/zalo-automation-campaigns-workbench.tsx`
    - `Quota account` đổi mô tả thành quota/ngày reset 24:00.
    - label input đổi `Quota/ngày`.
    - `Max recipients` thêm cảnh báo đặt cao làm snapshot nặng hơn.

- Spec update:
  - `docs/specs/ZALO_CAMPAIGNS_V1.md`

## Verification
- Backend:
  - `npm run -w apps/api prisma:generate` ✅
  - `npm run -w apps/api lint` ✅
  - `npm run -w apps/api build` ✅
  - `npm run -w apps/api test -- --run test/zalo-campaign-template.util.test.ts test/zalo-campaign.service.test.ts test/zalo.controller.test.ts` ✅ (`14 passed`)

- Frontend:
  - `npm run -w apps/web build` ✅
  - `npm run -w apps/web lint` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)

## Migration / Stability Gate
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:deploy` ✅
- `docker ps` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
