# Session Log: 2026-04-02_0801

## THONG TIN SESSION

```text
Ngay/gio bat dau : 2026-04-02 07:54
Ngay/gio ket thuc: 2026-04-02 08:01
Agent            : Codex
Task dang lam    : Fix khong hien thi module Tro ly AI tren UI
```

## TOM TAT SESSION

- Xu ly van de user khong thay `Tro ly AI` tren sidebar.
- Tim ra root cause do runtime enabledModules tenant thieu `assistant` + fallback legacy chi add `audit`.
- Bo sung fallback `assistant`, cap nhat test, va hardening script dev de tranh stale shared dist.

## CONG VIEC DA THUC HIEN

### Code changes
- `apps/api/src/common/settings/runtime-settings.service.ts`
  - them fallback module `assistant` trong normalize runtime enabledModules.
- `apps/api/src/modules/settings/settings-policy.service.ts`
  - them fallback module `assistant` cho normalize org_profile enabledModules (legacy rows).
- `apps/api/test/settings-policy.service.test.ts`
  - cap nhat expectation enabledModules co them `assistant`.
- `package.json` (root)
  - them `predev:api` va `predev:web` de build `@erp/shared` truoc khi chay dev.

### Runtime/package sync
- `npm run build --workspace @erp/shared`
  - xac nhan `packages/shared/dist/index.js` co `assistant` trong `ERP_MODULES`.

## VERIFICATION

- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts` ✅
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- verify bang API instance moi (port 3011):
  - `GET /api/v1/settings/runtime` tra `enabledModules` co `assistant` ✅

## HANDOFF

1. API dang chay tren port 3001 cua user la process cu, can restart de nap fix fallback moi.
2. Sau restart, menu `Tro ly AI` se hien lai ngay ca khi org_profile cu chua co `assistant`.
3. Neu van khong thay, check nhanh:
   - `curl -s http://localhost:3001/api/v1/settings/runtime | jq '.enabledModules'`
   - phai co `"assistant"` trong ket qua.
