# Session Log - 2026-03-30 15:32 +07

## Scope
- Điều tra lỗi toàn hệ thống `Request failed (401)` trên các trang web và xử lý dứt điểm theo mode MVP single-tenant.

## Changes
1. Runtime validation và xác minh nguyên nhân:
   - Kiểm tra auth/tenant config và guard hiện tại.
   - Xác nhận API health + batch smoke các endpoint module dùng trên UI.
   - Kết quả: toàn bộ endpoint list trong `module-definitions.ts` trả `200` khi gọi không Bearer token, có `x-tenant-id: GOIUUDAI`.
2. Chuẩn hóa tenant mặc định về duy nhất `GOIUUDAI` ở runtime/config:
   - `docker-compose.yml`: đổi fallback `DEFAULT_TENANT_ID` từ `tenant_demo_company` -> `GOIUUDAI`.
   - `config/.env.example`: đổi `DEFAULT_TENANT_ID` và `JWT_DEFAULT_TENANT_CLAIM` sang `GOIUUDAI`.
   - `apps/api/src/prisma/prisma.service.ts`: đổi fallback tenant mặc định trong `createTenantPrismaExtension` và `getTenantId()` sang `GOIUUDAI`.
   - `apps/api/scripts/issue-token.ts`: default `tenantId` sang `GOIUUDAI`.
   - `docs/api/ERROR_RESPONSE_CONTRACT.md`: cập nhật ví dụ `meta.tenantId` thành `GOIUUDAI`.
3. Kiểm tra dữ liệu demo single-tenant trong DB:
   - Query tất cả bảng có cột `tenant_Id`.
   - Kết quả: bảng có dữ liệu chỉ chứa đúng 1 tenant `GOIUUDAI`; bảng rỗng không có tenant khác.

## Verification
- `curl http://localhost:3001/api/v1/health` -> `200`.
- `curl /api/v1/crm/customers?limit=5` (không token, tenant header `GOIUUDAI`) -> `200`.
- `curl /api/v1/reports/overview` (không token, tenant header `GOIUUDAI`) -> `200`.
- Full smoke list endpoint module (`48 endpoint`) -> `ALL_200`.
- `npm run lint --workspace @erp/api` -> pass.

## Runtime
- Web app vẫn chạy tại `http://localhost:3000`.
- API chạy tại `http://localhost:3001`.
