# Session Log - 2026-04-07 22:55 +07

## Scope
- User yêu cầu thêm chức năng `Bộ lọc` tùy chỉnh cho CRM Customers.
- Chốt hướng:
  - lưu filter **server-side theo user account**;
  - hỗ trợ logic điều kiện **AND/OR**;
  - có thể lưu bộ lọc, đánh dấu mặc định, tự apply cho lần truy cập sau.

## Changes
- API saved filters cho CRM Customers (không đổi schema DB)
  - `apps/api/src/modules/crm/crm.controller.ts`
    - thêm endpoints:
      - `GET /crm/customers/filters`
      - `POST /crm/customers/filters`
      - `DELETE /crm/customers/filters/:id`
  - `apps/api/src/modules/crm/crm.service.ts`
    - thêm model dữ liệu/filter contract cho CRM saved filters.
    - lưu/đọc/xóa filter theo key namespace trên bảng `Setting` hiện có (tenant-safe + actor-safe).
    - validate field/operator/value của filter conditions.
    - quản lý `defaultFilterId` (một default tại một thời điểm).
- StandardDataTable toolbar mở rộng slot trái
  - `apps/web/components/ui/standard-data-table.tsx`
    - thêm prop `toolbarLeftContent`.
    - tách layout toolbar thành trái/phải để đặt nút `Bộ lọc` đúng vị trí UX.
  - `apps/web/app/styles/tables.css`
    - thêm style `standard-table-toolbar-left` và `standard-table-toolbar-right`.
- CRM Customers filter builder UI
  - `apps/web/components/crm-customers-board.tsx`
    - thêm nút `Bộ lọc` ở toolbar trái của bảng.
    - thêm modal cấu hình filter:
      - nhiều điều kiện,
      - chọn logic `AND/OR`,
      - lưu filter,
      - xóa filter,
      - đánh dấu default,
      - apply saved filter hoặc apply tạm thời.
    - load saved filters từ API và auto-apply default filter khi vào trang.
    - field hỗ trợ gồm nhóm cơ bản + relation snapshots (gói cước, loại hợp đồng, loại xe, biển số, ngày hết hạn BH, số GCN, dịch vụ số...).
    - custom filter evaluation chạy trên dữ liệu CRM đã tải vào UI.
- Tests
  - `apps/api/test/crm.service.test.ts`
    - thêm test lưu filter + set default theo actor.
    - thêm test xóa default filter.
  - `apps/api/test/crm.api-flow.test.ts`
    - thêm integration test cho endpoints saved filters.
  - `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`
    - cập nhật mock route/state cho saved filters API.

## Tests & Verification
- System gate
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅
- Quality gate
  - `npm run lint --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run build --workspace @erp/web` ✅
- Test gate
  - `npm run test --workspace @erp/api -- --run test/crm.service.test.ts test/crm.api-flow.test.ts` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅

## Notes
- Không phát sinh migration/schema mới.
- Không thay đổi logic import/bulk CRM đã triển khai trước đó.
