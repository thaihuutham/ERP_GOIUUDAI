# Session Log - 2026-03-31 22:38 +07

## Context
- User yêu cầu triển khai end-to-end phương án tối ưu audit log 7 năm:
  - Hot tier PostgreSQL 12 tháng,
  - Cold tier MinIO trên VM,
  - API/UI tra cứu thống nhất hot+cold,
  - giảm log nhiễu tại nguồn,
  - vận hành ổn định, tránh treo/lag.

## Work completed
- Backend:
  - Bổ sung model archive:
    - `AuditArchiveStatus`, `AuditArchiveManifest` trong Prisma.
    - migration `20260331234500_add_audit_archive_manifest`.
  - Triển khai `AuditArchiveService`:
    - archive theo window ngày (NDJSON.GZ),
    - checksum/hash boundary,
    - idempotent manifest,
    - prune guarded chỉ sau upload/verify thành công.
  - Triển khai `AuditArchiveStorageService`:
    - kết nối MinIO qua S3 API,
    - verify object,
    - auto ensure/create bucket.
  - Mở rộng `AuditService`:
    - query `includeArchived`,
    - merge hot+cold, sort `createdAt desc, id desc`,
    - `pageInfo.tier` + `coldScanStats`,
    - guard cold query bắt buộc `from/to`, giới hạn `AUDIT_COLD_QUERY_MAX_DAYS`.
  - Giảm log nhiễu ở Prisma capture:
    - bỏ ghi `updateMany/deleteMany` khi `count=0`,
    - denylist model kỹ thuật (mặc định `Notification`).
  - Scheduler:
    - thêm `SettingsMaintenanceSchedulerService` để chạy maintenance hằng ngày.
- Frontend:
  - Cập nhật `audit-operations-board`:
    - cursor pagination + load-more,
    - badge nguồn dữ liệu `Hot/Archive`,
    - banner trạng thái query archive + guidance thời gian.
- Deploy/ops:
  - Thêm `minio` vào `docker-compose`.
  - Mở rộng env và inject deploy:
    - `config/.env.example`
    - `.github/workflows/deploy-vm.yml`
    - `scripts/deploy/deploy-from-runner.sh`
  - Cập nhật runbook VM với playbook re-run/verify/restore archive.
- Governance:
  - Thêm ADR:
    - `docs/decisions/ADR-022-AUDIT-HOT-COLD-TIER-MINIO-VM.md`.
  - Cập nhật:
    - `docs/specs/PROJECT_OVERVIEW.md`
    - `docs/architecture/SCALING_DESIGN.md`
    - `docs/deployment/VM_AUTODEPLOY.md`.

## Verification
- Chưa chạy full lint/build/test toàn repo trong session này (repo đang có nhiều thay đổi song song).
- Đã bổ sung test mục tiêu:
  - `apps/api/test/audit.service.test.ts` (mixed hot+cold + cold guards),
  - `apps/api/test/prisma.audit-noise.test.ts` (noise suppression `count=0`).

## Notes
- Repo đang dirty rất lớn với nhiều thay đổi ngoài phạm vi audit; session chỉ chỉnh các file liên quan audit hot/cold, deploy MinIO và governance docs.
