# Session Log - 2026-03-31 21:05 +07

## Context
- User yêu cầu implement full plan Audit Log chuẩn ERP:
  - global audit cho write + sensitive read whitelist,
  - append-only + hash chain,
  - API filter theo object/action/actor/module/request/time,
  - module web riêng `/modules/audit`,
  - retention mặc định 7 năm.

## Work completed
- Hoàn thiện backend audit core:
  - Refactor capture write sang Prisma query extension (tương thích Prisma v6), thay cho `$use`.
  - Giữ đầy đủ before/after snapshot, changedFields, masking và hash chain theo tenant.
  - Sửa typing/compile cho `PrismaService`, `AuditController`.
- Hoàn thiện web audit:
  - Tạo route `apps/web/app/modules/audit/page.tsx`.
  - Cập nhật board audit để gọi endpoint object history khi có `entityType+entityId`.
  - Thêm deep-link object history từ:
    - `apps/web/components/crm-customers-board.tsx`
    - `apps/web/components/sales-operations-board.tsx`
    - `apps/web/components/finance-operations-board.tsx`
- Bổ sung test audit:
  - Unit:
    - `apps/api/test/audit.util.test.ts` (hash deterministic, masking, changedFields).
    - `apps/api/test/audit.service.test.ts` (filter/action taxonomy/prune flow).
  - Integration:
    - `apps/api/test/audit-read.interceptor.api-flow.test.ts` (READ whitelist endpoint tạo audit read).
  - E2E web:
    - `apps/web/e2e/tests/audit-module.spec.ts` (deep-link từ Sales detail sang Audit module + object history load).
- Bổ sung ADR:
  - `docs/decisions/ADR-021-GLOBAL-AUDIT-LOG-HASH-CHAIN-AND-AUDIT-MODULE.md`.

## Verification
- `npm run prisma:generate --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `npm run test --workspace @erp/api -- test/audit.util.test.ts test/audit.service.test.ts test/audit-read.interceptor.api-flow.test.ts` ✅
- `npx playwright test -c apps/web/e2e/playwright.crm-sales-finance.config.ts apps/web/e2e/tests/audit-module.spec.ts` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:deploy --workspace @erp/api` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date`)

## Notes
- E2E mặc định `playwright.config.ts` dùng port `3100` có nguy cơ reuse server ngoài repo; test audit dùng config `playwright.crm-sales-finance.config.ts` (port `3110`, `reuseExistingServer=false`) để đảm bảo chạy đúng app ERP.
