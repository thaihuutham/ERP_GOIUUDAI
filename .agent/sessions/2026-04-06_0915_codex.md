# Session Log - 2026-04-06 09:15 +07

## Scope
Cập nhật trang `Zalo Automation > Tài khoản Zalo` theo yêu cầu: chuyển tạo tài khoản sang modal, bỏ nhập `Zalo UID`, và ràng buộc owner theo user ERP đăng nhập thực tế.

## Changes
- `apps/web/components/zalo-automation-accounts-workbench.tsx`
  - Chuyển form tạo tài khoản từ inline sang modal mở bằng nút `Tạo tài khoản` ở hàng filter.
  - Bỏ trường `Zalo UID` khỏi UI và payload tạo mới.
  - Thêm validate bắt buộc cho `Loại tài khoản`, `Tên hiển thị`, `Số điện thoại`.
  - Trường `Chủ sở hữu` chỉ hiện cho admin (select danh sách `ADMIN|MANAGER`), ẩn cho non-admin.
  - Non-admin submit không gửi `ownerUserId`.
- `apps/web/app/styles/modules/crm.css`
  - Thêm style `zalo-account-filter-toolbar` và `zalo-account-filter-grid` để canh nút tạo tài khoản cùng hàng bộ lọc.
- `apps/api/src/modules/zalo/zalo.service.ts`
  - `createAccount(...)` bắt buộc `displayName` và `phone`.
  - Bỏ nhận `zaloUid` khi tạo (`zaloUid` luôn `null`, chờ đồng bộ từ Zalo runtime).
  - Resolve auth actor từ `AUTH_USER_CONTEXT_KEY`:
    - non-admin: bắt buộc `ownerUserId = actor.userId`.
    - admin: cho phép chỉ định `payload.ownerUserId`, fallback actor nếu không chỉ định.
  - Thêm helper `resolveCurrentActorContext()`.
- `apps/api/test/zalo.service.test.ts`
  - Thêm test đảm bảo non-admin không override owner và `zaloUid` bị bỏ qua.
  - Thêm test admin có thể chỉ định owner khi tạo.
- `apps/web/e2e/tests/zalo-automation-accounts-actions.spec.ts`
  - Mở rộng mock cho POST `/zalo/accounts`.
  - Thêm test admin: modal create + payload không có `zaloUid`, có `ownerUserId` khi chọn.
  - Thêm test non-admin: ẩn field owner + payload không có `ownerUserId`.

## Verification
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/zalo.service.test.ts` ✅ (`8 passed`)
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-automation-accounts-actions.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`3 passed`)
- Stability gate:
  - `docker ps` (có `erp-postgres` Up) ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Notes
- Logic owner giờ nhất quán với yêu cầu nghiệp vụ: lấy theo user ERP đang thực thi request, không theo role label.
- UI bỏ nhập `Zalo UID` để tránh dữ liệu giả; UID sẽ do runtime Zalo đồng bộ sau login/reconnect.
