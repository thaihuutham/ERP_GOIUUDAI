# Session Log: 2026-03-30_1438

## THÔNG TIN SESSION

```
Ngày/giờ bắt đầu : 2026-03-30 14:32
Ngày/giờ kết thúc: 2026-03-30 14:38
Agent            : Codex
Account/Instance : Local desktop session
Task đang làm    : Audit và vá các issue cấu hình/bảo mật theo checklist user cung cấp
```

## TÓM TẮT SESSION
Đã kiểm tra từng issue user nêu (Auth default, tenant spoofing, webhook signature bypass, default DB credential, tenant override). Kết quả: một số lỗi đã được fix sẵn, một số mục còn tồn tại về cấu hình đã được vá ngay trong code/config.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Các bản vá mới
- `apps/api/src/app.module.ts`
  - Chuẩn hóa `ConfigModule.forRoot().envFilePath` để load `.env` đúng khi chạy từ root hoặc `apps/api`.
- `apps/api/src/common/tenant/tenant-context.util.ts`
  - Giữ nguyên JWT priority (source of truth khi có token).
  - Bổ sung đọc header tenant theo danh sách tương thích: header cấu hình + `x-tenant-id` + `tenant-id`.
- `apps/api/src/prisma/tenant-prisma.extension.ts`
  - Chặn override `tenant_Id` ở where unique/update/delete/upsert: luôn ép theo tenant context.
- `config/.env.example`
  - Đổi `AUTH_ENABLED=true`.
  - Bỏ default yếu cho DB URL (`erp:erp`) -> placeholder secure password.
- `docker-compose.yml`
  - Bỏ fallback `DATABASE_URL` dùng mật khẩu yếu `erp`.

### ✅ Đã xác minh sau vá
- `npm run lint --workspace @erp/api` pass.
- `npm run lint --workspace @erp/web` pass.
- `/api/v1/reports/overview` không token -> `401`.
- JWT tenant = `GOIUUDAI` + header tenant khác -> vẫn trả dữ liệu `GOIUUDAI`.
- JWT tenant khác + header `GOIUUDAI` -> vẫn theo tenant trong JWT.
- `/api/v1/zalo/oa/webhook/messages` thiếu secret/signature -> `401` fail-closed.

## THAY ĐỔI FILE

```
ADDED:
  .agent/sessions/2026-03-30_1438_codex.md

MODIFIED:
  apps/api/src/app.module.ts
  apps/api/src/common/tenant/tenant-context.util.ts
  apps/api/src/prisma/tenant-prisma.extension.ts
  config/.env.example
  docker-compose.yml
  planning/CURRENT_TASK.md
  .agent/memory/CONTEXT_SNAPSHOT.md
```

## HANDOFF CHO AGENT TIẾP THEO
1. Nếu muốn vừa secure vừa demo nhanh trên UI không login, cân nhắc bổ sung cơ chế dev token riêng thay vì tắt auth global.
2. Tách riêng profile môi trường `dev-public` và `dev-secure` để tránh nhầm AUTH mode khi test.
3. Duy trì kiểm tra tenant isolation bằng test integration (JWT tenant != header tenant) trong pipeline CI.
