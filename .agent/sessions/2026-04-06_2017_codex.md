# Session Log - 2026-04-06 20:17 +07

## Scope
- Hoàn tất yêu cầu chuẩn hóa Customer360:
  - tạo data dictionary chuẩn cho `Customer360`;
  - bỏ route alias trùng nghĩa giữa `/crm/customers` và `/crm/customer-360`.

## Changes
- `apps/api/src/modules/crm/crm.controller.ts`
  - loại toàn bộ endpoints alias `customer-360` (GET/POST/PATCH/DELETE).
  - giữ endpoint canonical `customers`.

- `apps/web/lib/module-definitions.ts`
  - đồng bộ module `customer-360` sang endpoint `/crm/customers` cho:
    - list
    - create
    - update
    - archive

- `docs/specs/CUSTOMER360_DATA_DICTIONARY.md`
  - bổ sung data dictionary chuẩn Customer360:
    - field
    - kiểu dữ liệu
    - bắt buộc/không
    - nguồn cập nhật
    - màn hình sử dụng

- `docs/decisions/ADR-050-CRM-CUSTOMER360-ENDPOINT-CANONICALIZATION.md`
  - ghi nhận quyết định chuẩn hóa API route.

- Handoff files cập nhật:
  - `.agent/memory/CONTEXT_SNAPSHOT.md`
  - `planning/CURRENT_TASK.md`

## Verification
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/permission.util.test.ts test/crm.service.test.ts` ✅

## Notes
- Không thay đổi business logic customer service, chỉ chuẩn hóa surface API route.
- Không chạm schema production, không thay đổi Firestore rules, không thay đổi deploy flow VM.
