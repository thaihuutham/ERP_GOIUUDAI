# Session Log: 2026-03-30_1423

## THÔNG TIN SESSION

```
Ngày/giờ bắt đầu : 2026-03-30 13:55
Ngày/giờ kết thúc: 2026-03-30 14:23
Agent            : Codex
Account/Instance : Local desktop session
Task đang làm    : Sửa layout menu trái + seed dữ liệu demo liên kết theo yêu cầu UAT
```

## TÓM TẮT SESSION
Đã xử lý feedback UI: bỏ cụm nút `Search`/`Settings` lớn trong sidebar, chuyển tìm kiếm sang toolbar bên phải khu vực nội dung chính. Đồng thời seed lại dữ liệu demo số lượng lớn (100+ bản ghi/module chính) với các quan hệ liên kết chéo giữa CRM/Sales/Finance/HR/SCM/Projects/Workflows.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Cập nhật `apps/web/components/app-shell.tsx`:
  - Xóa quick actions dư thừa trong sidebar.
  - Thêm global search form ở `toolbar-right`.
  - Điều chỉnh xử lý query tìm kiếm không dùng `useSearchParams` để tránh lỗi prerender.
- Cập nhật `apps/web/components/crm-customers-board.tsx`:
  - Đồng bộ tìm kiếm theo query URL qua `window.location.search` (không dùng `useSearchParams`).
- Cập nhật style `apps/web/app/globals.css` để hỗ trợ `global-search-form` trong toolbar.
- Verify web:
  - `npm run build --workspace @erp/web` ✅
  - `npm run lint --workspace @erp/web` ✅
- Seed dữ liệu demo liên kết:
  - `npm run seed:demo --workspace @erp/api` ✅
  - Summary sau seed: customers 120, orders 140, invoices 140, products 120, employees 120, vendors/PO/shipments 120, projects 100, workflows 100+, reports 120, notifications 150.
- Khởi động runtime cho UAT:
  - Web: `http://localhost:3000`
  - API: `http://localhost:3001`

### ⚠️ Ghi chú kỹ thuật
- Lỗi build Next.js do `useSearchParams` trong client shell/board đã được loại bỏ bằng cách đọc query từ `window.location.search`.
- API chạy local với DB: `postgresql://erp:erp@localhost:55432/erp_retail`.

## VERIFICATION
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run seed:demo --workspace @erp/api` ✅
- `curl http://localhost:3001/api/v1/health` ✅

## THAY ĐỔI FILE

```
ADDED:
  .agent/sessions/2026-03-30_1423_codex.md

MODIFIED:
  apps/web/components/app-shell.tsx
  apps/web/components/crm-customers-board.tsx
  apps/web/app/globals.css
  planning/CURRENT_TASK.md
  .agent/memory/CONTEXT_SNAPSHOT.md
```

## HANDOFF CHO AGENT TIẾP THEO
1. Chờ user UAT xác nhận vị trí ô Search và bố cục sidebar đã phù hợp.
2. Nếu user muốn search toàn cục đa module (không chỉ CRM), mở rộng router/query mapping theo module active.
3. Giữ thói quen sau mỗi task: đảm bảo web app đang chạy `localhost:3000` để user kiểm tra ngay.
