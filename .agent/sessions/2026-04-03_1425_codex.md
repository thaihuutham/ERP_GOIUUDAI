# Session Log - 2026-04-03 14:25 +07

## Muc tieu
Implement plan `HR tu thiet ke form theo phu luc` theo huong regulation-first:
- Structured Field Builder (khong JSON ky thuat cho HR).
- Global Field Library + Per-Appendix Template.
- Metadata contract tra field catalog + appendix resolved fields.
- Analytics gating theo `analyticsEnabled + aggregator`.
- Scope rendering thong nhat: self chart-only, shared chart + table.

## Da thuc hien
- Backend settings/runtime:
  - `apps/api/src/modules/settings/settings-policy.types.ts`
    - bo sung default `appendixFieldCatalog` + `appendixTemplates`.
  - `apps/api/src/modules/settings/settings-policy.service.ts`
    - normalize/validate hr policy cho field catalog/template.
    - auto build legacy `appendixCatalog`.
  - `apps/api/src/common/settings/runtime-settings.service.ts`
    - runtime resolve global fields + per-appendix fields.
- Backend regulation:
  - `apps/api/src/modules/hr/hr-regulation.service.ts`
    - metadata tra `fieldCatalog` + `appendices`.
    - validate dynamic payload theo schema field.
    - append `_schema.fieldVersions` vao payload de giu lich su.
    - lock employeeId cho non-admin, admin duoc override.
- Frontend:
  - `apps/web/components/settings-center.tsx`
    - section admin cho field library + appendix templates.
  - `apps/web/components/hr-regulation-board.tsx`
    - render form dynamic theo metadata.
    - doi appendix reset/rebind field state.
    - analytics field chart/table theo `viewerScope` va config field analytics.
- Tests/doc:
  - update `apps/api/test/hr-regulation.service.test.ts`.
  - update `apps/api/test/hr-regulation.api-flow.test.ts`.
  - pass `apps/web/e2e/tests/hr-regulation-board.spec.ts`.
  - update + pass `apps/web/e2e/tests/settings-center-reports.spec.ts` voi selector HR policy linh hoat.
  - them ADR moi:
    - `docs/decisions/ADR-034-HR-REGULATION-STRUCTURED-FIELD-LIBRARY-GLOBAL-PLUS-APPENDIX.md`.
  - cap nhat runbook:
    - `docs/operations/RUNBOOK.md`.

## Verify
- `npm run lint --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/hr-regulation.service.test.ts test/hr-regulation.api-flow.test.ts` ✅
- `CI=1 PLAYWRIGHT_PORT=4245 npx playwright test apps/web/e2e/tests/hr-regulation-board.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅
- `CI=1 PLAYWRIGHT_PORT=4246 npx playwright test apps/web/e2e/tests/settings-center-reports.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅
- `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run build --workspace @erp/web` ✅

## Ghi chu
- Khong thay doi nghiep vu core cua luong duyet Regulation.
- Fallback runtime van duoc giu de tranh chan van hanh khi policy chua day du.
