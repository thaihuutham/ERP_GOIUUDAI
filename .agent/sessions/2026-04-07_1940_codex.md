# Session Log - 2026-04-07 19:40 +07

## Scope
- Implement plan: CRM Import page riêng + preview simulate + chuẩn hóa Bulk Actions modal.
- Mục tiêu chính:
  - bỏ bulk action inline trên toàn `StandardDataTable`;
  - thêm nút `Bulk Actions` mở modal;
  - CRM Customers có custom bulk modal theo field hiện có;
  - tách trang import CRM customers sang route riêng;
  - thêm backend dry-run endpoint `POST /crm/customers/import/preview`.

## Changes
- Global table bulk UX
  - `apps/web/components/ui/standard-data-table.tsx`
  - bỏ render inline `standard-table-bulk-bar`.
  - thêm nút `Bulk Actions` cạnh `Cấu hình cột`.
  - thêm generic bulk modal + hook render custom content/footer.
- CRM Customers board
  - `apps/web/components/crm-customers-board.tsx`
  - bỏ import inline block cũ.
  - thêm nút `Import` cạnh `Export` -> `/modules/crm/customers/import`.
  - thêm custom bulk modal fields: soft-skip, status, source, last contact date, tags append/replace.
  - submit rule: patch từng dòng trước, sau đó soft-skip (delete) nếu có tick.
- CRM Import page mới
  - `apps/web/app/modules/crm/customers/import/page.tsx`
  - `apps/web/components/crm-customers-import-board.tsx`
  - `apps/web/lib/crm-customer-import.ts`
  - có upload Excel, tải template, chạy mô phỏng, import thật, summary + lỗi theo dòng.
- API preview + shared import pipeline
  - `apps/api/src/modules/crm/crm.controller.ts`
  - `apps/api/src/modules/crm/crm.service.ts`
  - thêm `POST /crm/customers/import/preview` (admin only).
  - refactor pipeline import để dùng chung validation/planning cho preview và import.
- Route shell mapping
  - `apps/web/components/app-shell.tsx`
  - thêm title + active mapping cho route import CRM customers.
- Tests
  - `apps/api/test/crm.service.test.ts`
  - `apps/api/test/crm.api-flow.test.ts`
  - `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`

## Tests & Verification
- System/DB gate
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (up to date)
- Quality gate
  - `npm run lint --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run build --workspace @erp/web` ✅
- Test gate
  - `npm run test --workspace @erp/api -- --run test/crm.service.test.ts test/crm.api-flow.test.ts` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅

## Notes
- Không thêm schema/field DB mới cho bulk modal CRM.
- Không triển khai `Đánh dấu mất liên lạc` trong phase này.
- Không tạo ADR mới vì không phát sinh quyết định kiến trúc mới ngoài scope plan đã chốt.
