# Session Log - 2026-04-05 16:06 +07

## Scope
Tiếp tục IAM rollout theo kế hoạch, hoàn thành Task 6: workflow record-level exception grants để user/approver có thể truy cập workflow instance được giao ngay cả khi ngoài data scope thông thường.

## Files Changed
- `apps/api/src/modules/workflows/workflows.service.ts`
- `apps/api/test/workflows.service.test.ts`
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `.agent/sessions/2026-04-05_1606_codex.md`

## Key Outcomes
1. Hoàn thiện enforcement helper cho workflow record access:
   - `shouldEnforceWorkflowRecordAccess(...)`
   - `canActorViewWorkflowInstance(...)`
   - `assertWorkflowInstanceAccessible(...)`
   - `grantWorkflowInstanceAccess(...)`
2. Áp kiểm quyền record-level cho read-path:
   - `listInstances`
   - `getInstanceDetail`
   - `listInstanceTimeline`
   - `listInstanceApprovals`
3. Tự động cấp record grant (`recordType=WORKFLOW_INSTANCE`, actions `VIEW/APPROVE`) tại các điểm assignment:
   - `ensureStepApprovals`
   - `reassignInstance`
   - `delegateInstance`
   - `escalateInstance`
   - `autoEscalateTask`
   - `createApproval`
   - `updateApproval`
4. Bổ sung test cho assignment exception:
   - cho phép actor xem instance ngoài scope khi có grant.
   - chặn actor không liên quan khi không có grant.

## Verification
- `npm run test --workspace @erp/api -- test/workflows.service.test.ts` ✅ (`6 passed`)
- `npm run test --workspace @erp/api -- test/workflows.service.test.ts test/iam-access.service.test.ts` ✅ (`9 passed`)
- Regression gộp:
  - `npm run test --workspace @erp/api -- test/crm.api-flow.test.ts test/crm.service.test.ts test/sales.service.test.ts test/finance.service.test.ts test/permission.guard.test.ts test/iam-access.service.test.ts test/iam-scope.service.test.ts test/workflows.service.test.ts` ✅ (`36 passed`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Blockers / External
- Không có blocker kỹ thuật trong phạm vi Task 6.

## Notes
- Controller không cần thay đổi API surface ở Task 6; enforcement thực hiện tại service layer dựa trên `iamScope` context và record grants.
