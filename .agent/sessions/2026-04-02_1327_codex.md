# Session Log — 2026-04-02 13:27 +07

## Mục tiêu
- Hoàn thiện kế hoạch V1 “Bulk Selection + Bulk Actions” cho các bảng chính toàn bộ module theo phạm vi đã chốt.
- Chuẩn hóa UX/action contract ở frontend, không đổi backend API (`/bulk` không được thêm).
- Chốt quality gate web + e2e regression theo test plan.

## Thay đổi đã thực hiện
- Bổ sung phần còn thiếu trong phạm vi module:
  - `apps/web/components/assistant/assistant-access-board.tsx`
    - chuyển bảng module actions sang `StandardDataTable` có controlled selection.
    - bật bulk read-only utilities (`Copy IDs`, `Export CSV`) + `clear selection`.

- Chuẩn hóa kiến trúc bằng ADR:
  - thêm `docs/decisions/ADR-028-GLOBAL-BULK-INTERACTION-CONTRACT-MAIN-TABLES-V1.md`.
  - chốt contract v1:
    - select-all chỉ trên tập dữ liệu đang tải theo filter hiện tại.
    - `continue-on-error`, tổng hợp `success/failed`, giữ failed rows để retry.
    - destructive bulk action confirm 1 lần.

- Cập nhật e2e theo plan:
  - `apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts`
    - thêm case bulk core flow CRM/Sales/Finance (select-all loaded + mutate).
  - `apps/web/e2e/tests/workflows-module.spec.ts`
    - thêm bulk inbox approve với partial-failure.
    - mở rộng route matching cho inbox actions.
  - `apps/web/e2e/tests/assistant-module.spec.ts`
    - thêm bulk test runs/knowledge/channels.
    - fix selector assertions để giảm flake.
  - `apps/web/e2e/tests/audit-module.spec.ts`
    - thêm bulk read-only utilities (copy/export/clear).
  - `apps/web/e2e/tests/conversations-inbox.spec.ts`
    - thêm bulk `run-now` cho jobs.
  - `apps/web/e2e/tests/settings-center-reports.spec.ts`
    - thêm bulk reset-password cho IAM users.

## Verification
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅

- Frontend gates:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅

- E2E theo phạm vi kế hoạch:
  - `PLAYWRIGHT_PORT=3110 CI=1 npx playwright test -c apps/web/e2e/playwright.config.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts apps/web/e2e/tests/workflows-module.spec.ts apps/web/e2e/tests/assistant-module.spec.ts apps/web/e2e/tests/audit-module.spec.ts apps/web/e2e/tests/conversations-inbox.spec.ts apps/web/e2e/tests/settings-center-reports.spec.ts --reporter=line` ✅
  - Kết quả: `24 passed`.

## Ghi chú
- Không thay đổi backend/domain business logic; toàn bộ bulk mutate vẫn gọi endpoint hiện có theo từng bản ghi.
- Scope v1 giữ nguyên: chỉ bảng chính, chưa mở rộng bảng phụ side panel.
