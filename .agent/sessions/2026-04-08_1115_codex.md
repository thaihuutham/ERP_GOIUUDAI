# Session Log - 2026-04-08 11:15 +07

## User request
- Housekeeping `apps/web/tsconfig.tsbuildinfo`.
- UAT nghiệp vụ trên dữ liệu production-like.
- Trigger deploy + xác nhận UAT staging/prod.

## Work completed
1. Housekeeping build artifact
- Added `*.tsbuildinfo` to `.gitignore`.
- Untracked `apps/web/tsconfig.tsbuildinfo` from git index (`git rm --cached`) while keeping local file for incremental build.

2. UAT production-like data
- Fixed `apps/api/scripts/seed-demo.ts` to match current schema and reset order:
  - delete `workflowActionLog` before `workflowInstance` during tenant reset.
  - switch `customer.status` seed values to `CustomerCareStatus` enum.
- Fixed `apps/api/scripts/smoke-workflow-definition-lifecycle.ts`:
  - support `WORKFLOW_SMOKE_DEV_*` headers for auth-disabled environments.
  - read workflow `status` from `status` or `base.status` (custom-fields wrapped payload).
- Enabled `assistantAccessPolicy.enabled=true` via Settings domain API for assistant smoke verification.

3. Verification evidence (UAT)
- Infra gate:
  - `docker ps` -> stack up (`erp-postgres`, `erp-api`, `erp-web`, `redis`, `meilisearch`, `minio`).
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` -> DB port listening.
  - `npm run prisma:migrate:status --workspace @erp/api` -> `Database schema is up to date!`.
- Seed production-like:
  - `npm run seed:demo --workspace @erp/api` -> PASS (120 users/customers/vendors..., full summary table generated).
- Smoke backend:
  - `WORKFLOW_SMOKE_DEV_ROLE=ADMIN npm run smoke:workflows:lifecycle --workspace @erp/api` -> PASS (`create -> validate -> simulate -> publish -> archive`).
  - `scripts/deploy/smoke-assistant-access-boundary.sh` -> PASS (scope mismatch blocked correctly).
  - `SMOKE_SKIP_AI_QUALITY=true SMOKE_SKIP_OA_OUTBOUND=true scripts/deploy/smoke-crm-conversations.sh` -> PASS.
- E2E production-like bundle:
  - `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/assistant-module.spec.ts apps/web/e2e/tests/audit-module.spec.ts apps/web/e2e/tests/workflows-module.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts apps/web/e2e/tests/hr-attendance-board.spec.ts apps/web/e2e/tests/scm-operations-board.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line`
  - Result: `22 passed`.
- Quality gate:
  - `npm run lint --workspace @erp/api` -> PASS.
  - `npm run build --workspace @erp/api` -> PASS.
  - `npm run lint --workspace @erp/web` -> PASS.
  - `npm run build --workspace @erp/web` -> PASS.

## Notes
- No DB migration/schema change introduced in this session.
- No new ADR required (operational/script/housekeeping fixes only).
