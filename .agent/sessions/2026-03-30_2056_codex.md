# Session Log - 2026-03-30 20:56 (Codex)

## Scope
Đóng toàn bộ batch fail integration `401` của API test suite theo mode MVP single-tenant và xử lý các hạng mục deploy/UAT còn dang dở trong repo.

## Changes Completed
- API test harness (`apps/api/test`)
  - Thêm helper dùng chung `auth-test.helper.ts` để chuẩn hóa env test auth:
    - `AUTH_ENABLED=true`
    - `TENANCY_MODE=single`
    - `DEFAULT_TENANT_ID=GOIUUDAI`
    - `JWT_SECRET` theo từng suite.
  - Refactor 6 suite integration dùng helper/token tenant runtime:
    - `crm.api-flow.test.ts`
    - `scm.api-flow.test.ts`
    - `hr.api-flow.test.ts`
    - `conversations.api-flow.test.ts`
    - `conversation-quality.api-flow.test.ts`
    - `smoke.test.ts`
  - Bổ sung regression test trong `smoke.test.ts`:
    - token tenant mismatch ở single-tenant phải trả `401`.

- Deploy defaults theo MVP single-tenant
  - `.github/workflows/deploy-vm.yml`:
    - default `AUTH_ENABLED=false`
    - default `DEFAULT_TENANT_ID=GOIUUDAI`
    - thêm `TENANCY_MODE=single` và truyền sang deploy script.
  - `scripts/deploy/deploy-from-runner.sh`:
    - nhận/validate/ghi `TENANCY_MODE` vào `.deploy.env`
    - đồng bộ default runtime mvp-safe (`single`, `GOIUUDAI`, `AUTH_ENABLED=false`)
    - giữ guard bắt buộc `JWT_SECRET` hợp lệ khi `AUTH_ENABLED=true`.
  - `scripts/deploy/smoke-crm-conversations.sh`:
    - tenant mặc định theo `DEFAULT_TENANT_ID` (fallback `GOIUUDAI`)
    - auth mặc định theo `AUTH_ENABLED` (fallback `false`).

- Documentation
  - Cập nhật `docs/deployment/VM_AUTODEPLOY.md` phản ánh default mới và cách override.

## Verification
- `npm --prefix apps/api run test` ✅ (`18/18` files, `48/48` tests)
- `npm --prefix apps/api run lint` ✅
- `bash -n scripts/deploy/deploy-from-runner.sh` ✅
- `bash -n scripts/deploy/smoke-crm-conversations.sh` ✅

## Notes
- Không thay đổi contract endpoint nghiệp vụ.
- Các bước external (workflow deploy thật, smoke trên VM thật) đã ở trạng thái ready-to-run, chờ môi trường/secrets production.
