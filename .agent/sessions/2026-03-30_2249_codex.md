# Session Log - 2026-03-30 22:49 (Codex)

## Scope
Implement plan menu cây Nhân sự 9 trang con + full v1 nội bộ cho Thuế TNCN, Mục tiêu, Thông tin nhân sự (API + Web), đồng thời giữ tương thích HR cũ.

## Changes Completed

### Web (Sidebar + Route + UI)
- Refactor sidebar HR dạng cây trong `apps/web/components/app-shell.tsx`:
  - Parent `Nhân sự` toggle mở/đóng.
  - Auto-open khi path bắt đầu bằng `/modules/hr`.
  - Parent click: toggle + điều hướng `/modules/hr`.
  - Child menu đủ 9 mục với route cố định.
- Bổ sung style submenu tại `apps/web/app/globals.css`:
  - indent, active/hover, collapsed behavior và mobile spacing.
- Thêm map section HR dùng chung tại `apps/web/lib/hr-sections.ts`.
- Tạo dynamic route `apps/web/app/modules/hr/[section]/page.tsx`.
- Tạo shared screen `apps/web/components/hr-section-screen.tsx` để render section HR theo map.
- Mở rộng `apps/web/lib/module-definitions.ts` cho 3 feature mới:
  - `personal-income-tax`
  - `goals`
  - `employee-info`
- Mở rộng `apps/web/components/module-workbench.tsx` để hỗ trợ filters động theo feature.

### Backend (HR v1 domains)
- Mở rộng Prisma schema + migration cho:
  - `PersonalIncomeTaxProfile`
  - `PersonalIncomeTaxRecord`
  - `HrGoal`
- Bổ sung endpoint tại `apps/api/src/modules/hr/hr.controller.ts`:
  - `GET/POST/PATCH /api/v1/hr/personal-income-tax/profiles`
  - `GET/POST/PATCH /api/v1/hr/personal-income-tax/records`
  - `POST /api/v1/hr/personal-income-tax/records/generate`
  - `GET/POST/PATCH /api/v1/hr/goals`
  - `PATCH /api/v1/hr/goals/:id/progress`
  - `GET /api/v1/hr/employee-info`
  - `GET/PATCH /api/v1/hr/employee-info/:id`
- Bổ sung service logic tại `apps/api/src/modules/hr/hr.service.ts`:
  - PIT formula v1 theo rule khóa cứng.
  - goals CRUD + progress update.
  - employee-info list/detail/update.
- Seed demo dữ liệu PIT/goals trong `apps/api/scripts/seed-demo.ts`.

### Tests
- Unit tests mới:
  - `apps/api/test/hr.service.test.ts` (PIT formula, goal progress, employee-info validation).
- Integration tests mới:
  - `apps/api/test/hr-v1.api-flow.test.ts` (PIT profile/record/generate, goals CRUD/progress, employee-info).

### Docs / Decisions
- ADR mới:
  - `docs/decisions/ADR-016-HR-SIDEBAR-TREE-PIT-GOALS-EMPLOYEE-INFO-V1.md`

## Verification
- `npm run prisma:generate --workspace @erp/api` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- hr.service.test.ts hr-v1.api-flow.test.ts hr.api-flow.test.ts` ✅
- `npm run test --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅

## Notes
- Giữ nguyên endpoint/luồng HR cũ; chỉ bổ sung route/feature mới.
- Route `/modules/hr` vẫn là trang tổng quan HR như trước.
- Mức triển khai là v1 nội bộ, chưa có workflow approval/compliance nâng cao.
