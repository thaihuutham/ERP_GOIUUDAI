# Session Log - 2026-04-07 18:27 +07

## Scope
- User yêu cầu xử lý luôn lỗi nền khi chạy chung:
  - `crm.api-flow`
  - `search-hybrid.api-flow`
- Lỗi trước fix:
  - unhandled từ `ZaloCampaignSchedulerService.onModuleInit` (config undefined).
  - unhandled từ `ZaloPersonalPoolService.onModuleInit` / `restoreConnectedPersonalSessions` (prisma undefined/not ready).

## Changes
- Harden lifecycle init cho scheduler:
  - `apps/api/src/modules/zalo/zalo-campaign-scheduler.service.ts`
  - thêm guard dependency readiness trước khi start timer.
  - dùng `config.get` an toàn qua `configGet?.(...)`.
  - nếu campaign service chưa sẵn sàng thì skip scheduler thay vì nổ runtime.
- Harden lifecycle init cho personal pool:
  - `apps/api/src/modules/zalo/zalo-personal.pool.service.ts`
  - thêm `isPrismaReady()` check.
  - `onModuleInit` skip restore khi prisma chưa sẵn sàng.
  - bắt lỗi async `restoreConnectedPersonalSessions()` để tránh unhandled rejection.
  - thêm guard an toàn trong `resolveOrgId` và `restoreConnectedPersonalSessions`.

## Tests & Verification
- `npm run lint --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- --run test/crm.api-flow.test.ts test/search-hybrid.api-flow.test.ts` ✅
  - `crm.api-flow`: 3 passed.
  - `search-hybrid.api-flow`: 3 passed.
  - Không còn unhandled scheduler/pool như trước.

## Notes
- Fix này tập trung vào lifecycle stability trong bối cảnh integration test bootstrap.
- Không thay đổi business flow campaign/pool runtime khi dependency đầy đủ ở production.
