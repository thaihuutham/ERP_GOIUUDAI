# Session Log — 2026-04-02 14:31 +07

## Mục tiêu
- Đáp ứng yêu cầu UX: mỗi bảng có nút `Xem dữ liệu đã xóa` để xem **chỉ** dữ liệu archived, không trộn với dữ liệu hiện hành.

## Trạng thái triển khai
- Không cần sửa thêm code trong session này vì `StandardDataTable` đã có đầy đủ hành vi theo yêu cầu:
  - Nút `Xem dữ liệu đã xóa`.
  - Khi bật chỉ hiển thị tập archived.
  - Khi tắt quay về tập hiện hành.
- Đã rà soát phạm vi sử dụng `StandardDataTable` trong các bảng chính module để xác nhận áp dụng nhất quán.

## Verification
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `PLAYWRIGHT_PORT=3110 CI=1 npx playwright test -c apps/web/e2e/playwright.config.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --reporter=line` ✅ (`2 passed`)

## Ghi chú
- Không có thay đổi API/backend.
- Không phát sinh ADR mới.
