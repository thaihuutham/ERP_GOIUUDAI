# Session Log: 2026-04-01_1510

## THÔNG TIN SESSION

```text
Ngày/giờ bắt đầu : 2026-04-01 15:07
Ngày/giờ kết thúc: 2026-04-01 15:10
Agent            : Codex
Task đang làm    : Runtime recovery cho lỗi EADDRINUSE + Failed to fetch
```

## TÓM TẮT SESSION

- User gặp `EADDRINUSE` khi chạy `npm run dev:web` và UI báo `Failed to fetch`.
- Xác định cổng 3000 bị `next dev` cũ chiếm, còn API port 3001 chưa chạy.
- Đã khởi chạy lại stack dev và xác nhận API/Web đều healthy.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Runtime checks:
  - `lsof -nP -iTCP:3000 -sTCP:LISTEN` -> có process `node next-server`.
  - `lsof -nP -iTCP:3001 -sTCP:LISTEN` -> trống trước khi xử lý.
- Runtime recovery:
  - kill process cũ chiếm port `3000`.
  - start API: `npm run dev:api`.
  - start Web: `npm run dev:web`.
- Verify:
  - `curl http://localhost:3001/api/v1/health` ✅
  - `curl http://localhost:3001/api/v1/reports/overview` ✅
  - `curl -I http://localhost:3000` ✅

## HANDOFF CHO AGENT TIẾP THEO

1. Khi user báo `Failed to fetch` ở web, check API port `3001` trước.
2. Khi user báo `EADDRINUSE`, check `lsof` và dọn process cũ trước khi khởi chạy lại.
