# Session Log - 2026-04-05 09:30 +07

## Scope
Đáp ứng yêu cầu user: không rollout theo nhánh/module list, cho phép IAM v2 áp dụng toàn bộ module toàn hệ thống.

## Files Changed
- `apps/api/src/modules/settings/settings-policy.service.ts`
- `apps/api/src/common/settings/runtime-settings.service.ts`
- `apps/api/src/common/auth/permission.guard.ts`
- `apps/api/test/settings-policy.service.test.ts`
- `apps/api/test/permission.guard.test.ts`
- `docs/decisions/ADR-045-IAMV2-ALL-MODULE-ENFORCEMENT.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `planning/CURRENT_TASK.md`

## Key Outcomes
1. Chuẩn hóa semantics enforcement:
   - `iamV2.enforcementModules=[]` => áp dụng toàn bộ module.
   - Token `ALL`/`*` => canonical về `[]`.
   - Danh sách module cụ thể vẫn được hỗ trợ cho rollout từng phần.
2. `PermissionGuard` hiểu token `ALL`/`*` và chuyển về chế độ all-modules.
3. Runtime settings cho `iamV2.enforcementModules` không còn bị trộn fallback module ngoài ý muốn, và hỗ trợ token all-modules.
4. Bổ sung test regression cho all-module token ở cả settings normalization và permission guard.

## Verification
- `npm run test --workspace @erp/api -- test/permission.guard.test.ts test/settings-policy.service.test.ts test/iam-access.service.test.ts test/iam-scope.service.test.ts` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅

## Notes
- Không có migration mới trong session này.
- Mục tiêu session chỉ là semantics + wiring cho all-modules enforcement, không đổi business flow nghiệp vụ module.
