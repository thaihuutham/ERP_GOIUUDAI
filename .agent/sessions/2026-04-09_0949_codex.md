# Session Log — 2026-04-09 09:49 +07

## User request
- Tiếp tục mục `2`: xử lý callback `404` để `phase4:smoke:prod-readiness` pass mặc định.

## Work completed
- Cập nhật `scripts/deploy/smoke-production-readiness.sh`:
  - thêm biến `SMOKE_PAYMENT_CALLBACK_REQUIRED` (default `false`).
  - callback boundary behavior:
    - endpoint callback trả `404` và `SMOKE_PAYMENT_CALLBACK_REQUIRED=false` -> log + skip callback check.
    - endpoint callback trả `404` và `SMOKE_PAYMENT_CALLBACK_REQUIRED=true` -> fail cứng.
- Cập nhật docs vận hành/deploy:
  - `docs/operations/RUNBOOK.md`
  - `docs/deployment/VM_AUTODEPLOY.md`

## Verification
- `bash -n scripts/deploy/smoke-production-readiness.sh` ✅
- `set -a; source .env; set +a; scripts/deploy/smoke-production-readiness.sh` ✅
- `set -a; source .env; set +a; SMOKE_PAYMENT_CALLBACK_REQUIRED=true scripts/deploy/smoke-production-readiness.sh` ✅ (fail expected)
- `set -a; source .env; set +a; npm run phase4:smoke:prod-readiness` ✅
- `set -a; source .env; set +a; npm run phase4:release-gate` ⚠️ fail ở `@erp/web build`:
  - `Error: <Html> should not be imported outside of pages/_document` (prerender `/404`).

## Notes
- Mục tiêu unblock callback 404 cho production-readiness smoke đã hoàn thành.
- Blocker còn lại của release gate full nằm ở web build, không thuộc nhánh callback smoke.
