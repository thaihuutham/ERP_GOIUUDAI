# Session Log - 2026-04-08 16:18 +07

## User request
- Implement đầy đủ plan AI auto-reply qua n8n theo ngành.
- Mỗi nick kênh map vào một ngành để route đúng agent/workflow.
- Admin có thể đổi ngành realtime.
- AI reply bắt buộc ghi lại trong hội thoại ERP để theo dõi liền mạch.

## Work completed
1. Schema + migration
- Thêm enum `ConversationMessageOrigin` và gắn vào `ConversationMessage.origin`.
- Thêm các bảng:
  - `ai_industries`
  - `ai_routing_channel_accounts`
  - `ai_industry_bindings`
  - `ai_conversation_jobs`
  - `ai_conversation_outbox`
- Migration mới: `apps/api/prisma/migrations/20260408164000_add_ai_routing_n8n_v1/migration.sql`.

2. Routing + lifecycle services (ERP-owned)
- Thêm `apps/api/src/modules/zalo/zalo-ai-routing.service.ts`:
  - runtime mode `legacy|n8n|shadow`
  - CRUD/list cho industries, account mappings, industry bindings
  - resolver route active theo `channel + channelAccountId`.
- Thêm `apps/api/src/modules/zalo/zalo-ai-jobs.service.ts`:
  - enqueue AI job + outbox
  - outbound HMAC dispatch sang n8n
  - retry/backoff (10s/30s/90s) và handoff
  - callback verify HMAC + idempotency + guardrail re-check trước send.

3. API endpoints
- Thêm controller `apps/api/src/modules/zalo/zalo-ai-routing.controller.ts`:
  - `GET/POST/PATCH /api/v1/ai-industries`
  - `GET/POST/PATCH /api/v1/ai-routing/channel-accounts`
  - `GET/POST/PATCH /api/v1/ai-routing/industry-bindings`
  - `GET /api/v1/ai-jobs`, `GET /api/v1/ai-jobs/:id`
  - `POST /api/v1/integrations/n8n/ai-replies`.

4. Zalo flow integration
- `zalo-personal.pool.service.ts`:
  - debounce theo runtime config (default 8s)
  - enqueue n8n job khi mode `n8n/shadow`
  - mode `n8n`: chỉ queue + chờ callback send từ ERP
  - mode `shadow`: queue n8n và vẫn chạy legacy reply.
- `zalo.service.ts` + `conversations.service.ts`:
  - propagate `origin` để lưu `origin=AI` cho tin callback send thành công.

5. Context + privacy
- Context gửi sang n8n gồm tối đa 20 tin gần nhất.
- Bổ sung `historySummary` khi thread dài hơn cửa sổ 20 tin.
- Mask PII mặc định cho phone/email; hỗ trợ whitelist field theo ngành bằng `piiMaskConfigJson`.

6. Testing + ADR
- Thêm test:
  - `apps/api/test/zalo-ai-routing.service.test.ts`
  - `apps/api/test/zalo-ai-jobs.service.test.ts`
- Pass test hiện hữu:
  - `apps/api/test/zalo.service.test.ts`
  - `apps/api/test/zalo-personal.pool.service.test.ts`
- Thêm ADR:
  - `docs/decisions/ADR-055-AI-N8N-ROUTING-PER-CHANNEL-ACCOUNT.md`.

## Verification evidence
- Infra/DB gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- Prisma:
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
  - `npm run prisma:generate --workspace @erp/api` ✅
- Backend quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/zalo.service.test.ts test/zalo-personal.pool.service.test.ts test/zalo-ai-routing.service.test.ts test/zalo-ai-jobs.service.test.ts` ✅ (`23 passed`)
  - `npm run build --workspace @erp/api` ✅
- Frontend quality:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/zalo-automation-accounts-actions.spec.ts apps/web/e2e/tests/conversations-inbox.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`8 passed`)

## Notes
- Scope giữ đúng plan: ERP làm source-of-truth, n8n làm AI inference workflow.
- V1 chạy production path cho Zalo Personal; kiến trúc đã ready-by-design cho FB/TikTok.
