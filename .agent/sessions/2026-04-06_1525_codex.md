# Session Log - 2026-04-06 15:25 +07

## Scope
- Tiếp tục hoàn thiện Zalo Campaigns theo yêu cầu user.
- Đóng các khoảng trống còn thiếu: delete campaign, test backend cho core rules, e2e flow campaign.

## Changes
- Backend:
  - `apps/api/src/modules/zalo/zalo-campaign.service.ts`
    - thêm `deleteCampaign(campaignId)`:
      - bắt buộc role `ADMIN`;
      - chỉ cho phép xóa khi campaign `DRAFT`;
      - thực thi `zaloCampaign.delete(...)`.
  - `apps/api/src/modules/zalo/zalo.controller.ts`
    - thêm `DELETE /zalo/campaigns/:id`.

- Frontend:
  - `apps/web/components/zalo-automation-campaigns-workbench.tsx`
    - thêm action `Xóa nháp` cho campaign `DRAFT` (admin only);
    - thêm confirm dialog trước khi xóa;
    - refresh list + hiển thị notice sau xóa;
    - thêm `data-testid`:
      - `zalo-automation-campaigns-workbench`
      - `zalo-campaign-action-start|pause|resume|cancel|delete`

- Backend tests:
  - `apps/api/test/zalo-campaign-template.util.test.ts`
    - cover render variable alias, spin syntax, missing var, allowlist, random delay bounds.
  - `apps/api/test/zalo-campaign.service.test.ts`
    - cover:
      - filter campaign list theo operator với non-admin;
      - deny campaign detail cho user không được gán;
      - conflict account khi start campaign;
      - delete campaign draft success;
      - delete denied cho non-draft/non-admin.
  - `apps/api/test/zalo.controller.test.ts`
    - mở rộng cover campaign endpoints ở controller:
      - delegate delete;
      - parse limit query cho recipients/attempts.

- Frontend e2e:
  - `apps/web/e2e/tests/zalo-campaigns-flow.spec.ts`
    - mock API stateful;
    - cover create campaign + lifecycle actions (start/pause/resume/cancel);
    - cover delete draft campaign.

## Verification
- `npm run -w apps/api lint` ✅
- `npm run -w apps/api build` ✅
- `npm run -w apps/api test -- --run test/zalo-campaign-template.util.test.ts test/zalo-campaign.service.test.ts test/zalo.controller.test.ts` ✅ (`14 passed`)
- `npm run -w apps/web lint` ✅
- `npm run -w apps/web build` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/zalo-campaigns-flow.spec.ts apps/web/e2e/tests/sidebar-grouped-navigation.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`3 passed`)

## Stability Gate
- `docker ps --format '{{.Names}} {{.Status}}'` ✅ (`erp-postgres Up`)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
