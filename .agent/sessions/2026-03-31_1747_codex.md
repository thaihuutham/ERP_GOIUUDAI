# Session Log - 2026-03-31 17:47 +07

## Context
- Task: Fix `reports` module mismatch between Settings Center Enterprise and Dashboard warning flow.
- Scope implemented:
  - Align module options in Settings Center to shared module constants.
  - Normalize backend `enabledModules` for `org_profile`.
  - Friendly dashboard behavior when reports is disabled.
  - Remove all Settings Center presets.

## Changes implemented
- Web:
  - `apps/web/components/settings-center.tsx`
    - Replaced hardcoded `MODULE_OPTIONS` with options generated from `@erp/shared` `ERP_MODULES` (excluding `settings`).
    - Removed all preset configuration and preset UI rendering.
  - `apps/web/components/home-dashboard.tsx`
    - Added runtime pre-check via `/settings/runtime`.
    - Skip `/reports/overview` call when reports disabled and show friendly warning banner.
    - Map backend disabled-module error to same friendly message.
  - `apps/web/components/app-shell.tsx`
    - Replaced fallback enabled module list with shared constant-derived runtime togglable modules.
  - `apps/web/app/globals.css`
    - Removed obsolete `.preset-row` / `.preset-chip` styles.

- API:
  - `apps/api/src/modules/settings/settings-policy.types.ts`
    - Added `RUNTIME_TOGGLABLE_ERP_MODULES` from `ERP_MODULES` excluding `settings`.
    - Default `org_profile.enabledModules` now derives from this constant.
  - `apps/api/src/modules/settings/settings-policy.service.ts`
    - Added normalization for `org_profile.enabledModules`: lowercase, dedupe, valid-runtime-module filter.
  - `apps/api/src/common/settings/runtime-settings.service.ts`
    - Runtime reads now normalize enabled modules similarly (exclude invalid + `settings`).
  - `apps/api/src/modules/settings/settings.service.ts`
    - Legacy `system_config.enabledModules` normalization now aligns with runtime togglable module set.
  - `apps/api/scripts/seed-demo.ts`
    - Seeded `enabledModules` excludes `settings` for runtime lists.

- Tests:
  - Updated API test: `apps/api/test/settings-policy.service.test.ts`
    - Added coverage for enabledModules normalization/filtering.
  - Added E2E tests:
    - `apps/web/e2e/tests/settings-center-reports.spec.ts`
    - `apps/web/e2e/tests/dashboard-reports-availability.spec.ts`

## Verification
- `npm run test --workspace @erp/api -- settings-policy.service.test.ts` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `npx playwright test -c apps/web/e2e/playwright.reports.config.ts apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/dashboard-reports-availability.spec.ts` ✅
  - Note: temporary Playwright config used port `3110`, then removed.

## Notes
- No new ADR added (behavioral/runtime alignment only; no architecture decision change).
