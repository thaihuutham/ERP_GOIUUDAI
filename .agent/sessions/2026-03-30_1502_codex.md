# Session Log - 2026-03-30 15:02 +07

## Scope
- Kiểm tra lại dữ liệu demo theo yêu cầu user.
- Ép hệ thống chạy 1 tenant duy nhất `GOIUUDAI` cho MVP.
- Khóa phần multi-tenant runtime để tránh sai lệch khi chưa triển khai SaaS onboarding.

## Work done
1. Audit dữ liệu tenant hiện tại trên toàn bộ bảng có `tenant_Id`:
   - Kết quả: chỉ có `GOIUUDAI`.
2. Reseed dữ liệu demo:
   - Chạy `npm run seed:demo --workspace @erp/api` thành công.
   - Dữ liệu demo giữ chuẩn 100+ record/module như thiết kế.
3. Hardening single-tenant mode:
   - `resolveTenantRuntimeConfig()` thêm `TENANCY_MODE` (`single` mặc định).
   - `resolveTenantIdFromRequest()` trả cố định `DEFAULT_TENANT_ID` khi mode `single`.
   - `JwtAuthGuard` từ chối token có tenant khác tenant lock trong mode `single`.
4. Cập nhật cấu hình/tài liệu:
   - `.env`: thêm `TENANCY_MODE=single`.
   - `config/.env.example`: thêm `TENANCY_MODE=single`.
   - `docker-compose.yml`: inject `TENANCY_MODE` vào API container.
   - `docs/specs/PROJECT_OVERVIEW.md`: mô tả rõ luồng tenant theo single/multi mode.
   - `docs/deployment/VM_AUTODEPLOY.md`: thêm biến deploy `TENANCY_MODE`.
   - Tạo ADR mới: `docs/decisions/ADR-011-MVP-SINGLE-TENANCY-MODE.md`.

## Verification
- Tenant distribution check: `MISMATCH_TABLES: 0`, chỉ có tenant `GOIUUDAI`.
- API behavior:
  - Không token vào route bảo vệ => `401`.
  - Token `GOIUUDAI` + spoof header tenant khác => vẫn trả dữ liệu `GOIUUDAI`.
  - Token tenant khác `GOIUUDAI` trong mode single => `401`.
  - Public webhook + fake JWT => tenant context vẫn `GOIUUDAI`.
- `npm run lint --workspace @erp/api` pass.
- `docker compose config` pass.

## Runtime
- Web vẫn chạy tại `http://localhost:3000`.
- API chạy tại `http://localhost:3001` (session PTY), dùng DB override local: `postgresql://erp:erp@localhost:55432/erp_retail`.
