# Session Log - 2026-04-04 15:40 +07

## Mục tiêu session
- Hoàn thiện phase taxonomy CRM (stage/source) theo hướng quản trị có kiểm soát.
- Bổ sung guard backend để không thể xóa taxonomy đang được dữ liệu khách hàng sử dụng, kể cả khi update trực tiếp domain settings.
- Đồng bộ CRM Operations Board dùng controlled taxonomy cho `customerStage/source`.
- Cập nhật kế hoạch rollout cho toàn bộ trường tag/comma-list tương tự trên hệ thống.

## Thay đổi chính

### 1) Settings Center taxonomy manager (frontend)
- File: `apps/web/components/settings-center.tsx`
- Kết quả:
  - Thêm field type `taxonomyManager` cho:
    - `sales_crm_policies.customerTaxonomy.stages`
    - `sales_crm_policies.customerTaxonomy.sources`
  - Tích hợp component quản trị taxonomy:
    - load từ `GET /settings/sales-taxonomy`
    - create: `POST /settings/sales-taxonomy/:type`
    - rename: `PATCH /settings/sales-taxonomy/:type/:value`
    - delete: `DELETE /settings/sales-taxonomy/:type/:value`
  - Đồng bộ reload center/domain/taxonomy sau mỗi thao tác CRUD.

- File mới: `apps/web/components/settings-center/taxonomy-manager-field.tsx`
  - Bảng quản trị taxonomy + search + modal add/edit + nút xóa có disabled theo `canDelete`.

### 2) Guard backend chống xóa taxonomy đang dùng
- File: `apps/api/src/modules/settings/settings-policy.service.ts`
- Kết quả:
  - `updateDomain('sales_crm_policies', ...)` được bổ sung guard:
    - phát hiện taxonomy values bị remove (stages/sources)
    - query usage thực tế từ bảng `Customer`
    - throw `BadRequestException` nếu còn usage.
  - Chuẩn hóa `sales_crm_policies.customerTaxonomy` về UPPERCASE + unique trong normalize domain pipeline.

### 3) CRM Operations Board dùng controlled taxonomy cho stage/source
- File: `apps/web/components/crm-operations-board.tsx`
- Kết quả:
  - Load taxonomy từ `/crm/taxonomy`.
  - Chuyển các input stage/source từ text sang select:
    - customer filters
    - create customer form
    - update customer form
    - interaction form (`Update customer stage`)
  - Giữ các tags tự do hiện tại (nằm ở phase sau theo rollout plan).

### 4) Kế hoạch rollout đồng bộ toàn hệ thống
- File: `docs/design/TAXONOMY-TAG-FIELD-ROLLOUT-PLAN.md`
- Kết quả:
  - Bổ sung inventory theo module (Settings/CRM/Custom fields).
  - Chia phase rõ ràng từ taxonomy core đến HR template lists, CRM tag registry, custom-field options manager.
  - Định nghĩa DoD cho từng phase.

### 5) Test bổ sung
- File: `apps/api/test/settings-policy.service.test.ts`
  - Thêm test normalize taxonomy uppercase/unique.
  - Thêm test guard chặn remove taxonomy đang được sử dụng qua direct domain update.

- File mới: `apps/api/test/settings-taxonomy.service.test.ts`
  - Cover overview usage stats.
  - Cover delete guard khi đang có usage.
  - Cover rename + migrate customer values.

## Verify đã chạy

### Hạ tầng/DB
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

### API
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/settings-taxonomy.service.test.ts` ✅ (`15 passed`)

### Web
- `npm run lint --workspace @erp/web` ✅
- `npm run test:unit --workspace @erp/web` ✅ (`5 passed`)
- `npm run build --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4278 npx playwright test apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/settings-center-audit-scope.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`8 passed`)

## Ghi chú
- Session này không thay đổi contract nghiệp vụ ngoài phạm vi taxonomy/tag governance.
- ADR mới đã có sẵn trong workspace cho hướng taxonomy manager + delete guard:
  - `docs/decisions/ADR-036-TAXONOMY-MANAGEMENT-PATTERN-AND-DELETE-GUARD.md`

## Update 2026-04-04 15:43 +07 (theo yêu cầu user mở rộng đồng bộ toàn hệ thống)
- User xác nhận tiếp tục phase hiện tại và yêu cầu **cập nhật kế hoạch để sửa đồng bộ toàn bộ field cùng kiểu nhập tags tự do**.
- Đã cập nhật chi tiết `docs/design/TAXONOMY-TAG-FIELD-ROLLOUT-PLAN.md`:
  - Bổ sung inventory đầy đủ theo nhóm A-E (CRM taxonomy, Settings tags, HR appendix lists, CRM free tags, custom fields options CSV) và phase hardening đóng legacy parser.
  - Gắn rõ file UI/API liên quan cho từng nhóm để tránh sót scope.
  - Nâng roadmap thành phase matrix có thứ tự phụ thuộc, nguyên tắc rollout và DoD chung bắt buộc.
- Verify lại System Stability Gate:
  - `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/settings-taxonomy.service.test.ts` ✅ (`15 passed`)
  - `npm run lint --workspace @erp/web` ✅
  - `npm run test:unit --workspace @erp/web` ✅ (`5 passed`)
  - `npm run build --workspace @erp/web` ✅
  - `CI=1 PLAYWRIGHT_PORT=4278 npx playwright test apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/settings-center-audit-scope.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`8 passed`)

## Update 2026-04-04 16:01 +07 (Phase 2 triển khai thực tế: managed list cho security/finance)
- User yêu cầu vào luôn Phase 2 và đồng bộ UI theo chuẩn `ui-ux-pro-max`.
- Đã triển khai manager UI mới cho các field mục tiêu:
  - `access_security.superAdminIds`
  - `access_security.permissionPolicy.superAdminIds`
  - `access_security.permissionPolicy.superAdminEmails`
  - `finance_controls.postingPeriods.lockedPeriods`
- Frontend:
  - thêm component mới `apps/web/components/settings-center/settings-list-manager-field.tsx`
    - dạng bảng + search + modal add/edit/delete.
    - enforce format theo type (`userId`, `email`, `period`).
  - cập nhật wiring `apps/web/components/settings-center.tsx`
    - thêm field type `managedList`.
    - map 4 field Phase 2 sang `managedList`.
  - cập nhật style đồng bộ Settings Center:
    - `apps/web/app/styles/modules/workbench.css`.
- Backend:
  - `apps/api/src/modules/settings/settings-policy.service.ts`
    - normalize typed list cho `superAdminIds/superAdminEmails/lockedPeriods`.
    - validate chặt format email, userId, period (`YYYY-MM`) trước khi lưu.
- Test:
  - `apps/api/test/settings-policy.service.test.ts`
    - thêm test normalize managed lists.
    - thêm test reject email sai format.
  - e2e:
    - `apps/web/e2e/tests/settings-center-reports.spec.ts`
    - `apps/web/e2e/tests/settings-center-audit-scope.spec.ts`
    - verify manager UI mới ở domain security/finance.
- ADR mới:
  - `docs/decisions/ADR-037-SETTINGS-TYPED-LIST-MANAGER-FOR-SECURITY-FINANCE-TAGS.md`
- Verify System Stability Gate:
  - `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/settings-taxonomy.service.test.ts` ✅ (`17 passed`)
  - `npm run test:unit --workspace @erp/web` ✅ (`5 passed`)
  - `npm run build --workspace @erp/web` ✅
  - `npm run lint --workspace @erp/web` ✅ (chạy sau build theo known issue `.next/types`)
  - `CI=1 PLAYWRIGHT_PORT=4278 npx playwright test apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/settings-center-audit-scope.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`9 passed`)
