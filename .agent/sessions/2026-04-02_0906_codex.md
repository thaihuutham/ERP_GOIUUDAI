# Session Log — 2026-04-02 09:06 +07

## Mục tiêu
- Mã hóa key lưu DB để giảm rủi ro khi lộ database dump.
- Chọn phương án triển khai nhanh: AES-256-GCM với master key từ env.
- Không thực hiện migration plaintext -> ciphertext do môi trường chưa có dữ liệu cũ.

## Thay đổi đã thực hiện
- Crypto utility:
  - `apps/api/src/common/settings/settings-secret-crypto.util.ts`
  - thêm API:
    - `encryptSettingsSecret`
    - `decryptSettingsSecret`
    - `isEncryptedSettingsSecret`
  - chuẩn ciphertext: `enc:v1:gcm:<iv>.<tag>.<cipher>`
  - env key: `SETTINGS_ENCRYPTION_MASTER_KEY` (32-byte base64/hex).

- Settings domain persistence/runtime:
  - `apps/api/src/modules/settings/settings-policy.service.ts`
    - decrypt-on-read cho:
      - `integrations.bhtot.apiKey`
      - `integrations.ai.apiKey`
      - `integrations.zalo.accessToken`
      - `integrations.zalo.webhookSecret`
    - encrypt-on-write khi persist `settings.integrations.v1`.
    - reuse ciphertext cũ nếu plaintext không đổi để tránh write churn do IV random.
    - snapshot `integrations` cũng lưu ciphertext.
    - `resolveSecretValue` đọc trực tiếp key đã decrypt trước khi fallback `secretRef/env`.
  - `apps/api/src/common/settings/runtime-settings.service.ts`
    - runtime decrypt secret trước khi resolve precedence (`direct` > `secretRef` > `env`).
  - `apps/api/src/modules/settings/settings.service.ts`
    - mã hóa at-rest cho `system_config.bhtotSync.apiKey` (legacy bridge).
    - normalize/read tự decrypt, save tự encrypt.

- Tests/docs/ADR:
  - update `apps/api/test/settings-policy.service.test.ts`:
    - assert DB không còn plaintext key.
    - assert read lại domain vẫn nhận plaintext cho runtime/UI.
  - thêm `apps/api/test/settings-secret-crypto.util.test.ts`.
  - update docs/env:
    - `config/.env.example`
    - `docs/deployment/VM_AUTODEPLOY.md`
    - `docs/operations/RUNBOOK.md`
  - cập nhật ADR:
    - thêm `docs/decisions/ADR-027-SETTINGS-SECRETS-AT-REST-ENCRYPTION-AES-GCM.md`
    - bổ sung note liên quan vào `docs/decisions/ADR-026-UI-MANAGED-INTEGRATION-SECRETS-RUNTIME-HOT-RELOAD.md`

## Verification
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅

- API quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `SETTINGS_ENCRYPTION_MASTER_KEY=<test-key> npm run test --workspace @erp/api -- test/settings-policy.service.test.ts test/settings-secret-crypto.util.test.ts test/zalo.service.test.ts` ✅

## Ghi chú
- Không thay đổi precedence runtime đã chốt ở ADR-026 (`direct` > `secretRef` > `env`).
- Không thay đổi luồng nghiệp vụ ERP; chỉ hardening tầng lưu secret.
