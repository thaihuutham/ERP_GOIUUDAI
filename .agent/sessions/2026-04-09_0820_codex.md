# Session Log — 2026-04-09 08:20 +07

## User request
- User xác nhận tiếp tục triển khai Phase 2 của roadmap hoàn thiện ERP (Auth + RBAC rollout theo module).

## Work completed
- Settings Center (access security):
  - `apps/web/components/settings-center.tsx`
    - thêm section `IAM v2 rollout (Phase 2)`:
      - `iamV2.enabled`
      - `iamV2.mode` (`OFF|SHADOW|ENFORCE`)
      - `iamV2.enforcementModules` (ordered rollout baseline)
      - `iamV2.protectAdminCore` (advanced)
      - `iamV2.denySelfElevation` (advanced)
  - `apps/web/components/settings-center/view-model.ts`
    - tab `security-governance` bổ sung `security-iam-v2-rollout`.
  - `apps/web/components/settings-center/__tests__/view-model.test.ts`
    - thêm assertion section IDs cho `security-governance`.
- Deploy/ops scripts:
  - `scripts/deploy/iam-v2-rollout.sh`
    - action: `status`, `shadow`, `enforce`, `rollback-shadow`, `rollback-module`, `off`.
    - giới hạn rollout modules theo baseline Phase 2:
      - `sales,finance,crm,hr,scm,assets,projects,reports`.
  - `scripts/deploy/smoke-auth-rbac-modules.sh`
    - smoke health, auth boundary, mismatch endpoint, module read probes theo list enforce.
- Docs:
  - `docs/deployment/VM_AUTODEPLOY.md`
    - bổ sung script list, command rollout/rollback nhanh, command smoke auth/rbac.
  - `docs/operations/RUNBOOK.md`
    - bổ sung section vận hành Phase 2 Auth + RBAC rollout.
  - `docs/plans/2026-04-09-erp-completion-safe-baseline-roadmap.md`
    - đánh dấu baseline implementation cho Phase 2.

## Verification
- `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test:unit --workspace @erp/web -- components/settings-center/__tests__/view-model.test.ts` ✅ (`6 passed`)
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `bash -n scripts/deploy/iam-v2-rollout.sh scripts/deploy/smoke-auth-rbac-modules.sh` ✅
- `scripts/deploy/iam-v2-rollout.sh status` ✅
- `scripts/deploy/smoke-auth-rbac-modules.sh` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --workers=1 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`3 passed`)
- `CI=1 ... settings-center-reports.spec.ts --grep phase-2 ...` ⚠️ 1 pass, 1 fail (harness redirect về dashboard theo role scope trong e2e run).

## Notes
- Không thay đổi role model runtime v1 (`ADMIN/MANAGER/STAFF`).
- Không thêm migration/schema mới trong batch Phase 2 này.
- Rollout an toàn giữ đúng nguyên tắc:
  - module-by-module,
  - `SHADOW` trước `ENFORCE`,
  - rollback nhanh bằng `rollback-shadow` hoặc `rollback-module`.
