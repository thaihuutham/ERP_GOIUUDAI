# Session Log — 2026-04-08 20:44 +07

## User request
- Ở `/modules/zalo-automation/messages` cần:
  - hiển thị tên + số điện thoại khi đã nhận diện khách hàng.
  - hỗ trợ hiển thị và tải file đính kèm về máy cá nhân (không lưu trên server app).
  - nút `Mở hồ sơ đầy đủ` trong Customer 360 phải mở popup bên phải giống trang khách hàng.

## Work completed
- Customer identity display
  - `apps/web/components/zalo-automation-messages-workbench.tsx`
    - thêm hiển thị tên + SĐT khách hàng đã matched trong danh sách hội thoại.
    - thêm dòng `Khách hàng nhận diện` ở header luồng tin nhắn.
- File attachment message rendering + download
  - `apps/web/components/zalo-automation-messages-workbench.tsx`
    - thêm parser file từ `content` (RICH JSON) và `attachmentsJson`.
    - render file card (tên file, kích thước, mô tả) thay JSON raw.
    - thêm hành động `Tải file`:
      - tải trực tiếp bằng `fetch -> blob -> object URL` ở client.
      - fallback mở tab mới nếu browser/source chặn tải trực tiếp.
- Right-side full profile popup
  - `apps/web/components/zalo-automation-messages-workbench.tsx`
    - thay nút `Mở hồ sơ đầy đủ` từ Link điều hướng sang mở `SidePanel` bên phải.
    - side panel hiển thị thông tin hồ sơ khách (tổng quan, tags, nhu cầu, hợp đồng gần đây, xe/tài sản, lịch sử chăm sóc).
- Style updates
  - `apps/web/app/styles/modules/crm.css`
    - thêm style cho phone trong thread list.
    - thêm style file card/tải file trong message bubble.
    - thêm style customer profile side panel.

## Verification
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Notes
- Không có thay đổi schema/migration backend.
- Không tạo ADR mới vì đây là batch cải tiến UI/render behavior trong module Zalo messages.
