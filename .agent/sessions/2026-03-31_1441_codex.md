# Session Log: 2026-03-31_1441

## THÔNG TIN SESSION

```text
Ngày/giờ bắt đầu : 2026-03-31 14:38
Ngày/giờ kết thúc: 2026-03-31 14:41
Agent            : Codex
Account/Instance : Local Codex desktop
Task đang làm    : Runtime follow-up lỗi truy cập localhost:3000 (connection refused)
```

## TÓM TẮT SESSION

User báo không truy cập được ERP với lỗi `ERR_CONNECTION_REFUSED`.
Kiểm tra cho thấy web runtime đã dừng (không có listener trên cổng `3000`) trong khi API `3001` vẫn hoạt động.
Đã khởi động lại web runtime và xác nhận endpoint trang chủ + CSS trả `200`.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Kiểm tra trạng thái cổng dịch vụ:
  - `3000` không có listener (nguyên nhân browser bị refused).
  - `3001` vẫn có listener.
- Khởi động lại web runtime:
  - `nohup npm run dev --workspace @erp/web > /tmp/erp_web_dev_main.log 2>&1 &`
- Verify sau restart:
  - `lsof -iTCP:3000 -sTCP:LISTEN` ✅
  - `GET http://127.0.0.1:3000/` => `200` ✅
  - `GET /_next/static/css/app/layout.css?v=...` => `200` ✅

### 🔄 Đang dở
- Chưa thiết lập process manager cho runtime local (PM2/systemd) để tự khởi động lại khi process dừng.

### ❌ Chưa làm / Bỏ qua
- Không thay đổi business logic ERP.
- Không thay đổi schema/database.

## THAY ĐỔI FILE

```text
ADDED:
  .agent/sessions/2026-03-31_1441_codex.md

MODIFIED:
  planning/CURRENT_TASK.md
  .agent/memory/CONTEXT_SNAPSHOT.md
```

## QUYẾT ĐỊNH TRONG SESSION NÀY

| Quyết định | Lý do | Thay thế đã cân nhắc |
|---|---|---|
| Chỉ restart runtime web, không sửa code thêm | Lỗi lần này là process down, không phải regression code | Tiếp tục refactor code (không cần thiết) |

## HANDOFF CHO AGENT TIẾP THEO

**Context cần biết:**
- Runtime web đã được bật lại và có thể truy cập tại `http://localhost:3000`.
- Lỗi user gặp ở vòng này là hạ tầng runtime local bị dừng, không phải lỗi CSS mapping.

**Lệnh verify nhanh:**
```bash
lsof -iTCP:3000 -sTCP:LISTEN -n -P
curl -I http://127.0.0.1:3000/
```
