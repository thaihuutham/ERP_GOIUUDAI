# Session Log - 2026-03-30 15:57 +07

## Scope
- Điều tra và sửa khoảng trống lớn bất thường ở sidebar (giữa tiêu đề WORKSPACE và danh sách menu).

## Root Cause
- `apps/web/app/globals.css` đang cấu hình `.side-menu` bằng CSS Grid với `grid-template-rows: auto auto 1fr`.
- Sau khi thêm dòng tiêu đề `WORKSPACE` trong `app-shell`, phần tử này rơi vào hàng `1fr`, làm nó chiếm toàn bộ phần chiều cao còn lại và đẩy menu xuống dưới.

## Changes
1. File: `apps/web/app/globals.css`
   - `.side-menu`: chuyển từ `display: grid` sang `display: flex; flex-direction: column`.
   - `.side-nav`: thêm `flex: 1 1 auto; min-height: 0` để nav chiếm phần còn lại và scroll đúng.

## Verification
- `npm run lint --workspace @erp/web` ✅
- `curl http://localhost:3000` -> `web:200` ✅

## Runtime
- Web app vẫn chạy tại `http://localhost:3000`.
