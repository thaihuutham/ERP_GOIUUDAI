# Session Log - 2026-03-30 15:52 +07

## Scope
- Sửa UX bảng dữ liệu: chỉ mở chi tiết khi bấm vào cột link chính (ví dụ tên khách hàng), không mở khi bấm mọi vị trí trên dòng để người dùng có thể copy SĐT/text.

## Changes
1. Chuẩn hóa hành vi ở bảng dùng chung `StandardDataTable`:
   - File: `apps/web/components/ui/standard-data-table.tsx`
   - Bỏ `onClick` trên toàn bộ `<tr>`.
   - Tự xác định cột trigger mở chi tiết (`isLink` nếu có, fallback cột đầu tiên) và chỉ render nút link ở cột đó.
   - Giữ side panel chi tiết hoạt động qua `onRowClick`, nhưng không chặn thao tác chọn/copy text ở các ô còn lại.
2. Sửa bảng custom CRM:
   - File: `apps/web/components/crm-operations-board.tsx`
   - Bảng Customers: bỏ click cả dòng, chỉ mở chọn customer khi bấm vào ô `fullName`.
   - Bảng Payment Requests: bỏ click cả dòng, chỉ chọn payment request khi bấm cột `Invoice/Order`.
3. Sửa bảng custom Conversations/QC:
   - File: `apps/web/components/crm-conversations-workbench.tsx`
   - Bảng Threads/Jobs/Runs: bỏ click cả dòng, chỉ chọn khi bấm cột đầu tiên dạng link.
4. Bổ sung style cho nút link chọn dòng:
   - File: `apps/web/app/globals.css`
   - Thêm class `.row-select-trigger` để reset style button thành link text, hỗ trợ `focus-visible`.

## Verification
- `npm run lint --workspace @erp/web` ✅
- `rg "<tr...onClick" apps/web/components` ✅ (không còn `onClick` trực tiếp trên `<tr>`)
- Runtime check:
  - `web:200` tại `http://localhost:3000`
  - `api:200` tại `http://localhost:3001/api/v1/health`

## Runtime
- Web app đang chạy tại `http://localhost:3000`.
- API app đang chạy tại `http://localhost:3001`.
