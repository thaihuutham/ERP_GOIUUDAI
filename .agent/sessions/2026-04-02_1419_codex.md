# Session Log — 2026-04-02 14:19 +07

## Mục tiêu
- Ẩn các bản ghi đã đánh dấu archive để bảng chính gọn hơn.
- Không đổi API/backend/business flow, chỉ chỉnh hành vi hiển thị ở UI table.

## Thay đổi đã thực hiện
- Cập nhật component dùng chung `StandardDataTable`:
  - `apps/web/components/ui/standard-data-table.tsx`
  - thêm cơ chế nhận diện row archived theo marker:
    - `status === ARCHIVED`
    - `lifecycleStatus === ARCHIVED`
    - `isArchived` / `is_archived` = `true`
    - có `archivedAt` / `archived_at`
  - mặc định `hideArchivedRows=true`.
  - toàn bộ selection/bulk/select-all/retry/empty-state chuyển sang tập dữ liệu sau lọc (`tableData`).
  - bulk meta hiển thị thêm số dòng archived đang bị ẩn.

## Verification
- Frontend gates:
  - `npm run build --workspace @erp/web` ✅
  - `npm run lint --workspace @erp/web` ✅
- Regression e2e đa module:
  - `PLAYWRIGHT_PORT=3110 CI=1 npx playwright test -c apps/web/e2e/playwright.config.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts apps/web/e2e/tests/workflows-module.spec.ts apps/web/e2e/tests/assistant-module.spec.ts apps/web/e2e/tests/audit-module.spec.ts apps/web/e2e/tests/conversations-inbox.spec.ts apps/web/e2e/tests/settings-center-reports.spec.ts --reporter=line` ✅
  - Kết quả: `24 passed`.

## Ghi chú
- Không phát sinh quyết định kiến trúc mới (không cần ADR mới).
- Thay đổi áp dụng ngay cho các bảng đang dùng `StandardDataTable`.
