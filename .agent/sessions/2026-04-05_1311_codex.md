# Session Log - 2026-04-05 13:11 +07

## Scope
Thực thi nhóm việc “next project task” theo yêu cầu user: tăng độ sẵn sàng deploy/UAT và chạy xác nhận local cho health, search, smoke.

## Files Changed
- `.github/workflows/deploy-vm.yml`
- `scripts/deploy/deploy-from-runner.sh`
- `scripts/deploy/smoke-crm-conversations.sh`
- `docker-compose.yml`
- `apps/web/Dockerfile`
- `config/.env.example`
- `docs/operations/RUNBOOK.md`
- `docs/deployment/VM_AUTODEPLOY.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `planning/CURRENT_TASK.md`

## Key Outcomes
1. Đồng bộ env deploy quan trọng xuyên suốt workflow -> runner script -> compose:
   - `NEXT_PUBLIC_AUTH_ENABLED`
   - `PERMISSION_ENGINE_ENABLED`
   - `SETTINGS_ENCRYPTION_MASTER_KEY`
   - `BHTOT_API_KEY`
2. Sửa build web để `NEXT_PUBLIC_AUTH_ENABLED` được apply ở compile-time trong container build.
3. Sửa bug `unbound variable` trong `smoke-crm-conversations.sh` và thêm cờ `SMOKE_SKIP_AI_QUALITY=true` để smoke phân lớp khi chưa có AI key.
4. Chạy reindex search thành công sau khi bật Meilisearch local.

## Verification
- Deploy/runtime scripts:
  - `bash -n scripts/deploy/deploy-from-runner.sh` ✅
  - `bash -n scripts/deploy/smoke-crm-conversations.sh` ✅
  - `bash -n scripts/deploy/smoke-assistant-access-boundary.sh` ✅
  - `docker compose config` ✅
  - `docker compose build web` ✅
- Stability gate:
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
- Health + search:
  - `scripts/deploy/healthcheck.sh` ✅
  - `docker compose up -d meilisearch` ✅
  - `GET /api/v1/settings/search/status` ✅ (`healthy=true`)
  - `POST /api/v1/settings/search/reindex` (`entity=all`, header `x-erp-dev-role: ADMIN`) ✅
    - `customers=122`, `orders=140`, `products=120`
- Smoke:
  - `scripts/deploy/smoke-assistant-access-boundary.sh` ✅
  - `SMOKE_SKIP_OA_OUTBOUND=true scripts/deploy/smoke-crm-conversations.sh` ❌ (expected: thiếu `AI_OPENAI_COMPAT_*`, run status `ERROR`)
  - `SMOKE_SKIP_AI_QUALITY=true SMOKE_SKIP_OA_OUTBOUND=true scripts/deploy/smoke-crm-conversations.sh` ✅

## Blockers / External
- Không thể dispatch workflow GitHub trực tiếp từ môi trường hiện tại vì thiếu `gh` CLI/token (`gh: command not found`).
- Cần chạy `deploy-vm` từ GitHub UI (workflow_dispatch) hoặc từ môi trường đã cấu hình GitHub auth.

## Notes
- Không phát sinh quyết định kiến trúc mới; không tạo ADR mới.
