# Session Log — 2026-04-08 21:08 +07

## User request
- Gộp các cụm toolbar nhiều dòng (như màn CRM khách hàng) thành 1 dòng để giao diện gọn và tăng diện tích hiển thị bảng.

## Work completed
- Added shared right-side toolbar slot for `StandardDataTable`
  - `apps/web/components/ui/standard-data-table.tsx`
    - thêm prop `toolbarRightContent?: ReactNode`.
    - render action custom trong vùng `standard-table-toolbar-right` trước cụm nút mặc định.
- Added toolbar custom group style
  - `apps/web/app/styles/tables.css`
    - thêm `.standard-table-toolbar-right-custom` để group action custom đồng bộ gap/wrap.
- Collapsed CRM customers controls into a single toolbar row
  - `apps/web/components/crm-customers-board.tsx`
    - bỏ `main-toolbar` tách riêng phía trên bảng.
    - chuyển dropdown trạng thái + nút lọc vào `toolbarLeftContent` của `StandardDataTable`.
    - chuyển `Export`, `Import`, `Quản lý xe`, `Khách hàng` vào `toolbarRightContent`.

## Verification
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`3 passed`)

## Notes
- Không thay đổi schema/migration backend.
- Không phát sinh ADR mới vì đây là tối ưu layout UI trên component sẵn có.
