# Session Log - 2026-04-08 08:30 +07

## Scope
- User yêu cầu implement kế hoạch Phase 1: pagination + sorting server-driven cho toàn bộ màn dùng `StandardDataTable`.
- Mục tiêu trong session này: hoàn tất phần còn thiếu, verify quality gate và cập nhật handoff bắt buộc.

## Changes
- Assistant Runs board hoàn tất server-driven table wiring
  - `apps/web/components/assistant/assistant-runs-board.tsx`
    - default `limit` đổi về `25`, options còn `25/50/100`.
    - thêm `cursor + sortBy + sortDir` khi gọi `assistantApi.listRuns(...)`.
    - đồng bộ `pageInfo`/`sortMeta` vào `StandardDataTable`.
    - bổ sung `sortKey`/disable-sort tooltip cho cột không whitelist.

- Audit module chuyển sang pagination/sorting contract chuẩn
  - `apps/api/src/modules/audit/audit.controller.ts`
    - nhận thêm query `sortBy`, `sortDir` cho `/audit/logs` và `/audit/objects/:entityType/:entityId/history`.
  - `apps/api/src/modules/audit/audit.service.ts`
    - mở rộng query type với `sortBy/sortDir`.
    - thêm `resolveSortQuery()` whitelist `createdAt`.
    - hot-tier query dùng order động theo `sortDir`.
    - mixed-tier merge sort theo `sortDir`.
    - response bổ sung `sortMeta`.
  - `apps/web/components/audit-operations-board.tsx`
    - chuyển từ "load more append" sang pager cursor history (`useCursorTableState`).
    - gửi `cursor/limit/sortBy/sortDir` khi gọi API audit.
    - `StandardDataTable` nhận `pageInfo/sortMeta/onPage*/onSortChange`.

- Module Workbench thêm hỗ trợ dual-contract server-driven
  - `apps/web/components/module-workbench.tsx`
    - dùng `normalizePagedListPayload(...)` + `useCursorTableState(...)`.
    - query list endpoint gửi `limit/cursor` và `sortBy/sortDir` khi có sort.
    - fallback an toàn nếu endpoint cũ không trả metadata.
    - pass `pageInfo/sortMeta/callbacks` vào `StandardDataTable`.

- StandardDataTable hook-order safety fix
  - `apps/web/components/ui/standard-data-table.tsx`
    - bỏ `useMemo` chạy sau early-return loading để tránh vi phạm rules-of-hooks khi render condition.

## Tests & Verification
- Docker/DB gate
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (có `erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date`)

- Build/lint gate
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅

- API test gate (targeted)
  - `npm run test --workspace @erp/api -- --run test/audit.service.test.ts test/workflows.service.test.ts test/sales.service.test.ts test/finance.service.test.ts test/hr.service.test.ts test/scm.service.test.ts test/crm.service.test.ts` ✅ (48 tests pass)

- E2E smoke (targeted multi-module)
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/assistant-module.spec.ts apps/web/e2e/tests/audit-module.spec.ts apps/web/e2e/tests/workflows-module.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line`
  - Kết quả: **15 passed, 4 failed**.
  - Các fail tập trung vào expectation cũ của test bulk/guard (không phải compile/runtime crash):
    - assistant route-guard heading expectation mismatch.
    - assistant/workflows bulk action button expectation (`Phê duyệt`) không còn inline.
    - audit bulk summary text expectation khác sau cơ chế bulk modal.

## Notes
- Không thêm migration/schema mới.
- Không tạo ADR mới trong session này (thực thi theo plan đã chốt).
