# Session Log - 2026-03-31 19:28 +07

## Context
- User follow-up:
  - yêu cầu trigger deploy workflow staging/prod để apply migrations.
  - hỏi lý do không sửa được hồ sơ khách hàng trong panel chi tiết khi click tên khách hàng.

## Work completed
- CRM detail panel edit hotfix:
  - File: `apps/web/components/crm-customers-board.tsx`
  - Root cause:
    - Nút `Chỉnh sửa hồ sơ` trong panel detail bị `disabled`, không có submit handler.
  - Implemented:
    - Added editable mode in detail panel (`isDetailEditing` + `detailForm`).
    - Added save handler `handleSaveDetailProfile` with real API call:
      - `PATCH /crm/customers/:id`
    - Added editable fields:
      - `fullName`, `phone`, `email`, `customerStage`, `source`, `segment`, `status`, `tags`
    - Added proper save/cancel UX:
      - `Lưu hồ sơ`, `Hủy chỉnh sửa`, loading state, success/error message.
    - Preserved role guard (`canMutate`) so chỉ manager/admin được chỉnh sửa.

- Deploy trigger attempt:
  - Checked workflow: `.github/workflows/deploy-vm.yml`
    - only `deploy-vm` workflow exists, currently bound to runner label `vm-prod`.
    - no dedicated staging workflow/job found in repo.
  - Checked GitHub CLI/auth:
    - `gh` CLI not installed in this environment (`command not found`).
    - no GitHub token (`GH_TOKEN`/`GITHUB_TOKEN`) available in environment.
  - Result:
    - cannot trigger `workflow_dispatch` from this session.

## Verification
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅

## Notes
- Deploy request is partially blocked by environment capability (no `gh` + no token).
- Core flow implementation from prior session remains valid and verified; this session focused on user-reported customer-detail edit issue and deploy trigger attempt.
