# Session Log - 2026-04-08 10:20 +07

## Scope
- User yêu cầu: tiếp tục theo roadmap xây dựng chức năng phân trang và sắp xếp dữ liệu cho cột.
- Phạm vi thực thi: hoàn thiện server-driven list contract (`cursor + sortBy/sortDir + pageInfo/sortMeta`) cho các module còn lại đang dùng `ModuleWorkbench/StandardDataTable`.

## Work Done
- Backend `catalog`:
  - Nâng `GET /catalog/products` sang cursor pagination + sort whitelist.
  - Trả response thống nhất: `items`, `pageInfo`, `sortMeta`.
  - Giữ tương thích nhánh hybrid search, đồng bộ về contract mới.
- Backend `assets`:
  - Nâng `GET /assets` và `GET /assets/allocations` sang cursor pagination + sort metadata.
- Backend `projects`:
  - Nâng các list endpoint:
    - `GET /projects`
    - `GET /projects/tasks`
    - `GET /projects/resources`
    - `GET /projects/budgets`
    - `GET /projects/time-entries`
  - Cập nhật controller nhận `PaginationQueryDto` cho resources/budgets/time-entries.
- Backend `reports`:
  - Nâng `GET /reports/module`, `GET /reports`, `GET /reports/:id/runs` sang contract list chuẩn.
  - Cập nhật controller `reports/module` forward full query dto.
- Tests:
  - Thêm coverage cho cursor/sort contract trong:
    - `catalog.service.test.ts`
    - `assets.service.test.ts`
    - `projects.service.test.ts`
    - `reports.service.test.ts`
  - Cập nhật integration `search-hybrid.api-flow.test.ts` theo response contract mới của `catalog/products`.

## Verification
- System gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up).
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅.
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date`).
- Backend quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
- Targeted tests:
  - `npm run test --workspace @erp/api -- --run test/catalog.service.test.ts test/assets.service.test.ts test/projects.service.test.ts test/reports.service.test.ts test/search-hybrid.api-flow.test.ts` ✅ (`19 passed`).

## Notes
- Không thêm migration/schema.
- Không tạo ADR mới (không có quyết định kiến trúc mới ngoài mở rộng pattern đã duyệt).
- `apps/web/tsconfig.tsbuildinfo` vẫn để ngoài commit theo housekeeping plan.
