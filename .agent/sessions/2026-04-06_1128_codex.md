# Session Log - 2026-04-06 11:28 +07

## Scope
- Điều tra lỗi duplicate message trong ERP khi gửi từ UI ERP (Zalo thật chỉ có 1 message).

## Findings
- Frontend chỉ gọi một endpoint gửi tin cho ZALO_PERSONAL, không có double-submit từ UI.
- DB cho thấy duplicate pattern rõ ràng:
  - row 1 (optimistic): senderName `admin@local.erp`, `externalMessageId` rỗng;
  - row 2 (listener self): senderName `Tào Thám`, có `externalMessageId` thật.
- Sau khi parse được external id, phát sinh race-condition insert giữa listener và optimistic path gây lỗi `P2002` unique constraint.

## Changes
- `apps/api/src/modules/zalo/zalo-personal.pool.service.ts`
  - mở rộng `resolveOutboundMessageId(...)` để parse nested response (`message`, `attachment`, `response`, `result`).
  - trong `sendMessage(...)`, nếu không có `externalMessageId` thì skip optimistic ingest và chờ listener sync.
- `apps/api/src/modules/conversations/conversations.service.ts`
  - harden `createOrReuseMessage(...)`:
    - catch `PrismaClientKnownRequestError` code `P2002`;
    - nếu conflict theo `externalMessageId`, fetch row existing và return thay vì throw.
- `apps/api/test/zalo-personal.pool.service.test.ts`
  - thêm test parse nested outbound id.
  - thêm test skip optimistic ingest khi send response thiếu external id.

## Verification
- `npm run -w apps/api test -- --run test/zalo-personal.pool.service.test.ts test/conversations.realtime.test.ts` ✅
- `npm run -w apps/api build` ✅
- `npm run -w apps/api lint` ✅

## Runtime Verification
- Restart API thành công (new PID: `25363`) và health `ok=true`.
- Listener account `cmnmdk9gh0rcqt3dwvak88c0a` ở trạng thái `CONNECTED`, `wsState=OPEN`.
- Controlled test send marker `"[ERP-DEDUPE-CHECK-1775449689]"`:
  - API trả `{"success":true}`;
  - DB query cho marker có đúng `COUNT=1` (không duplicate).
