# Session Log - 2026-04-07 16:45 +07

## Scope
- Bổ sung import dữ liệu xe bằng Excel ở trang CRM Vehicles và giới hạn chỉ `ADMIN` được sử dụng.

## Changes
- Backend/API:
  - `apps/api/src/modules/crm/crm.controller.ts`
    - thêm `POST /crm/vehicles/import`.
  - `apps/api/src/modules/crm/crm-contracts.service.ts`
    - thêm `importVehicles(payload)`:
      - bắt buộc role `ADMIN`, non-admin trả `ForbiddenException`;
      - validate có `rows`;
      - import từng dòng qua `createVehicle(...)`;
      - hỗ trợ resolve customer owner theo:
        - `ownerCustomerId` (ưu tiên),
        - fallback `ownerCustomerPhone`/`customerPhone`/`phone` (normalize phone);
      - trả summary: `totalRows`, `importedCount`, `skippedCount`, `errors`.
    - thêm helper `resolveVehicleImportOwnerCustomerId(...)`.
- Frontend/UI:
  - `apps/web/components/crm-vehicles-board.tsx`
    - thêm parser `xlsx` cho file import xe (sheet đầu tiên);
    - hỗ trợ alias cột: `ownerCustomerId | ownerCustomerPhone`, `ownerFullName`, `ownerAddress`,
      `plateNumber`, `chassisNumber`, `engineNumber`, `vehicleKind`, `vehicleType`, `seatCount`, `loadKg`, `status`;
    - thêm section upload `.xlsx/.xls` chỉ hiện cho admin;
    - gọi API `/crm/vehicles/import` và hiển thị kết quả + lỗi theo dòng.
- Tests:
  - `apps/api/test/crm-contracts.service.test.ts`
    - thêm test chặn import khi non-admin;
    - thêm test admin import và resolve owner theo phone.

## Tests & Verification
- `npm run test --workspace @erp/api -- --run test/crm-contracts.service.test.ts` ✅
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run build --workspace @erp/web` ✅
- `cd apps/web && rm -f tsconfig.tsbuildinfo && npm run lint` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅
- `docker ps --format 'table {{.Names}}\\t{{.Status}}'` ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅

## Notes
- `npm run lint --workspace @erp/web` có thể fail do `.next/types` stale; workaround đã áp dụng: xóa `apps/web/tsconfig.tsbuildinfo` rồi lint lại.
