# Session Log - 2026-04-05 16:28 +07

## Scope
Tiếp tục IAM rollout theo kế hoạch, hoàn thành Task 7: bổ sung guardrail cho Settings Enterprise user permission overrides (admin-core protection + self-elevation protection).

## Files Changed
- `apps/api/src/modules/settings/settings-enterprise.service.ts`
- `apps/api/src/modules/settings/settings.controller.ts`
- `apps/api/test/settings-enterprise.service.test.ts`
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `.agent/sessions/2026-04-05_1628_codex.md`

## Key Outcomes
1. Thêm guardrail trong `putUserPermissionOverrides`:
   - Chặn actor không phải `ADMIN` khi target user có role `ADMIN`.
   - Chặn self-elevation cho actor không phải `ADMIN`:
     - tự thêm `ALLOW` overrides.
     - tự gỡ `DENY` overrides hiện có.
2. Bổ sung helper trong service:
   - `resolveActorContext(...)`
   - `assertPermissionOverrideGuardrails(...)`
3. Tích hợp `IamCeilingService` (optional injection, fallback local instance) để thống nhất rule admin-core protection.
4. Bổ sung audit trail cho endpoint override user permission:
   - `@AuditAction({ action: 'UPDATE_USER_PERMISSION_OVERRIDES', ... })`.
5. Mở rộng test service cho 2 case guardrail mới.

## Verification
- `npm run test --workspace @erp/api -- test/settings-enterprise.service.test.ts` ✅ (`4 passed`)
- `npm run test --workspace @erp/api -- test/settings-enterprise.service.test.ts test/permission.guard.test.ts test/iam-access.service.test.ts test/iam-scope.service.test.ts` ✅ (`15 passed`)
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Blockers / External
- Không có blocker kỹ thuật trong phạm vi Task 7.

## Notes
- Không thay đổi business flow ERP ngoài guardrail bảo mật cho endpoint override quyền.
- Chưa triển khai Task 8+ trong IAM plan (scope override APIs, title mapping, mismatch report).
