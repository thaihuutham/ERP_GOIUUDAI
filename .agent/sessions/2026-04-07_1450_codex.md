# Session Log - 2026-04-07 14:50 +07

## Scope
- Triển khai mở rộng quản lý xe CRM theo phương án đã chốt:
  - thêm cập nhật/lưu trữ xe (soft-delete);
  - phân quyền server-side: `ADMIN` toàn quyền, non-admin chỉ sửa/xóa xe thuộc khách hàng mình phụ trách;
  - bổ sung UI thao tác xe tại panel chi tiết khách hàng;
  - bổ sung trang quản lý xe tập trung `/modules/crm/vehicles`.

## Changes
- Backend API:
  - `apps/api/src/modules/crm/crm.controller.ts`
    - thêm `PATCH /crm/vehicles/:id` (`UPDATE_CRM_VEHICLE`)
    - thêm `DELETE /crm/vehicles/:id` (`ARCHIVE_CRM_VEHICLE`, soft-delete)
  - `apps/api/src/modules/crm/crm-contracts.service.ts`
    - thêm `updateVehicle(...)`, `archiveVehicle(...)`;
    - áp dụng enforcement quyền ghi xe theo actor context (`AUTH_USER_CONTEXT_KEY`):
      - `ADMIN` bypass;
      - non-admin phải match `customer.ownerStaffId === actor.userId`, sai trả `403`.
    - create vehicle cũng kiểm tra quyền theo owner customer;
    - `listVehicles` trả thêm `ownerCustomer.ownerStaffId` để UI quyết định quyền thao tác.
- Backend test:
  - `apps/api/test/crm-contracts.service.test.ts`
    - thêm test chặn non-admin tạo xe ngoài ownership;
    - thêm test admin update/archive (archive set `status=ARCHIVED`).
- Frontend CRM customer detail:
  - `apps/web/components/crm-customers-board.tsx`
    - thêm identity resolve phía client (auth/dev mode) để quyết định quyền hiển thị thao tác xe;
    - thêm nút `Quản lý xe` trên toolbar CRM;
    - trong panel chi tiết khách hàng:
      - thêm `Thêm xe`, `Sửa`, `Xóa` cho mục Thông tin xe theo quyền;
      - thêm form create/edit xe inline, gọi API `/crm/vehicles`.
- Frontend trang quản lý xe tập trung:
  - thêm component `apps/web/components/crm-vehicles-board.tsx`:
    - list/filter theo keyword, khách hàng, nhóm xe;
    - chi tiết xe;
    - create/edit/archive theo quyền.
  - thêm route `apps/web/app/modules/crm/vehicles/page.tsx`.
  - `apps/web/components/app-shell.tsx`
    - title mapping: `CRM • Quản lý xe` cho `/modules/crm/vehicles`.

## Verification
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- Backend quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- --run test/crm-contracts.service.test.ts` ✅
- Frontend quality:
  - `npm run build --workspace @erp/web` ✅
  - `cd apps/web && rm -f tsconfig.tsbuildinfo && npm run lint` ✅
  - note: `npm run lint --workspace @erp/web` có thể fail nếu `apps/web/tsconfig.tsbuildinfo` stale; xóa file buildinfo rồi lint lại pass.
- E2E regression:
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`2 passed`)

## Notes
- Không tạo ADR mới vì chưa phát sinh quyết định kiến trúc mới; đây là mở rộng đúng hướng đã được user xác nhận.
- `apps/web/tsconfig.tsbuildinfo` đã bị thay đổi do lint incremental (file đang được track trong repo).
