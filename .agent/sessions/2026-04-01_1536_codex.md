# Session Log: 2026-04-01_1536

## THÔNG TIN SESSION

```text
Ngày/giờ bắt đầu : 2026-04-01 15:14
Ngày/giờ kết thúc: 2026-04-01 15:36
Agent            : Codex
Task đang làm    : Bật chức năng xóa mềm (archive) cho CRM/Sales/Finance/HR
```

## TÓM TẮT SESSION

- User cần xóa được nhân viên, bảng lương, tuyển dụng, khách hàng, hóa đơn, đơn hàng.
- Hệ thống trước đó thiếu cả endpoint API lẫn nút thao tác UI cho các entity này.
- Đã triển khai xóa mềm (`status=ARCHIVED`) + cập nhật UI để thao tác trực tiếp.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Backend API
- CRM:
  - `DELETE /crm/customers/:id`
  - `DELETE /crm/customer-360/:id`
- Sales:
  - `DELETE /sales/orders/:id`
  - Guard: chặn archive nếu còn invoice liên kết chưa đóng.
- Finance:
  - `DELETE /finance/invoices/:id`
  - Guard: chặn archive nếu đã có allocation / paid amount.
- HR:
  - `DELETE /hr/employees/:id`
  - `DELETE /hr/payrolls/:id`
  - `DELETE /hr/payroll-components/:id`
  - `DELETE /hr/recruitment/:id`

### ✅ Frontend UI
- Thêm nút lưu trữ trên panel chi tiết:
  - `apps/web/components/crm-customers-board.tsx`
  - `apps/web/components/sales-operations-board.tsx`
  - `apps/web/components/finance-operations-board.tsx`
  - `apps/web/components/hr-operations-board.tsx`
- Bổ sung action `DELETE` vào `apps/web/lib/module-definitions.ts` cho các feature:
  - customer-360, orders, employees, payroll-components, payroll, recruitment, invoices.

## VERIFY

- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/sales.service.test.ts test/finance.service.test.ts test/hr.service.test.ts test/crm.api-flow.test.ts` ✅
- `npm run lint --workspace @erp/web` ✅
- `npm run build --workspace @erp/web` ✅
- `npx playwright test -c apps/web/e2e/playwright.crm-sales-finance.config.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts` ✅
- `npx playwright test -c apps/web/e2e/playwright.crm-sales-finance.config.ts apps/web/e2e/tests/hr-recruitment-pipeline.spec.ts` ✅
- `docker ps` có `erp-postgres` Up ✅
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅

## HANDOFF CHO AGENT TIẾP THEO

1. Nếu cần “xóa cứng” thật sự (hard delete), phải có kế hoạch migration + ADR riêng vì liên quan audit/đối soát.
2. Với invoice/order có dữ liệu tài chính đã phát sinh, giữ guard hiện tại để tránh mất tính toàn vẹn nghiệp vụ.
