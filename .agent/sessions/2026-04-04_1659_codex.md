# Session Log - 2026-04-04 16:59 +07

## Mục tiêu session
- Vào phase tiếp theo trong rollout plan theo yêu cầu user: hoàn tất chuẩn hóa `CRM free tags` + `custom-fields options`.
- Khóa gate chất lượng end-to-end trước handoff.

## Thay đổi chính

### 1) Chốt phase CRM tag registry + custom-fields options theo chuẩn managed input
- Xác nhận và giữ đồng bộ implementation đang có:
  - CRM tag registry CRUD/usage guard ở settings backend.
  - CRM service enforce tag registry cho create/update customer + create interaction.
  - UI CRM đổi từ free-text tags/resultTag sang controlled select/multi-select.
  - Custom fields đổi từ CSV options sang row model `{ key, label, order }` ở API + UI.

### 2) Sửa test backend còn lệch contract message
- File: `apps/api/test/settings-policy.service.test.ts`
- Điều chỉnh assertion của guard xóa taxonomy/tag theo message hiện tại:
  - từ: `Không thể xóa taxonomy đang được sử dụng bởi khách hàng`
  - sang: `Không thể xóa taxonomy/tag đang được sử dụng`

### 3) Cập nhật handoff bắt buộc
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- file session hiện tại.

## Verify đã chạy

### Hạ tầng/DB
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `DATABASE_URL=postgresql://erp:erp@localhost:55432/erp_retail npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

### API
- `npm run lint --workspace @erp/api` ✅
- `npm run build --workspace @erp/api` ✅
- `npm run test --workspace @erp/api -- test/settings-taxonomy.service.test.ts test/custom-fields.service.test.ts test/settings-policy.service.test.ts` ✅ (`28 passed`)

### Web
- `npm run test:unit --workspace @erp/web` ✅ (`5 passed`)
- `npm run build --workspace @erp/web` ✅
- `npm run lint --workspace @erp/web` ✅
- `CI=1 PLAYWRIGHT_PORT=4286 npx playwright test apps/web/e2e/tests/settings-center-reports.spec.ts apps/web/e2e/tests/settings-center-audit-scope.spec.ts apps/web/e2e/tests/crm-sales-finance-core-flow.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`10 passed`)

## Ghi chú
- Session này không thay đổi business logic ERP ngoài phạm vi chuẩn hóa free tag/options theo rollout plan.
- Không phát sinh migration DB.
