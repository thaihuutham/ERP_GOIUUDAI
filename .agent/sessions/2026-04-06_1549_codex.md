# Session Log - 2026-04-06 15:49 +07

## Scope
- Debug 2 issue runtime user report:
  - `Cannot GET /api/v1/zalo/campaigns`.
  - account PERSONAL hiển thị `DISCONNECTED` dù vẫn nhắn được qua ERP.

## Root Cause
1. Campaign API 404:
- Process API đang chạy từ 11:27 (cũ), chưa nạp phần code route campaigns mới.

2. Status DISCONNECTED giả:
- `zaloAccount.status` có thể stale khi listener từng emit `closed`.
- Runtime có thể vẫn còn `api` và websocket `OPEN`, nhưng UI list account đọc status DB nên vẫn hiện `DISCONNECTED`.

## Changes
- Runtime operations:
  - restart API process cổng `3001` để load source mới.
  - verify lại route `GET /api/v1/zalo/campaigns` trả `200`.
  - reconnect account `cmnmdk9gh0rcqt3dwvak88c0a` bằng endpoint:
    - `POST /api/v1/zalo/accounts/:id/personal/reconnect`.

- Code fix:
  - `apps/api/src/modules/zalo/zalo.service.ts`
    - cập nhật `listAccounts(...)`:
      - với account `PERSONAL`, đọc runtime listener debug từ pool;
      - nếu `hasApi=true` và `wsStateLabel='OPEN'` thì override status trả về thành `CONNECTED`.

## Verification
- `npm run -w apps/api lint` ✅
- `npm run -w apps/api build` ✅
- `npm run -w apps/api test -- --run test/zalo.controller.test.ts test/zalo.service.test.ts` ✅ (`12 passed`)
- Runtime checks:
  - `GET /api/v1/zalo/campaigns` ✅ (`200`)
  - `GET /api/v1/zalo/accounts?accountType=PERSONAL` ✅ (`status=CONNECTED` cho account đã reconnect)
  - `GET /api/v1/zalo/accounts/:id/personal/listener-debug` ✅ (`poolStatus=CONNECTED`, `wsStateLabel=OPEN`)

## Stability Gate
- `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅ (`erp-postgres Up`)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
