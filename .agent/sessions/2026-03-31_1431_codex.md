# Session Log: 2026-03-31_1431

## THÔNG TIN SESSION

```text
Ngày/giờ bắt đầu : 2026-03-31 14:20
Ngày/giờ kết thúc: 2026-03-31 14:31
Agent            : Codex
Account/Instance : Local Codex desktop
Task đang làm    : Hotfix lỗi UI mất CSS tái phát trên localhost:3000 + chống tái phát
```

## TÓM TẮT SESSION

Đã xử lý lỗi UI hiển thị thô (mất style) tái phát do CSS endpoint `/_next/static/css/app/layout.css` trả `404`.
Nguyên nhân gốc được xác định là nhiều tiến trình `next dev` chạy song song dùng chung thư mục `.next` (runtime web + runtime e2e), gây collision asset/manifest.

Đã triển khai fix bền vững bằng cách tách thư mục build theo từng dev instance (`.next-dev-web`, `.next-dev-e2e*`) và chuẩn hóa command chạy e2e để không đè cache của runtime chính.

## CÔNG VIỆC ĐÃ THỰC HIỆN

### ✅ Hoàn thành
- Reproduce lỗi:
  - HTML trang chủ trả bình thường.
  - CSS `/_next/static/css/app/layout.css?v=...` trả `404`.
- Sửa cấu hình runtime web:
  - `apps/web/next.config.mjs`: dùng `distDir` theo `NEXT_DEV_INSTANCE` ở phase dev.
  - Giữ production build tại `.next`.
- Sửa scripts:
  - `apps/web/package.json`: `dev` đọc `NEXT_DEV_INSTANCE` + `PORT`.
  - `apps/web/e2e/playwright.config.ts`: chạy webServer với `NEXT_DEV_INSTANCE=e2e`.
- Hygiene:
  - `.gitignore`: thêm ignore `apps/web/.next-dev-*/`.
- Verify:
  - Chạy song song 2 dev runtime (3000 + 3110) -> CSS endpoint đều `200`.
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅

### 🔄 Đang dở
- Chưa chạy full e2e suite sau hotfix (đã verify trực tiếp endpoint CSS + build/lint).

### ❌ Chưa làm / Bỏ qua
- Không thay đổi business logic ERP; chỉ xử lý lớp runtime/dev workflow để chống tái phát lỗi giao diện.

## THAY ĐỔI FILE

```text
ADDED:
  .agent/sessions/2026-03-31_1431_codex.md

MODIFIED:
  .gitignore
  apps/web/next.config.mjs
  apps/web/package.json
  apps/web/e2e/playwright.config.ts
  .agent/memory/CONTEXT_SNAPSHOT.md
  planning/CURRENT_TASK.md
```

## QUYẾT ĐỊNH TRONG SESSION NÀY

| Quyết định | Lý do | Thay thế đã cân nhắc |
|---|---|---|
| Tách `distDir` theo `NEXT_DEV_INSTANCE` cho `next dev` | Loại bỏ collision `.next` giữa runtime web và e2e | Tiếp tục dùng chung `.next` + restart thủ công khi lỗi |
| Giữ `.next` cho production build | Tương thích pipeline build/deploy hiện tại | Đổi toàn bộ distDir cho mọi phase |

## VẤN ĐỀ PHÁT SINH

| Vấn đề | Đã xử lý chưa | Cách xử lý / TODO |
|---|---|---|
| `ERR_MODULE_NOT_FOUND` khi import `next/constants` trong ESM config | ✅ | Đổi import sang `next/constants.js` |
| Script dev bị trùng `--port` khi chạy e2e | ✅ | Chuyển sang truyền `PORT=3100` thay vì append thêm `-- --port` |

## HANDOFF CHO AGENT TIẾP THEO

**Context cần biết:**
- Lỗi mất style tái phát đã được xử lý tại nguồn bằng tách cache dev theo instance.
- Khi chạy e2e/web song song, bắt buộc giữ `NEXT_DEV_INSTANCE` khác nhau để tránh đè asset.

**Bước tiếp theo cụ thể:**
1. Nếu cần, chạy lại full Playwright suite để xác nhận toàn bộ flow UI sau thay đổi runtime script.
2. Giữ nguyên convention mới cho mọi command khởi chạy `next dev` phụ trợ (e2e/smoke) bằng `NEXT_DEV_INSTANCE` riêng.

**Lệnh verify nhanh:**
```bash
npm run dev --workspace @erp/web
NEXT_DEV_INSTANCE=e2e PORT=3100 npm run dev --workspace @erp/web
npm run lint --workspace @erp/web
npm run build --workspace @erp/web
```
