# Session Log - 2026-04-06 11:59 +07

## Scope
- Cố định phần header + tìm kiếm khi thao tác trang `Zalo Automation > Tin nhắn`.
- Giới hạn cuộn vào đúng vùng nội dung:
  - danh sách hội thoại (trái),
  - luồng tin nhắn (phải).

## Findings
- `shell-main` trước đó không khóa chiều cao theo viewport nên page ngoài vẫn cuộn.
- Khi wheel event lan lên parent, header/tìm kiếm bị trôi khỏi màn hình.
- Dù list đã có `overflow: auto`, thiếu cơ chế chống scroll chaining khiến parent tiếp tục cuộn.

## Changes
- `apps/web/app/styles/layout.css`
  - `shell-main`:
    - `height: 100vh`
    - `display: flex`
    - `flex-direction: column`
    - `overflow: hidden`
  - `app-content`:
    - `display: flex`
    - `flex-direction: column`
    - `flex: 1 1 auto`
    - `min-height: 0`
    - `overflow: auto`
- `apps/web/components/zalo-automation-messages-workbench.tsx`
  - thêm class `zalo-chat-workbench` ở root article.
- `apps/web/app/styles/modules/crm.css`
  - thêm `.zalo-chat-workbench` (full-height, flex column).
  - `.zalo-chat-layout` chuyển sang `flex: 1` + `min-height: 0`.
  - panel chat/thread đặt `overflow: hidden`.
  - list cuộn:
    - `.zalo-chat-thread-list`
    - `.zalo-chat-message-list`
    thêm `overscroll-behavior: contain`.

## Verification
- `npm run -w apps/web build` ✅
- `npm run -w apps/web lint` ✅
- `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/conversations-inbox.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`4 passed`)

## Stability Gate
- `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
- `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)
