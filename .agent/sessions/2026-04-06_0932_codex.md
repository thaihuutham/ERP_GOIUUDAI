# Session Log - 2026-04-06 09:32 +07

## Scope
Điều tra và fix lỗi: khách mới nhắn vào Zalo cá nhân đã đăng nhập nhưng không thấy hội thoại mới ở trang Tin nhắn.

## Changes
- `apps/api/src/modules/zalo/zalo-personal.pool.service.ts`
  - Harden parser trong `attachListener` cho event `message`:
    - resolve `senderExternalId` với fallback nhiều key (`uidFrom`, `fromUid`, `uid`, ...).
    - resolve `externalThreadId` với fallback (`threadId`, `conversationId`, ...).
    - resolve `senderName`/`externalMessageId` với fallback nhiều key.
  - Thêm warning log khi không resolve được thread id để tránh silent drop.
  - Thêm helper `parseZaloSentAt(...)` để chuẩn hóa timestamp theo đơn vị:
    - seconds -> milliseconds,
    - microseconds/nanoseconds -> milliseconds,
    - giữ nguyên milliseconds.
  - Dùng `parseZaloSentAt(...)` khi ingest inbound message để `lastMessageAt` và thứ tự thread đúng.
  - Thêm helper `pickFirstNonEmptyString(...)` để gom logic fallback key.
- `apps/api/test/zalo-personal.pool.service.test.ts`
  - Thêm 3 unit tests cho timestamp normalization (seconds/ms/microseconds).

## Verification
- `npm run -w apps/api test -- --run test/zalo-personal.pool.service.test.ts` ✅ (`3 passed`)
- `npm run -w apps/api test -- --run test/conversations.realtime.test.ts` ✅ (`2 passed`)
- `npm run -w apps/api lint` ✅
- `npm run -w apps/api build` ✅
- Stability gate:
  - `docker ps` có `erp-postgres` Up ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run -w apps/api prisma:migrate:status` ✅ (`Database schema is up to date!`)

## Notes
- Worktree đang có nhiều thay đổi trước đó; session này chỉ can thiệp trực tiếp vào listener parse + timestamp normalization cho inbound personal message và thêm test timestamp tương ứng.
