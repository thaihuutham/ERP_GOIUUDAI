# Session Log — 2026-04-09 09:59 +07

## User request
- Xử lý blocker web build để `phase4:release-gate` pass end-to-end.

## Work completed
- Điều tra lỗi web build trong release gate:
  - `next build` fail với `/404` và lỗi `<Html> should not be imported outside of pages/_document`.
  - xác định nguyên nhân là gate script source `.env` với `NODE_ENV=development`.
- Cập nhật `scripts/quality/run-phase4-release-gate.sh`:
  - bước web build dùng `NODE_ENV=production npm run build --workspace @erp/web`.

## Verification
- `set -a; source .env; set +a; NODE_ENV=production npm run build --workspace @erp/web` ✅
- `set -a; source .env; set +a; npm run phase4:release-gate` ✅
  - infra/prisma/api lint+build+tests/web lint+build/phase3 stabilization/phase4 prod smoke đều pass.
- Trong phase4 prod smoke:
  - callback endpoint `/integrations/payments/bank-events` trả `404`,
  - script skip đúng theo default `SMOKE_PAYMENT_CALLBACK_REQUIRED=false`.

## Notes
- Blocker web build trong gate đã được xử lý.
- Không thay đổi business logic ERP.
