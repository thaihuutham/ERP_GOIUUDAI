# Session Log - 2026-04-05 18:13 +07

## Scope
Tiếp tục IAM rollout theo kế hoạch, hoàn tất Task 10 (web IAM admin UI + access policy adaptation) và Task 11 (rollout docs + stability gate + handoff).

## Files Changed
- `apps/web/lib/access-policy.ts`
- `apps/web/components/access-policy-context.tsx`
- `apps/web/components/settings-center.tsx`
- `apps/web/lib/__tests__/access-policy.test.ts`
- `apps/web/e2e/tests/settings-center-reports.spec.ts`
- `docs/deployment/VM_AUTODEPLOY.md`
- `planning/CURRENT_TASK.md`
- `.agent/memory/CONTEXT_SNAPSHOT.md`
- `.agent/sessions/2026-04-05_1813_codex.md`

## Key Outcomes
1. Access policy web đã map role runtime theo IAM v2 mode:
   - `ADMIN` giữ nguyên,
   - `MANAGER/STAFF` map về `USER` khi `access_security.iamV2.enabled=true` và `mode=ENFORCE`.
2. `AccessPolicyProvider` nạp thêm domain `access_security` để xác định `iamV2Enabled` ngay ở runtime snapshot.
3. Settings Center đã có cụm IAM admin controls trong domain `access_security`:
   - scope override theo user,
   - title scope mapping,
   - mismatch report viewer + filter.
4. Đã sửa lỗi build web:
   - `normalizeIamMismatchReport` không còn union `(item | null)[]`, đảm bảo `IamMismatchReportItem[]` type-safe.
5. Đã harden e2e test domain tabs trong settings center bằng helper click retry để giảm flaky do DOM detach khi rerender.
6. Đã cập nhật runbook deploy:
   - thêm `IAM_V2_ENABLED`,
   - thêm smoke endpoint mismatch report,
   - thêm hướng dẫn rollout/rollback IAM v2 theo module.

## Verification
- Frontend:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
  - `npm run test:unit --workspace @erp/web` ✅ (`13 passed`)
  - `CI=1 PLAYWRIGHT_PORT=4310 npx playwright test apps/web/e2e/tests/access-policy-hardening.spec.ts apps/web/e2e/tests/settings-center-reports.spec.ts --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`11 passed`)
- Backend sanity:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run build --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/settings-enterprise.service.test.ts test/permission.guard.test.ts` ✅ (`13 passed`)
- Stability gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'` ✅ (`erp-postgres` Up)
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)

## Blockers / External
- Không có blocker trong phạm vi Task 10/11.

## Notes
- Không thay đổi business logic ERP cũ; thay đổi tập trung vào adapter IAM v2 ở frontend, độ ổn định test, và vận hành rollout.
