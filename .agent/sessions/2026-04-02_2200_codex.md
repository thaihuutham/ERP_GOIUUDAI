# Session Log - 2026-04-02 22:00 +07

## Muc tieu
Xu ly loi runtime khi user bam thao tac exempt-day tren bang attendance:
- `Cannot POST /api/v1/hr/attendance/exempt-day`
- `Cannot DELETE /api/v1/hr/attendance/exempt-day?...`
va loi ket noi DB `Can't reach database server at localhost:55432`.

## Chan doan
- API runtime tren cong `3001` dang la process cu, chua nap route moi `exempt-day`.
- Kiem tra truc tiep truoc khi restart:
  - `POST /api/v1/hr/attendance/exempt-day` => `404`.
- DB local da up lai va API sau restart ket noi duoc.

## Xu ly
- Restart API dev process (`apps/api`, `npm run dev`).
- Verify sau restart:
  - `POST /api/v1/hr/attendance/exempt-day` => `201`.
  - `DELETE /api/v1/hr/attendance/exempt-day?...` => `200`.
  - `GET /api/v1/hr/attendance/monthly` tiep tuc `200`.

## Verify nhanh
- `lsof -nP -iTCP:3001 -sTCP:LISTEN` ✅
- `curl -X POST /api/v1/hr/attendance/exempt-day` ✅ (`201`)
- `curl -X DELETE /api/v1/hr/attendance/exempt-day?...` ✅ (`200`)

## Ghi chu
- Neu gap lai loi `Can't reach database server at localhost:55432`, can dam bao container `erp-postgres` dang `Up` truoc khi start API.
