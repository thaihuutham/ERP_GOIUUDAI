# Session Log - 2026-04-08 12:26 +07

## User request
- Kết nối trợ lý AI hiện có với Zalo Personal (zca-js) để AI tự trả lời khách.
- Chỉ chạy auto-reply cho các tài khoản Zalo đã bật cờ cho phép.
- Có takeover người thật: khi nhân viên nhắn thủ công thì AI tạm ngưng 5 phút; nếu sau tin nhắn cuối của khách 5 phút nhân viên không trả lời thì AI trả lời lại.
- Có nút bật/tắt nhanh auto-reply khi đang chat.

## Work completed
1. Data model + migration
- Thêm cờ theo tài khoản Zalo:
  - `aiAutoReplyEnabled` (default `false`)
  - `aiAutoReplyTakeoverMinutes` (default `5`)
- Tạo migration `20260408130000_add_zalo_auto_reply_account_flags`.

2. Backend auto-reply engine for Zalo Personal
- Thêm utility metadata thread: `zalo-auto-reply-state.util.ts`.
- Thêm service AI reply: `zalo-auto-reply.service.ts` (dùng runtime AI settings hiện hữu).
- `zalo-personal.pool.service.ts`:
  - ingest tin khách -> quyết định auto-reply ngay hoặc defer theo takeover.
  - scheduler defer theo `pendingDueAt`.
  - hủy timer khi takeover/manual.
- `zalo.service.ts`:
  - hỗ trợ create/update account với 2 cờ auto-reply.
  - thêm `origin` cho personal send (`USER|CAMPAIGN|AI|SYSTEM`).
  - gửi tay `origin=USER` sẽ set `pauseUntil` cho thread (takeover).
- `zalo-campaign.service.ts` gắn `origin=CAMPAIGN` để không kích hoạt takeover nhầm.

3. UI controls
- `zalo-automation-accounts-workbench.tsx`:
  - hiển thị trạng thái `AI ON/OFF`.
  - bật/tắt auto-reply theo account.
- `zalo-automation-messages-workbench.tsx` và `crm-conversations-workbench.tsx`:
  - thêm nút bật/tắt nhanh auto-reply ngay trong màn chat.

4. Tests + documentation
- Cập nhật test:
  - `apps/api/test/zalo.service.test.ts`
  - `apps/api/test/zalo-personal.pool.service.test.ts`
- Tạo ADR:
  - `docs/decisions/ADR-054-ZALO-PERSONAL-AI-AUTO-REPLY-PER-ACCOUNT-TAKEOVER.md`.

## Verification evidence
- Infra/DB gate:
  - `docker ps --format 'table {{.Names}}\t{{.Status}}'` ✅
  - `lsof -nP -iTCP:55432 -sTCP:LISTEN` ✅
- Prisma:
  - `npm run prisma:generate --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:deploy --workspace @erp/api` ✅
  - `set -a; source .env; set +a; npm run prisma:migrate:status --workspace @erp/api` ✅ (`Database schema is up to date!`)
- Backend quality:
  - `npm run lint --workspace @erp/api` ✅
  - `npm run test --workspace @erp/api -- test/zalo.service.test.ts test/zalo-personal.pool.service.test.ts` ✅
  - `npm run build --workspace @erp/api` ✅
- Frontend quality:
  - `npm run lint --workspace @erp/web` ✅
  - `npm run build --workspace @erp/web` ✅
  - `CI=1 PLAYWRIGHT_PORT=4310 NEXT_PUBLIC_REMOTE_IDLE_TIMEOUT_MS=1000 npx playwright test apps/web/e2e/tests/zalo-automation-accounts-actions.spec.ts apps/web/e2e/tests/conversations-inbox.spec.ts --workers=2 --config=apps/web/e2e/playwright.config.ts --reporter=line` ✅ (`8 passed`)

## Notes
- Phạm vi giữ đúng yêu cầu: chỉ Zalo Personal, không mở rộng sang OA auto-reply.
- Không thay đổi logic nghiệp vụ ERP ngoài luồng Zalo auto-reply/takeover.
