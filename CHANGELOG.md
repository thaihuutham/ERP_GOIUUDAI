# Changelog

Tài liệu theo chuẩn Keep a Changelog.

## [Unreleased]
### Added
- Chuẩn hóa cấu trúc dự án theo template stateless agent.
- Bổ sung tài liệu kiến trúc cho quy mô 2 triệu khách hàng.
- Bổ sung runbook deploy VM tự động qua GitHub Actions.
- Thêm CI workflow và deploy workflow mẫu.

### Changed
- Dời `firestore.rules` sang `firestore/firestore.rules`.
- Dời `firebase-applet-config.json` vào `config/` và cập nhật import.
- Chuẩn hóa hệ thống docs/planning/.agent cho handoff đa-agent.

### Fixed
- Chuẩn hóa cấu trúc để giảm phụ thuộc vào trí nhớ session AI.
